# 90.

**1. 문제 원문**

While reviewing a diff from Claude Code that adds a background job scheduler, an architect notices two problems: the retry backoff logic uses the same shared timer instance that the job-locking logic also relies on to prevent double execution, so changing one affects the other; and separately, a log message uses the wrong log level and needs a one-word fix. How should the architect sequence feedback on these two problems?

A) Report the log-level fix as a separate immediate change requiring confirmation, then after it is resolved describe the retry backoff and job-locking interaction that shares the same timer, ensuring they are addressed together and not conflated with the log-level fix.

B) Describe the retry backoff and job-locking interaction in one detailed message because they depend on the same shared timer, and separately note the log-level fix as an unrelated change that should be addressed separately.

C) Combine all three items—the retry backoff logic, the job-locking logic, and the log-level fix—into a single message and instruct Claude to treat them as one inseparable change that must be implemented together due to their shared timer dependency.

D) List all three issues as a numbered list, with each item named by its symptom (e.g., 'retry backoff', 'job-locking', 'log-level') while intentionally leaving out the fact that the retry backoff and job-locking logic are coupled through the shared timer.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**While reviewing a diff**
diff(변경 사항)를 검토하는 동안

**from Claude Code**
Claude Code로부터의

**that adds**
추가하는

**a background job scheduler,**
백그라운드 작업 스케줄러를

**an architect notices**
아키텍터는 알아차립니다

**two problems:**
두 가지 문제를:

**the retry backoff logic**
재시도 백오프 로직이

**uses the same shared timer instance**
동일한 공유 타이머 인스턴스를 사용한다는 것을

**that the job-locking logic**
작업 잠금 로직이

**also relies on**
또한 의존하는

**to prevent double execution,**
중복 실행을 방지하기 위해

**so changing one**
따라서 하나를 변경하는 것이

**affects the other;**
다른 하나에 영향을 미칩니다;

**and separately,**
그리고 별개로,

**a log message**
로그 메시지가

**uses the wrong log level**
잘못된 로그 수준을 사용하고 있어

**and needs a one-word fix.**
한 단어 수정이 필요합니다.

**How should the architect**
아키텍트는 어떻게 해야 합니까

**sequence feedback**
피드백의 순서를 구성해야

**on these two problems?**
이 두 가지 문제에 대해?

---

**OPTIONS:**

**Option A:**

**Report the log-level fix**
로그 수준 수정을 보고하고

**as a separate immediate change**
별도의 즉각적인 변경 사항으로

**requiring confirmation,**
확인이 필요한

**then after it is resolved**
그런 다음 이것이 해결된 후에

**describe the retry backoff**
재시도 백오프와

**and job-locking interaction**
작업 잠금의 상호작용을 설명하여

**that shares the same timer,**
동일한 타이머를 공유하는

**ensuring they are addressed together**
이들이 함께 처리되도록 하고

**and not conflated**
혼동되지 않도록 합니다

**with the log-level fix.**
로그 수준 수정과

**Option B:**

**Describe the retry backoff**
재시도 백오프와

**and job-locking interaction**
작업 잠금 상호작용을 설명하고

**in one detailed message**
하나의 상세한 메시지로

**because they depend on**
의존하기 때문에

**the same shared timer,**
동일한 공유 타이머에

**and separately note**
그리고 별도로 기록합니다

**the log-level fix**
로그 수준 수정을

**as an unrelated change**
관련 없는 변경 사항으로

**that should be addressed separately.**
별개로 처리되어야 하는

**Option C:**

**Combine all three items—**
세 가지 항목 모두를 결합하여—

**the retry backoff logic,**
재시도 백오프 로직,

**the job-locking logic,**
작업 잠금 로직,

**and the log-level fix—**
그리고 로그 수준 수정을—

**into a single message**
단일 메시지로

**and instruct Claude**
그리고 Claude에게 지시합니다

**to treat them**
이것들을 다루도록

**as one inseparable change**
하나의 분리할 수 없는 변경 사항으로

**that must be implemented together**
함께 구현되어야 하는

**due to their shared timer dependency.**
공유 타이머 의존성 때문에

**Option D:**

**List all three issues**
세 가지 문제를 모두 나열합니다

**as a numbered list,**
번호가 매겨진 목록으로

**with each item named**
각 항목이 이름 붙여진 채로

**by its symptom**
그 증상에 의해

**(e.g., 'retry backoff', 'job-locking', 'log-level')**
(예: '재시도 백오프', '작업 잠금', '로그 수준')

**while intentionally leaving out**
의도적으로 제외하면서

**the fact that**
사실을

**the retry backoff and job-locking logic**
재시도 백오프와 작업 잠금 로직이

**are coupled**
결합되어 있다는

**through the shared timer.**
공유 타이머를 통해

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**B번**: Describe the retry backoff and job-locking interaction in one detailed message because they depend on the same shared timer, and separately note the log-level fix as an unrelated change that should be addressed separately.

**정답 및 해설:**


**핵심 개념**: **LLM 프롬프트 및 코드 피드백 모범 사례 (Prompt Engineering & Context Grouping)**
AI 코딩 에이전트(Claude Code 등)에 피드백을 전달할 때는 의존성이 있는 논리 구조는 하나의 맥락으로 묶고, 의존성이 없고 성격이 완전히 다른 독립적 수정 사항은 분리하여 전달해야 모델의 환각이나 엉뚱한 결합을 방지할 수 있습니다.

**문제 상황 분석:**
- 재시도 백오프(retry backoff)와 작업 잠금(job-locking)은 공유 타이머 인스턴스라는 동일한 의존성을 가지고 서로 영향을 주는 상호연관적 문제입니다.
- 로그 수준(log level) 변경은 단순 오타/단어 수정 수준의 무관한 독립적 문제입니다.
- 아키텍트는 두 커플링된 문제와 하나의 독립된 문제를 AI에게 효율적으로 분리/그룹화하여 전달하는 방법을 찾아야 합니다.

**B번이 정답인 이유:**
- 동일한 공유 타이머 인스턴스를 사용하는 두 로직(재시도 백오프, 작업 잠금)은 하나의 메시지에 상세히 묶어 설명함으로써 LLM이 두 로직 간의 상호작용과 타이머 공유 문제를 한 번에 이해하고 해결할 수 있게 합니다.
- 로그 수준 수정은 상호작용 로직과 무관한 변경 사항이므로 별도의 지시로 구분하여 처리하게 하는 것이 가장 명확하고 효과적인 피드백 전달 방식입니다.

**오답 분석:**
- Option A (오답): 로그 수준 수정을 단순한 순서상 이유로 먼저 처리할 때까지 기다렸다가 다음 피드백을 진행하는 식의 불필요한 직렬화(sequential block)는 개발 피드백 흐름을 비효율적으로 만듭니다.
- Option C (오답): 타이머 의존성과 아무 상관이 없는 로그 수준 수정까지 하나의 분리 불가능한(inseparable) 변경으로 묶어 버리면 모델이 오개념을 학습하거나 잘못된 코드 리팩토링을 유발합니다.
- Option D (오답): 두 로직이 공유 타이머로 결합(coupled)되어 있다는 핵심 원인/맥락을 숨긴 채 증상만 나열하면 AI가 근본적인 아키텍처 문제를 해결하지 못합니다.

---

# 91.

**1. 문제 원문**

A developer builds a `/fix-issue` skill that expects an issue number as input, such as `/fix-issue 482`. When teammates invoke it without typing a number, they get confused about what to supply. Which frontmatter field should the developer add to `SKILL.md` so the autocomplete menu shows teammates what to type?

A) `disable-model-invocation: true`, which forces teammates to always type an issue number when invoking the command manually.

B) `arguments: issue-number`, which validates that the typed value is numeric before the skill is allowed to run.

C) `description: Requires an issue number argument`, which Claude reads aloud to the user before executing the skill.

D) `argument-hint: [issue-number]`, which displays a placeholder in the autocomplete menu without changing how the skill executes.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A developer builds**
한 개발자가 만듭니다

**a `/fix-issue` skill**
`/fix-issue` 스킬을

**that expects**
기대하는 (필요로 하는)

**an issue number as input,**
입력값으로 이슈 번호를

**such as `/fix-issue 482`.**
`/fix-issue 482`와 같은

**When teammates invoke it**
팀원들이 이것을 호출할 때

**without typing a number,**
번호를 입력하지 않고

**they get confused**
그들은 혼란스러워합니다

**about what to supply.**
무엇을 제공해야 할지에 대해

**Which frontmatter field**
어떤 프론트매터(frontmatter) 필드를

**should the developer add**
개발자가 추가해야 합니까

**to `SKILL.md`**
`SKILL.md` 파일에

**so the autocomplete menu**
자동 완성 메뉴가

**shows teammates**
팀원들에게 보여주도록

**what to type?**
무엇을 입력해야 하는지?

---

**OPTIONS:**

**Option A:**

**`disable-model-invocation: true`,**
`disable-model-invocation: true`

**which forces teammates**
팀원들이 강제로 ~하도록 만드는

**to always type**
항상 입력하게

**an issue number**
이슈 번호를

**when invoking the command manually.**
명령어를 수동으로 호출할 때

**Option B:**

**`arguments: issue-number`,**
`arguments: issue-number`

**which validates**
검증하는

**that the typed value**
입력된 값이

**is numeric**
숫자인지

**before the skill is allowed to run.**
스킬 실행이 허용되기 전에

**Option C:**

**`description: Requires an issue number argument`,**
`description: Requires an issue number argument`

**which Claude reads aloud**
Claude가 소리 내어 읽어주는

**to the user**
사용자에게

**before executing the skill.**
스킬을 실행하기 전에

**Option D:**

**`argument-hint: [issue-number]`,**
`argument-hint: [issue-number]`

**which displays a placeholder**
플레이스홀더(위치 표시자)를 표시하는

**in the autocomplete menu**
자동 완성 메뉴에

**without changing**
변경하지 않고

**how the skill executes.**
스킬이 실행되는 방식을

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: `argument-hint: [issue-number]`, which displays a placeholder in the autocomplete menu without changing how the skill executes.

**정답 및 해설:**


**핵심 개념**: **Claude Code SKILL.md Frontmatter 구성 요소 (argument-hint)**
Claude Code의 커스텀 스킬 정의 파일(`SKILL.md`)은 메타데이터를 작성하는 Frontmatter 영역을 제공합니다. 이 중 `argument-hint` 필드는 사용자가 CLI 상에서 스킬(명령어)을 입력하거나 자동 완성을 사용할 때 입력해야 하는 인자의 안내문구(Placeholder)를 시각적으로 보여주는 역할을 합니다.

**문제 상황 분석:**
- 개발자가 `/fix-issue`라는 커스텀 스킬을 작성했으나 인자(이슈 번호)가 필수적인 구조입니다.
- 팀원들이 인자 입력 없이 명령어를 호출하여 어떤 값을 전달해야 할지 혼란을 겪고 있습니다.
- CLI 자동 완성(Autocomplete) 메뉴에서 입력 플레이스홀더를 통해 사용자에게 힌트를 제공할 수 있는 Frontmatter 설정이 필요합니다.

**D번이 정답인 이유:**
- `argument-hint` 필드는 CLI 자동 완성 UI에 플레이스홀더 텍스트(예: `[issue-number]`)를 노출하는 용도로 설계되었습니다.
- 스킬의 실제 동작 로직에는 영향을 주지 않으면서, 사용자가 명령어를 작성할 때 어떤 형태의 인자를 입력해야 하는지 명확하게 가이드를 제시합니다.

**오답 분석:**
- Option A (오답): `disable-model-invocation` 속성은 모델이 이 스킬을 자동으로 호출하는 것을 막고 사용자의 수동 호출만 허용할 때 사용되는 옵션이며, 자동 완성 힌트 표시 기능이 아닙니다.
- Option B (오답): Claude Code의 `SKILL.md` Frontmatter에는 인자 유효성 검사를 수행하는 `arguments:`라는 표준 설정 필드가 존재하지 않습니다.
- Option C (오답): `description` 속성은 스킬이 어떤 동작을 하는지 모델 및 사용자에게 알리는 설명글이며, Claude가 소리 내어 읽어주거나 자동 완성 메뉴의 인자 힌트 플레이스홀더로 동작하지 않습니다.

---

# 92.

**1. 문제 원문**

An architect is evaluating whether a requested change to a billing module needs plan mode. The change touches one file, has a single obvious implementation matching an existing pattern used elsewhere in the same file, and does not affect any other module. Which factor most strongly indicates that direct execution, rather than plan mode, is the right choice here?

A) The change is confined to a single file, has one clear implementation path, and touches no other module in the codebase

B) The existing pattern being followed in the file was written by a different engineer than the one requesting this change

C) The change can be described to Claude in a single short sentence, regardless of how many modules it actually ends up touching

D) The billing module is business-critical, so every change to it should always go through the exact same fixed review process

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**An architect is evaluating**
한 아키텍트가 평가하고 있습니다

**whether a requested change**
요청된 변경 사항이 ~해야 하는지를

**to a billing module**
결제 모듈에 대한

**needs plan mode.**
플랜 모드(plan mode)를 필요로 하는지를.

**The change touches one file,**
그 변경 사항은 하나의 파일을 다루고,

**has a single obvious implementation**
명확한 단일 구현 방식을 가지며

**matching an existing pattern**
기존 패턴과 일치하는

**used elsewhere in the same file,**
동일한 파일의 다른 곳에서 사용된,

**and does not affect**
그리고 영향을 미치지 않습니다

**any other module.**
다른 어떤 모듈에도.

**Which factor**
어떤 요인이

**most strongly indicates**
가장 강력하게 나타냅니까

**that direct execution,**
직접 실행이,

**rather than plan mode,**
플랜 모드보다는,

**is the right choice here?**
여기서 올바른 선택이라는 것을?

---

**OPTIONS:**

**Option A:**

**The change is confined**
변경 사항이 국한되어 있고

**to a single file,**
단일 파일에,

**has one clear implementation path,**
하나의 명확한 구현 경로를 가지며,

**and touches no other module**
다른 어떤 모듈도 다루지 않는다는 점

**in the codebase**
코드베이스 내의

**Option B:**

**The existing pattern**
기존 패턴이

**being followed in the file**
파일에서 따르고 있는

**was written by a different engineer**
다른 엔지니어에 의해 작성되었다는 점

**than the one**
엔지니어와는 다른

**requesting this change**
이 변경을 요청하는

**Option C:**

**The change can be described**
변경 사항이 설명될 수 있다는 점

**to Claude**
Claude에게

**in a single short sentence,**
단 하나의 짧은 문장으로,

**regardless of how many modules**
얼마나 많은 모듈을

**it actually ends up touching**
실제로 결국 다루게 되는지와 상관없이

**Option D:**

**The billing module**
결제 모듈이

**is business-critical,**
비즈니스에 치명적(중요)이므로,

**so every change to it**
따라서 이에 대한 모든 변경 사항은

**should always go through**
항상 거쳐야 한다는 점

**the exact same fixed review process**
정확히 동일한 고정된 검토 프로세스를

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**A번**: The change is confined to a single file, has one clear implementation path, and touches no other module in the codebase

**정답 및 해설:**


**핵심 개념**: **Claude Code Plan Mode vs Direct Execution (플랜 모드 vs 직접 실행)**
Claude Code에서 Plan Mode(플랜 모드)는 아키텍처 구상, 여러 파일에 걸친 복잡한 리팩토링, 또는 구현 방향성이 모호하여 사전에 계획을 검토해야 할 때 사용됩니다. 반면 변경 범위가 단일 파일로 국한되고, 구현 경로가 매우 명확하며, 타 모듈에 부작용(side effect)을 주지 않는 단순 작업의 경우 Direct Execution(직접 실행)을 사용하는 것이 효율적입니다.

**문제 상황 분석:**
- 변경하려는 작업은 단 1개의 파일만 수정합니다.
- 동일 파일 내 기존 패턴을 그대로 따르는 명확하고 단일한 구현 경로가 존재합니다.
- 다른 모듈에 영향(side effect)을 주지 않는 격리된 작업입니다.

**A번이 정답인 이유:**
- 문제에서 제시된 조건(단일 파일 수정, 명확한 단일 구현 방식, 타 모듈 미영향)을 가장 직접적이고 충실하게 종합하여 설명하고 있습니다.
- 사전에 복잡한 탐색이나 모의 계획 수립 단계(Plan Mode)를 거칠 필요 없이, 바로 코드 수정 작업을 진행(Direct Execution)하기에 완벽한 조건입니다.

**오답 분석:**
- Option B (오답): 코드를 과거에 누가 작성했는지의 인적 요소는 플랜 모드와 직접 실행 중 어떤 방식을 선택할지의 기술적 판단 기준이 되지 않습니다.
- Option C (오답): 문장이 짧다고 해서 영향 범위가 작은 것은 아닙니다. 실제로 결과적으로 여러 모듈을 수정해야 하는 작업이라면 짧은 설명과 상관없이 반드시 Plan Mode를 통해 사이드 이펙트를 검토해야 합니다.
- Option D (오답): 비즈니스 중요도가 높더라도 변경 범위와 복잡도가 매우 작다면 모든 작업에 무조건 동일한 엄격한 계획 단계를 강제하는 것은 불필요한 오버헤드를 발생시킵니다.

---

# 93.

**1. 문제 원문**

An engineer launches Claude Code from the `services/billing/` directory inside a larger repository. The repository contains three CLAUDE.md files: one at the repository root, one at `services/billing/CLAUDE.md`, and one at `services/billing/reports/CLAUDE.md`. The engineer has not yet read or edited any files in the `services/billing/reports/` subdirectory. At the moment the session starts, which files are loaded into context?

A) Only the root CLAUDE.md loads at launch; `services/billing/` and the `reports` subdirectory file both wait until Claude reads a file there.

B) Only `services/billing/CLAUDE.md` loads, since directory-level files never combine with ancestor files unless explicitly imported first.

C) All three CLAUDE.md files load immediately, because Claude Code always preloads every CLAUDE.md found anywhere under the working directory.

D) The root and `services/billing/CLAUDE.md` load at launch; `reports/CLAUDE.md` loads later, only when Claude reads a file in that subdirectory.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**An engineer launches**  
한 엔지니어가 실행합니다  

**Claude Code**  
Claude Code를  

**from the `services/billing/` directory**  
`services/billing/` 디렉터리에서  

**inside a larger repository.**  
더 큰 리포지토리 내부의.  

**The repository contains**  
리포지토리에는 포함되어 있습니다  

**three CLAUDE.md files:**  
세 개의 CLAUDE.md 파일이:  

**one at the repository root,**  
하나는 리포지토리 루트에,  

**one at `services/billing/CLAUDE.md`,**  
하나는 `services/billing/CLAUDE.md`에,  

**and one at `services/billing/reports/CLAUDE.md`.**  
그리고 하나는 `services/billing/reports/CLAUDE.md`에.  

**The engineer has not yet read**  
엔지니어는 아직 읽거나  

**or edited any files**  
어떤 파일도 수정하지 않았습니다  

**in the `services/billing/reports/` subdirectory.**  
`services/billing/reports/` 하위 디렉터리 안의.  

**At the moment the session starts,**  
세션이 시작되는 시점에,  

**which files are loaded**  
어떤 파일들이 로드됩니까  

**into context?**  
컨텍스트 안으로?  

---

**OPTIONS:**

**Option A:**

**Only the root CLAUDE.md loads**  
루트 CLAUDE.md만 로드됩니다  

**at launch;**  
시작 시에;  

**`services/billing/` and the `reports` subdirectory file**  
`services/billing/` 및 `reports` 하위 디렉터리 파일은  

**both wait**  
둘 다 대기합니다  

**until Claude reads a file there.**  
Claude가 그곳의 파일을 읽을 때까지.  

**Option B:**

**Only `services/billing/CLAUDE.md` loads,**  
`services/billing/CLAUDE.md`만 로드됩니다,  

**since directory-level files**  
디렉터리 수준 파일은  

**never combine with ancestor files**  
상위(조상) 파일과 절대 결합되지 않기 때문에  

**unless explicitly imported first.**  
명시적으로 먼저 임포트되지 않는 한.  

**Option C:**

**All three CLAUDE.md files load**  
세 개의 CLAUDE.md 파일이 모두 로드됩니다  

**immediately,**  
즉시,  

**because Claude Code always preloads**  
Claude Code가 항상 사전 로드하기 때문에  

**every CLAUDE.md found**  
발견되는 모든 CLAUDE.md를  

**anywhere under the working directory.**  
작업 디렉터리 하위의 어디에서든.  

**Option D:**

**The root and `services/billing/CLAUDE.md` load**  
루트 및 `services/billing/CLAUDE.md`가 로드됩니다  

**at launch;**  
시작 시에;  

**`reports/CLAUDE.md` loads later,**  
`reports/CLAUDE.md`는 나중에 로드됩니다,  

**only when Claude reads a file**  
Claude가 파일을 읽을 때만  

**in that subdirectory.**  
해당 하위 디렉터리 안의.  

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: The root and `services/billing/CLAUDE.md` load at launch; `reports/CLAUDE.md` loads later, only when Claude reads a file in that subdirectory.

**정답 및 해설:**


**핵심 개념**: **Claude Code의 CLAUDE.md 컨텍스트 로딩 계층 구조 및 온디맨드 로딩 (On-Demand Loading)**
Claude Code는 세션 실행 시 작업 디렉터리(Working Directory)와 그 상위(Ancestor) 상위 디렉터리의 `CLAUDE.md` 파일들을 자동으로 병합하여 기본 컨텍스트로 로드합니다. 반면, 하위 디렉터리(Subdirectory)에 위치한 `CLAUDE.md` 파일은 세션 시작 시 즉시 로드되지 않고, 모델이 해당 하위 디렉터리 내의 파일을 접근/읽기 시작할 때 비로소 컨텍스트에 추가(On-demand)됩니다.

**문제 상황 분석:**
- 세션 시작 위치(작업 디렉터리): `services/billing/`
- 존재 파일:
  1. 루트 `CLAUDE.md` (상위 디렉터리)
  2. `services/billing/CLAUDE.md` (현재 작업 디렉터리)
  3. `services/billing/reports/CLAUDE.md` (하위 디렉터리)
- 엔지니어는 아직 `services/billing/reports/` 하위 디렉터리 내의 어떠한 파일도 탐색하거나 편집하지 않았습니다.

**D번이 정답인 이유:**
- 세션 시작 시 Claude Code는 현재 실행 위치(`services/billing/`)의 `CLAUDE.md`와 상위 경로인 루트의 `CLAUDE.md`를 함께 초기 컨텍스트로 로드합니다.
- 하위 경로인 `reports/CLAUDE.md`는 세션 시작 시점에는 로드되지 않으며, 향후 Claude가 해당 하위 디렉터리의 파일(예: `reports/` 내 파일)을 조작하거나 읽을 때 비로소 지연 로딩(Lazy Loading)됩니다.

**오답 분석:**
- Option A (오답): 현재 작업 디렉터리에 위치한 `services/billing/CLAUDE.md` 역시 실행 즉시 상위 루트 파일과 함께 로드되므로 루트만 로드된다는 설명은 틀렸습니다.
- Option B (오답): Claude Code는 상위 경로의 `CLAUDE.md` 설정들을 계층적으로 계속 병합(Combine)하므로 상위 파일과 결합되지 않는다는 설명은 오답입니다.
- Option C (오답): 작업 디렉터리 하위에 존재하는 모든 `CLAUDE.md`를 무조건 사전 로드(Preload)하지 않습니다. 하위 디렉터리의 파일은 해당 경로에 접근할 때 온디맨드로 로드됩니다.

---

# 94.

**1. 문제 원문**

While in plan mode, an architect notices that Claude keeps reading files and running read-only search commands without prompting for approval on each one, even though no edits have occurred. What most plausibly explains this behavior?

A) Plan mode inherently blocks all edits and does not require approval for read operations, as the mode's read-only guarantee makes approval prompts unnecessary.

B) Plan mode has silently switched over to acceptEdits mode partway through this session, which is why operations proceed without prompts

C) Read-only commands are permanently exempt from every permission mode in Claude Code, no matter which mode is currently active

D) The architect's local settings have disabled every single permission check for the remainder of this particular session entirely

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**While in plan mode,**
플랜 모드에 있는 동안,

**an architect notices**
한 아키텍트가 알아차립니다

**that Claude keeps reading files**
Claude가 계속 파일을 읽고

**and running read-only search commands**
읽기 전용 검색 명령을 실행하고 있다는 것을

**without prompting for approval**
승인을 요청하지 않고

**on each one,**
각 작업에 대해,

**even though no edits have occurred.**
아무런 수정도 발생하지 않았음에도 불구하고.

**What most plausibly explains**
무엇이 가장 타당하게 설명합니까

**this behavior?**
이 동작을?

---

**OPTIONS:**

**Option A:**

**Plan mode inherently blocks all edits**
플랜 모드는 본질적으로 모든 수정을 차단하며

**and does not require approval**
승인을 요구하지 않습니다

**for read operations,**
읽기 작업에 대해서는,

**as the mode's read-only guarantee**
해당 모드의 읽기 전용 보장이

**makes approval prompts unnecessary.**
승인 프롬프트를 불필요하게 만들기 때문입니다.

**Option B:**

**Plan mode has silently switched over**
플랜 모드가 알리지 않고 전환되었습니다

**to acceptEdits mode**
acceptEdits 모드로

**partway through this session,**
이 세션 진행 중간에,

**which is why operations proceed**
이것이 작업이 진행되는 이유입니다

**without prompts**
프롬프트(승인) 없이

**Option C:**

**Read-only commands are permanently exempt**
읽기 전용 명령은 영구적으로 면제됩니다

**from every permission mode**
모든 권한 모드로부터

**in Claude Code,**
Claude Code 내의,

**no matter which mode**
어떤 모드인지와 상관없이

**is currently active**
현재 활성화되어 있는 모드가

**Option D:**

**The architect's local settings have disabled**
아키텍트의 로컬 설정이 비활성화했습니다

**every single permission check**
단 하나도 빠짐없이 모든 권한 검사를

**for the remainder of this particular session entirely**
이 특정 세션의 나머지 기간 동안 완전히

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**A번**: Plan mode inherently blocks all edits and does not require approval for read operations, as the mode's read-only guarantee makes approval prompts unnecessary.

**정답 및 해설:**


**핵심 개념**: **Claude Code Plan Mode의 권한 모델 (Plan Mode Permission Model)**
Claude Code의 Plan Mode(플랜 모드)는 코드베이스를 분석하여 실행 계획(Plan)을 세우는 읽기 전용(Read-Only) 탐색 모드입니다. 이 모드에서는 모든 파일 수정 및 파괴적인 명령어가 시스템 차원에서 엄격히 차단되므로, 안전한 읽기 전용(Read-only) 조회 및 검색 작업에 대해서는 매번 승인을 묻는 프롬프트를 띄우지 않고 자동으로 진행합니다.

**문제 상황 분석:**
- 사용자가 Claude Code를 Plan Mode로 실행 중입니다.
- Claude가 승인 요청(Prompt) 없이 파일 읽기 및 읽기 전용 검색 명령을 계속 수행하고 있습니다.
- 코드 변경/수정(Edit) 작업은 단 한 건도 발생하지 않고 있습니다.

**A번이 정답인 이유:**
- Plan Mode의 설계 원칙상, 이 모드는 코드베이스를 변경하는 모든 수정 행위를 원천 차단합니다.
- 변경이 불가능한 안전한 읽기 전용 상태가 보장되기 때문에 사용자에게 매번 읽기 승인을 요청할 필요가 없으므로 프롬프트 없이 빠르게 조회가 수행됩니다.

**오답 분석:**
- Option B (오답): Plan Mode가 사용자 개입 없이 임의로 `acceptEdits` 모드로 자동 전환(silently switch)되는 동작은 존재하지 않습니다.
- Option C (오답): 일반 모드(Normal/Default mode)에서는 설정이나 위험도에 따라 읽기/실행 명령도 승인을 요구할 수 있으므로, 모든 모드에서 읽기 명령이 영구히 면제(permanently exempt)된다는 설명은 틀렸습니다.
- Option D (오답): 세션 전체의 모든 권한 검사를 완전히 끌어버린 특수한 로컬 설정 때문이 아니라, Plan Mode 자체의 기본 정식 동작 메커니즘입니다.

---

# 95.

**1. 문제 원문**

During a long working session, an engineer triggers a context compaction. Afterward, they notice Claude has stopped following an instruction that was in a CLAUDE.md file located deep inside a subdirectory the engineer had already worked in earlier in the session, while an instruction from the project-root CLAUDE.md is still being followed correctly. What explains this difference in behavior?

A) Compaction only preserves instructions in the first 200 lines of a session, so the subdirectory file's later position caused it to drop

B) Root CLAUDE.md is re-read and re-injected after compaction, but nested CLAUDE.md files reload only when Claude next reads a file there

C) Compaction corrupts nested CLAUDE.md files on disk, so the subdirectory file must be re-created before its instructions work again

D) Nested CLAUDE.md files are deleted from context permanently after compaction and can only be restored by starting an entirely new session

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**During a long working session,**
긴 작업 세션 동안,

**an engineer triggers**
한 엔지니어가 실행합니다

**a context compaction.**
컨텍스트 압축(compaction)을.

**Afterward, they notice**
그 후, 그들은 알아차립니다

**Claude has stopped following**
Claude가 따르기를 중단했다는 것을

**an instruction**
지시 사항을

**that was in a CLAUDE.md file**
CLAUDE.md 파일에 있던

**located deep inside**
깊은 곳에 위치한

**a subdirectory**
하위 디렉터리

**the engineer had already worked in**
엔지니어가 이미 작업했었던

**earlier in the session,**
세션 초반에,

**while an instruction**
반면에 지시 사항은

**from the project-root CLAUDE.md**
프로젝트 루트 CLAUDE.md로부터의

**is still being followed correctly.**
여전히 올바르게 준수되고 있습니다.

**What explains**
무엇이 설명합니까

**this difference in behavior?**
이러한 동작의 차이를?

---

**OPTIONS:**

**Option A:**

**Compaction only preserves instructions**
압축(Compaction)은 지시 사항만을 보존합니다

**in the first 200 lines of a session,**
세션의 처음 200줄에 있는,

**so the subdirectory file's later position**
따라서 하위 디렉터리 파일의 나중 위치가

**caused it to drop**
이것이 누락되게 만들었습니다

**Option B:**

**Root CLAUDE.md is re-read**
루트 CLAUDE.md는 다시 읽히고

**and re-injected after compaction,**
압축 후 다시 주입됩니다,

**but nested CLAUDE.md files reload**
하지만 중첩된(하위) CLAUDE.md 파일들은 재로드됩니다

**only when Claude next reads**
Claude가 다음에 읽을 때만

**a file there**
그곳의 파일을

**Option C:**

**Compaction corrupts**
압축(Compaction)이 손상시킵니다

**nested CLAUDE.md files on disk,**
디스크 상의 중첩된 CLAUDE.md 파일들을,

**so the subdirectory file**
따라서 하위 디렉터리 파일은

**must be re-created**
다시 생성되어야 합니다

**before its instructions work again**
지시 사항이 다시 작동하기 전에

**Option D:**

**Nested CLAUDE.md files are deleted**
중첩된 CLAUDE.md 파일들은 삭제됩니다

**from context permanently**
컨텍스트에서 영구적으로

**after compaction**
압축(Compaction) 이후에

**and can only be restored**
그리고 복구될 수만 있습니다

**by starting an entirely new session**
완전히 새로운 세션을 시작함으로써만

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**B번**: Root CLAUDE.md is re-read and re-injected after compaction, but nested CLAUDE.md files reload only when Claude next reads a file there

**정답 및 해설:**


**핵심 개념**: **Claude Code의 컨텍스트 압축(Compaction) 및 계층적 CLAUDE.md 재로드 메커니즘**
Claude Code에서 컨텍스트 압축(Compaction)이 발생하면 이전까지 요약/누적되었던 대화 및 임시 컨텍스트가 정리됩니다. 이때 프로젝트 루트의 `CLAUDE.md`는 시스템의 핵심 가이드라인이므로 압축 직후 자동으로 재읽기(re-read)되어 컨텍스트에 다시 주입(re-inject)됩니다. 그러나 하위 디렉터리(Subdirectory)에 존재하는 중첩된 `CLAUDE.md` 파일들은 압축 시 컨텍스트에서 제외되며, 향후 Claude가 해당 하위 디렉터리 내의 파일에 다시 접근할 때 온디맨드로 재로드(Reload)됩니다.

**문제 상황 분석:**
- 긴 세션 진행 중 컨텍스트 압축(Compaction)을 실행함.
- 세션 이전에 방문했던 하위 디렉터리의 `CLAUDE.md` 지시 사항을 Claude가 더 이상 따르지 않음.
- 프로젝트 루트의 `CLAUDE.md` 지시 사항은 압축 이후에도 여전히 정상적으로 적용됨.

**B번이 정답인 이유:**
- 컨텍스트 압축 후 프로젝트 루트의 `CLAUDE.md`는 자동으로 재주입되어 계속 적용되지만, 하위 디렉터리의 `CLAUDE.md`는 압축 과정에서 지워진 상태가 됩니다.
- 해당 하위 디렉터리 안의 파일에 Claude가 다시 접근하여 읽는 시점이 되어야만 해당 하위 `CLAUDE.md`가 다시 로드되므로, 그전까지는 지시 사항을 따르지 않는 현상이 발생합니다.

**오답 분석:**
- Option A (오답): Compaction이 세션의 처음 200줄만 보존한다는 규칙은 존재하지 않습니다.
- Option C (오답): Compaction은 메모리 상의 컨텍스트를 정리할 뿐 디스크에 저장된 실제 파일들을 손상(corrupt)시키지 않습니다.
- Option D (오답): 중첩된 `CLAUDE.md`는 영구 삭제되는 것이 아니라, 해당 디렉터리의 파일을 다시 탐색/읽을 때 온디맨드로 다시 복구(재로드)됩니다.

---

# 96.

**1. 문제 원문**

A team places `.claude/rules/general-style.md` in the repo without adding a `paths` field to its YAML frontmatter, alongside a separate `.claude/rules/api.md` that does declare `paths: ["src/api/**/*.ts"]`. How will Claude Code treat `general-style.md` compared to `api.md`?

A) `general-style.md` loads only when Claude opens a file matching a default wildcard of `"**/*"`, functioning identically to `api.md` but with a broader glob pattern applied automatically.

B) `general-style.md` is ignored entirely because every file placed in `.claude/rules/` requires a `paths` field to be recognized before Claude Code will load it, while `api.md` loads correctly at launch as configured.

C) `general-style.md` loads at launch with the same priority as `.claude/CLAUDE.md`, since omitting the `paths` field makes the rule unconditional, while `api.md` only loads when Claude reads a matching file.

D) `general-style.md` and `api.md` both load only when Claude edits a file located inside the `.claude/rules/` directory itself, since rules files are scoped to their own containing directory by default.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A team places**
한 팀이 배치합니다

**`.claude/rules/general-style.md`**
`.claude/rules/general-style.md` 파일을

**in the repo**
리포지토리에

**without adding a `paths` field**
`paths` 필드를 추가하지 않고

**to its YAML frontmatter,**
YAML 프론트매터(frontmatter)에,

**alongside a separate `.claude/rules/api.md`**
별도의 `.claude/rules/api.md` 파일과 함께

**that does declare**
선언하고 있는

**`paths: ["src/api/**/*.ts"]`.**
`paths: ["src/api/**/*.ts"]`를.

**How will Claude Code treat**
Claude Code는 어떻게 처리할 것입니까

**`general-style.md`**
`general-style.md`를

**compared to `api.md`?**
`api.md`와 비교했을 때?

---

**OPTIONS:**

**Option A:**

**`general-style.md` loads**
`general-style.md`는 로드됩니다

**only when Claude opens a file**
Claude가 파일을 열 때만

**matching a default wildcard of `"**/*"`,**
기본 와일드카드 `"**/*"`와 일치하는,

**functioning identically to `api.md`**
`api.md`와 동일하게 작동하면서

**but with a broader glob pattern**
하지만 더 광범위한 글로브(glob) 패턴이

**applied automatically.**
자동으로 적용된 채로.

**Option B:**

**`general-style.md` is ignored entirely**
`general-style.md`는 완전히 무시됩니다

**because every file placed in `.claude/rules/`**
`.claude/rules/`에 배치된 모든 파일이

**requires a `paths` field**
`paths` 필드를 요구하기 때문에

**to be recognized**
인식되기 위해서

**before Claude Code will load it,**
Claude Code가 이를 로드하기 전에,

**while `api.md` loads correctly**
반면에 `api.md`는 올바르게 로드됩니다

**at launch as configured.**
설정된 대로 시작 시에.

**Option C:**

**`general-style.md` loads at launch**
`general-style.md`는 시작 시에 로드됩니다

**with the same priority as `.claude/CLAUDE.md`,**
`.claude/CLAUDE.md`와 동일한 우선순위로,

**since omitting the `paths` field**
`paths` 필드를 생략하는 것이

**makes the rule unconditional,**
규칙을 무조건적인(무조건 적용되는) 것으로 만들기 때문에,

**while `api.md` only loads**
반면에 `api.md`는 ~할 때만 로드됩니다

**when Claude reads a matching file.**
Claude가 일치하는 파일을 읽을 때만.

**Option D:**

**`general-style.md` and `api.md` both load**
`general-style.md`와 `api.md` 둘 다 로드됩니다

**only when Claude edits a file**
Claude가 파일을 편집할 때만

**located inside the `.claude/rules/` directory itself,**
`.claude/rules/` 디렉터리 자체 내부 위치한,

**since rules files are scoped**
규칙 파일들은 범위가 지정되기 때문에

**to their own containing directory by default.**
기본적으로 자신을 포함하는 디렉터리로.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**C번**: `general-style.md` loads at launch with the same priority as `.claude/CLAUDE.md`, since omitting the `paths` field makes the rule unconditional, while `api.md` only loads when Claude reads a matching file.

**정답 및 해설:**


**핵심 개념**: **Claude Code `.claude/rules/` 모듈식 규칙 및 경로 필터링 (Path-Scoped Rules)**
Claude Code에서는 프로젝트 가이드라인을 분할하여 관리하기 위해 `.claude/rules/*.md` 경로에 규칙 파일을 추가할 수 있습니다. 
- YAML 프론트매터에 `paths` 필드가 없으면 해당 규칙은 프로젝트 전체에 적용되는 무조건적 규칙(Unconditional Rule)이 되어 세션 시작 시(Launch) 프로젝트 루트 `CLAUDE.md`와 동일한 기본 컨텍스트로 로드됩니다.
- 반면 `paths: [...]` 필드가 지정되어 있으면 경로 조건부 규칙(Path-Scoped Rule)이 되어, Claude가 해당 패턴에 일치하는 파일(예: `src/api/**/*.ts`)을 읽을 때 비로소 컨텍스트에 동적으로 주입됩니다.

**문제 상황 분석:**
- `general-style.md` 파일은 YAML 프론트매터에 `paths` 필드가 생략(omitted)되어 있습니다.
- `api.md` 파일은 `paths: ["src/api/**/*.ts"]`로 특정 파일 경로가 지정되어 있습니다.
- 두 파일이 로드되는 시점과 방식의 차이를 묻고 있습니다.

**C번이 정답인 이유:**
- `paths` 필드를 명시하지 않은 `general-style.md`는 조건 없는 일반 규칙으로 처리되어 실행 초기(at launch)에 `.claude/CLAUDE.md`와 함께 기본 컨텍스트로 로드됩니다.
- 경로 필터가 명시된 `api.md`는 온디맨드 규칙으로 처리되어 지정된 경로(`src/api/**/*.ts`)에 해당하는 파일에 접근하여 읽을 때만 조건부로 로드되므로 C번의 설명이 완벽히 부합합니다.

**오답 분석:**
- Option A (오답): `paths`가 생략되었다고 해서 글로브 패턴(`"**/*"`) 조건부 로딩으로 전환되어 파일이 열릴 때까지 대기하는 것이 아니라, 시작 시 무조건 로드됩니다.
- Option B (오답): `.claude/rules/` 디렉터리 내의 파일에서 `paths` 필드는 필수(required) 항목이 아닙니다. 생략할 경우 전역/무조건 규칙으로 정상 동작합니다.
- Option D (오답): 규칙 파일이 `.claude/rules/` 디렉터리 내부 파일에 국한되어 적용된다는 스코프 규칙은 존재하지 않습니다.

---

# 97.

**1. 문제 원문**

An architect is designing a configuration strategy for a company rolling out Claude Code to all engineering teams. They need security and compliance instructions that every developer receives on every machine, in every repository, and that individual developers or teams cannot disable through their own settings. Which approach satisfies this requirement?

A) Commit a `CLAUDE.md` file to the root of each repository, as source control ensures every team member receives the same project-level instructions.

B) Populate each developer's personal `~/.claude/CLAUDE.md` via a one-time onboarding script, which developers can then edit as needed.

C) Place a `CLAUDE.local.md` at the root of each repository, distributed by an onboarding script and added to `.gitignore` to prevent accidental commits.

D) Deploy a `managed-settings.json` file to the system directory (e.g., `/etc/claude-code/` on Linux) using the organization's configuration management system, as it enforces policies that override developer and project settings.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**An architect is designing**
한 아키텍트가 설계하고 있습니다

**a configuration strategy**
설정 전략을

**for a company rolling out**
도입을 진행하는 회사를 위한

**Claude Code**
Claude Code의

**to all engineering teams.**
모든 엔지니어링 팀에.

**They need**
그들은 필요로 합니다

**security and compliance instructions**
보안 및 컴플라이언스 지침을

**that every developer receives**
모든 개발자가 전달받는

**on every machine,**
모든 머신에서,

**in every repository,**
모든 리포지토리에서,

**and that individual developers**
그리고 개별 개발자나

**or teams cannot disable**
팀이 비활성화할 수 없는

**through their own settings.**
그들 자체 설정을 통해.

**Which approach satisfies**
어떤 접근 방식이 충족합니까

**this requirement?**
이 요구사항을?

---

**OPTIONS:**

**Option A:**

**Commit a `CLAUDE.md` file**
`CLAUDE.md` 파일을 커밋합니다

**to the root of each repository,**
각 리포지토리의 루트에,

**as source control ensures**
소스 제어가 보장하므로

**every team member receives**
모든 팀원이 받도록

**the same project-level instructions.**
동일한 프로젝트 수준 지침을.

**Option B:**

**Populate each developer's personal**
각 개발자의 개인

**`~/.claude/CLAUDE.md`**
`~/.claude/CLAUDE.md`에 채워 넣습니다

**via a one-time onboarding script,**
일회성 온보딩 스크립트를 통해,

**which developers can then edit**
개발자가 이후 수정할 수 있는

**as needed.**
필요에 따라.

**Option C:**

**Place a `CLAUDE.local.md`**
`CLAUDE.local.md`를 위치시킵니다

**at the root of each repository,**
각 리포지토리의 루트에,

**distributed by an onboarding script**
온보딩 스크립트로 배포되고

**and added to `.gitignore`**
`.gitignore`에 추가된

**to prevent accidental commits.**
우발적인 커밋을 방지하기 위해.

**Option D:**

**Deploy a `managed-settings.json` file**
`managed-settings.json` 파일을 배포합니다

**to the system directory**
시스템 디렉터리에

**(e.g., `/etc/claude-code/` on Linux)**
(예: Linux의 `/etc/claude-code/`)

**using the organization's**
조직의 ~를 사용하여

**configuration management system,**
구성 관리 시스템을,

**as it enforces policies**
이것이 정책을 강제하므로

**that override developer**
개발자 및 ~를 재정의(override)하는

**and project settings.**
프로젝트 설정을.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: Deploy a `managed-settings.json` file to the system directory (e.g., `/etc/claude-code/` on Linux) using the organization's configuration management system, as it enforces policies that override developer and project settings.

**정답 및 해설:**


**핵심 개념**: **Claude Code Enterprise/Managed Configuration (관리형 설정 및 우선순위)**
Claude Code 설정 및 정책 적용 구조에는 엄격한 우선순위(Precedence) 규칙이 존재합니다. 조직 전체 차원의 관리자 정책(Enterprise / Managed Settings)은 시스템 중앙 위치(예: Linux의 `/etc/claude-code/managed-settings.json`)에 배치되며, 사용자 개인 설정(`~/.claude/`)이나 프로젝트별 설정(`.claude/`)보다 항상 상위의 최고 우선순위를 가집니다. 이를 통해 개별 개발자나 팀이 임의로 비활성화하거나 무시할 수 없도록 중앙 통제 정책을 강제합니다.

**문제 상황 분석:**
- 모든 엔지니어링 팀의 모든 머신, 모든 리포지토리에 일관되게 보안/컴플라이언스 지침을 적용해야 합니다.
- 개발자 개인이나 팀이 자체 설정을 통해 이를 비활성화하거나 수정할 수 없도록 강제(Enforce)해야 합니다.

**D번이 정답인 이유:**
- 시스템 디렉터리(예: `/etc/claude-code/managed-settings.json`)에 배포되는 중앙 관리형 설정(`managed-settings.json`)은 최우선순위(Enterprise/Managed 수준)로 적용됩니다.
- 이는 개발자 개인 환경 설정 및 프로젝트 레벨 설정을 재정의(Override)하고 우회를 차단하므로, 회사 전체 차원의 보안/컴플라이언스 강제 요구사항을 정확히 충족시킵니다.

**오답 분석:**
- Option A (오답): 프로젝트 루트의 `CLAUDE.md`는 프로젝트 수준 설정에 불과하며, 개발자가 로컬 설정으로 우회하거나 커밋을 수정/삭제할 수 있어 중앙 강제성이 부족합니다.
- Option B (오답): 개인 홈 디렉터리의 `~/.claude/CLAUDE.md`는 개발자가 자유롭게 수정할 수 있으며 우선순위도 중앙 정책보다 낮습니다.
- Option C (오답): `CLAUDE.local.md`는 개인 로컬 전용 파일로 `.gitignore` 처리되어 추적되지 않으므로, 개발자가 손쉽게 수정하거나 삭제할 수 있어 중앙 보안 정책 강제용으로 적합하지 않습니다.

---

# 98.

**1. 문제 원문**

A senior engineer says a proposed change to the checkout service is small enough that it doesn't need plan mode, but a teammate insists it does because it touches a database schema. Investigation shows the change adds one nullable column to one table and updates one model file to read it, with no other files affected and no schema decisions still open. Who has correctly assessed the situation?

A) Neither, since schema changes should bypass both plan mode and direct execution entirely and go straight to a migration tool

B) The senior engineer, since the change is confined to one table and one file with a single clear implementation, matching direct execution

C) The teammate, since any schema change is inherently architectural by nature and must always go through plan mode no matter how small it is

D) Neither, since plan mode should be the default for every change touching the checkout service given its business importance

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A senior engineer says**
시니어 엔지니어는 말합니다

**a proposed change**
제안된 변경 사항이

**to the checkout service**
체크아웃 서비스에 대한

**is small enough**
충분히 작아서

**that it doesn't need**
필요하지 않다고

**plan mode,**
플랜 모드(plan mode)를,

**but a teammate insists**
하지만 한 동료는 주장합니다

**it does**
플랜 모드가 필요하다고

**because it touches**
다루기 때문에

**a database schema.**
데이터베이스 스키마를.

**Investigation shows**
조사 결과 밝혀졌습니다

**the change adds**
변경 사항이 추가한다는 것이

**one nullable column**
null 허용 컬럼 하나를

**to one table**
하나의 테이블에

**and updates one model file**
그리고 하나의 모델 파일을 업데이트한다는 것이

**to read it,**
이를 읽기 위해,

**with no other files affected**
다른 파일에는 영향을 미치지 않고

**and no schema decisions**
스키마 결정 사항도 남지 않은 채로

**still open.**
여전히 결정되지 않은 채.

**Who has correctly assessed**
누가 올바르게 평가했습니까

**the situation?**
상황을?

---

**OPTIONS:**

**Option A:**

**Neither,**
둘 다 아닙니다,

**since schema changes**
스키마 변경은 ~해야 하므로

**should bypass**
우회해야 하므로

**both plan mode**
플랜 모드와

**and direct execution entirely**
직접 실행 모두를 완전히

**and go straight**
그리고 바로 진행해야 하므로

**to a migration tool**
마이그레이션 툴로

**Option B:**

**The senior engineer,**
시니어 엔지니어입니다,

**since the change is confined**
변경 사항이 국한되어 있고

**to one table and one file**
하나의 테이블과 하나의 파일에

**with a single clear implementation,**
하나의 명확한 구현 방식을 가진,

**matching direct execution**
직접 실행에 부합하므로

**Option C:**

**The teammate,**
팀 동료입니다,

**since any schema change**
어떠한 스키마 변경도 ~이므로

**is inherently architectural**
본질적으로 아키텍처적 성격을 띠므로

**by nature**
본질상

**and must always go through**
그리고 항상 거쳐야 하므로

**plan mode**
플랜 모드를

**no matter how small it is**
아무리 작다 할지라도

**Option D:**

**Neither,**
둘 다 아닙니다,

**since plan mode**
플랜 모드가 ~이어야 하므로

**should be the default**
기본 설정이어야 하므로

**for every change**
모든 변경 사항에 대해

**touching the checkout service**
체크아웃 서비스를 다루는

**given its business importance**
비즈니스 중요성을 고려할 때

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**B번**: The senior engineer, since the change is confined to one table and one file with a single clear implementation, matching direct execution

**정답 및 해설:**


**핵심 개념**: **Claude Code Plan Mode vs Direct Execution 판단 기준**
Claude Code에서 Plan Mode(플랜 모드)는 여러 파일에 걸친 아키텍처 변경, 모호한 구현 요구사항, 혹은 설계에 관한 미결정 사안이 있을 때 탐색과 계획 수립을 위해 사용됩니다. 반면 작업의 범위가 매우 명확하고, 추가 결정 사항이 없으며, 단일 파일 및 명확한 경로만 수정되는 작업은 사전 계획 단계 없이 바로 코드를 수정하는 Direct Execution(직접 실행)이 적합합니다.

**문제 상황 분석:**
- 변경 내용: 1개 테이블에 nullable 컬럼 1개 추가 + 이를 읽는 1개 모델 파일 수정.
- 다른 파일에는 영향 없음 (사이드 이펙트 없음).
- 아직 결정되지 않은 스키마 이슈나 불확실성 없음 (설계 완료 상태).
- 시니어 엔지니어는 직접 실행을, 동료는 'DB 스키마 변경'이라는 이유만으로 플랜 모드를 주장함.

**B번이 정답인 이유:**
- DB 스키마를 다룬다는 이유만으로 무조건 플랜 모드를 써야 하는 것은 아닙니다.
- 이번 변경 작업은 1개의 테이블과 1개의 파일에 국한된 매우 명확하고 고립된 작업이며 미결정 요소가 전혀 없습니다. 따라서 직접 실행(Direct Execution) 조건에 완벽히 부합하므로 시니어 엔지니어의 판단이 옳습니다.

**오답 분석:**
- Option A (오답): Claude Code를 통한 코드 및 마이그레이션 파일 작성 시 플랜 모드나 직접 실행을 아예 우회해야 한다는 규칙은 없습니다.
- Option C (오답): 아무리 작은 스키마 변경이라도 무조건 플랜 모드를 거쳐야 한다는 제약은 존재하지 않으며, 단순/명확한 작업은 직접 실행이 훨씬 효율적입니다.
- Option D (오답): 서비스의 비즈니스 중요도만으로 단순 컬럼 추가 작업에까지 매번 플랜 모드를 강제하는 것은 불필요한 오버헤드를 유발합니다.

---

# 99.

**1. 문제 원문**

A developer wants Claude Code to implement a rate limiter for an internal API and wants to reduce the number of correction cycles needed afterward. Which opening approach best follows a test-driven iteration pattern?

A) Ask Claude to describe the rate limiting algorithm in a design document covering throttle rules, burst limits, and reset windows, then implement it from that document without writing tests.

B) Ask Claude to implement the rate limiter first, then write a matching test suite afterward that validates throttle responses, burst handling, and window resets to document the built behavior.

C) Ask Claude to write a test suite covering the expected throttling behavior, burst limits, and reset timing first, then implement the rate limiter, iterating by sharing test failures.

D) Ask Claude to implement the rate limiter and manually verify it by running sample requests such as bursts, retries, and checking rate limit headers in the terminal, without automated tests.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A developer wants**
 한 개발자가 원합니다

**Claude Code to implement**
 Claude Code가 구현하기를

**a rate limiter**
 속도 제한기(rate limiter)를

**for an internal API**
 내부 API를 위한

**and wants to reduce**
 그리고 줄이기를 원합니다

**the number of correction cycles**
 수정 주기(반복) 횟수를

**needed afterward.**
 나중에 필요한.

**Which opening approach**
 어떤 시작 접근 방식이

**best follows**
 가장 잘 따릅니까

**a test-driven iteration pattern?**
 테스트 주도 반복(test-driven iteration) 패턴을?

---

**OPTIONS:**

**Option A:**

**Ask Claude to describe**
 Claude에게 설명을 요청합니다

**the rate limiting algorithm**
 속도 제한 알고리즘을

**in a design document**
 설계 문서에

**covering throttle rules,**
 스로틀 규칙,

**burst limits, and reset windows,**
 버스트 제한, 리셋 창을 다루는,

**then implement it**
 그런 다음 이를 구현하도록 합니다

**from that document**
 해당 문서로부터

**without writing tests.**
 테스트를 작성하지 않고.

**Option B:**

**Ask Claude to implement**
 Claude에게 구현을 요청합니다

**the rate limiter first,**
 속도 제한기를 먼저,

**then write a matching test suite**
 그런 다음 부합하는 테스트 수트를 작성하도록 합니다

**afterward**
 나중에

**that validates throttle responses,**
 스로틀 응답을 검증하는,

**burst handling, and window resets**
 버스트 처리, 그리고 창 리셋을

**to document the built behavior.**
 구축된 동작을 문서화하기 위해.

**Option C:**

**Ask Claude to write**
 Claude에게 작성을 요청합니다

**a test suite covering**
 ~를 다루는 테스트 수트를

**the expected throttling behavior,**
 예상되는 스로틀 동작,

**burst limits, and reset timing**
 버스트 제한, 그리고 리셋 타이밍을

**first,**
 먼저,

**then implement the rate limiter,**
 그런 다음 속도 제한기를 구현하도록 합니다,

**iterating by sharing**
 공유하면서 반복(iteration) 작업을 진행합니다

**test failures.**
 테스트 실패 결과를.

**Option D:**

**Ask Claude to implement**
 Claude에게 구현을 요청합니다

**the rate limiter**
 속도 제한기를

**and manually verify it**
 그리고 수동으로 검증하도록 합니다

**by running sample requests**
 샘플 요청을 실행함으로써

**such as bursts, retries,**
 버스트, 재시도와 같은,

**and checking rate limit headers**
 그리고 속도 제한 헤더를 확인하도록 합니다

**in the terminal,**
 터미널에서,

**without automated tests.**
 자동화된 테스트 없이.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**C번**: Ask Claude to write a test suite covering the expected throttling behavior, burst limits, and reset timing first, then implement the rate limiter, iterating by sharing test failures.

**정답 및 해설:**


**핵심 개념**: **테스트 주도 반복 패턴 (Test-Driven Iteration / TDD with AI)**
AI 코딩 에이전트(Claude Code)와 작업할 때 테스트 주도 패턴(TDD)을 적용하면 피드백 루프가 극적으로 단축됩니다. 즉, 구현 요구사항을 검증할 테스트 코드를 먼저 작성하고, 이를 실행하여 발생하는 실패 메시지(Test Failures)를 Claude에게 피드백으로 전달하며 코드를 반복 수정(Iterate)하도록 유도하는 방식입니다. 이를 통해 불필요한 수정 주기를 대폭 줄이고 정확한 코드를 빠르게 완성할 수 있습니다.

**문제 상황 분석:**
- 개발자는 내부 API용 속도 제한기(rate limiter) 구현을 원함.
- 구현 이후에 발생하는 디버깅/수정 주기(correction cycles)의 횟수를 최소화하고 싶어 함.
- 질문에서 요구하는 최선의 접근 방식은 '테스트 주도 반복 패턴(test-driven iteration pattern)'임.

**C번이 정답인 이유:**
- 테스트 주도 개발(TDD)의 핵심 원칙인 "테스트 코드 선 작성 -> 실제 코드 구현 -> 테스트 실패 결과 공유 및 피드백 반복(Iteration)" 구조를 정확하게 서술하고 있습니다.
- 예상되는 제약 조건(스로틀, 버스트 제한, 리셋 타이밍)을 테스트로 명확히 정의해 두고 Claude에게 피드백을 제공하므로 수정 주기를 최소화할 수 있습니다.

**오답 분석:**
- Option A (오답): 설계 문서만 작성하고 테스트 코드를 전혀 작성하지 않으므로, 테스트 주도 반복 패턴에 위배되며 실제 동작 검증 시 많은 수정 주기가 필요합니다.
- Option B (오답): 코드를 먼저 작성하고 나중에 테스트를 붙이는 방식(Test-Last)으로, TDD 패턴이 아니며 최초 코드 작성 시 오류 발생 가능성이 높습니다.
- Option D (오답): 자동화된 테스트 없이 터미널에서 수동으로만 검증하는 방식은 반복적이고 오차 가능성이 커 수정 주기를 줄이는 데 전혀 도움이 되지 않습니다.

---

# 100.

**1. 문제 원문**

A CI job runs `claude -p "apply the lint fixes" --permission-mode acceptEdits`, but the run aborts partway through when Claude attempts to invoke a network request to fetch an updated dependency list. Why did this specific action fail to auto-approve under acceptEdits, even though file edits earlier in the same run went through without prompting?

A) acceptEdits only applies to the first tool call in a run, so while it auto-approved earlier file edits, the later network request to fetch dependency lists required interactive approval and caused the abort.

B) acceptEdits auto-approves file writes and common filesystem commands like mkdir, mv, and cp, but other shell commands and network requests require an --allowedTools entry or a permissions.allow rule.

C) Network requests are always blocked outright in print mode, meaning the attempt to fetch an updated dependency list would have been stopped regardless of acceptEdits or any other permission configuration.

D) The job needed the --bare flag alongside acceptEdits, since acceptEdits has no effect at all unless bare mode is also enabled; without --bare, the network request to fetch dependency lists required interactive approval and caused the abort.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A CI job runs**
한 CI 작업이 실행합니다

**`claude -p "apply the lint fixes"`**
`claude -p "apply the lint fixes"`를

**`--permission-mode acceptEdits`,**
`--permission-mode acceptEdits` 옵션과 함께,

**but the run aborts**
하지만 실행이 중단됩니다

**partway through**
중간에

**when Claude attempts to invoke**
Claude가 호출을 시도할 때

**a network request**
네트워크 요청을

**to fetch**
가져오기 위해

**an updated dependency list.**
업데이트된 의존성 목록을.

**Why did this specific action fail**
왜 이 특정 작업이 실패했습니까

**to auto-approve**
자동 승인하는 데

**under acceptEdits,**
acceptEdits 모드 하에서,

**even though file edits**
파일 수정 작업들이 ~했음에도 불구하고

**earlier in the same run**
동일한 실행의 앞부분에 있던

**went through**
통과되었음에도 불구하고

**without prompting?**
프롬프트(승인 요청) 없이?

---

**OPTIONS:**

**Option A:**

**acceptEdits only applies**
acceptEdits는 오직 적용됩니다

**to the first tool call in a run,**
실행 중 첫 번째 툴 호출에만,

**so while it auto-approved**
따라서 그것이 자동 승인한 반면에

**earlier file edits,**
앞선의 파일 수정들을,

**the later network request**
나중의 네트워크 요청은

**to fetch dependency lists**
의존성 목록을 가져오기 위한

**required interactive approval**
대화형 승인을 요구했고

**and caused the abort.**
중단을 발생시켰습니다.

**Option B:**

**acceptEdits auto-approves**
acceptEdits는 자동 승인합니다

**file writes**
파일 쓰기 작업과

**and common filesystem commands**
일반적인 파일 시스템 명령을

**like mkdir, mv, and cp,**
mkdir, mv, cp와 같은,

**but other shell commands**
하지만 다른 쉘 명령 및

**and network requests require**
네트워크 요청은 요구합니다

**an --allowedTools entry**
--allowedTools 항목이나

**or a permissions.allow rule.**
permissions.allow 규칙을.

**Option C:**

**Network requests are always blocked**
네트워크 요청은 항상 차단됩니다

**outright in print mode,**
프린트 모드(-p)에서 완전히,

**meaning the attempt to fetch**
가져오려는 시도가 ~했음을 의미하면서

**an updated dependency list**
업데이트된 의존성 목록을

**would have been stopped**
중단되었을 것임을

**regardless of acceptEdits**
acceptEdits와 상관없이

**or any other permission configuration.**
 또는 다른 권한 설정과 상관없이.

**Option D:**

**The job needed the --bare flag**
해당 작업은 --bare 플래그가 필요했습니다

**alongside acceptEdits,**
acceptEdits와 함께,

**since acceptEdits has no effect at all**
acceptEdits는 전혀 효과가 없기 때문에

**unless bare mode is also enabled;**
베어(bare) 모드도 활성화되지 않는 한;

**without --bare,**
--bare가 없으면,

**the network request**
네트워크 요청은

**to fetch dependency lists**
의존성 목록을 가져오기 위한

**required interactive approval**
대화형 승인을 요구했고

**and caused the abort.**
중단을 발생시켰습니다.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**B번**: acceptEdits auto-approves file writes and common filesystem commands like mkdir, mv, and cp, but other shell commands and network requests require an --allowedTools entry or a permissions.allow rule.

**정답 및 해설:**


**핵심 개념**: **Claude Code 권한 모드 (`acceptEdits` Scope & CLI Permissions)**
`--permission-mode acceptEdits` 옵션은 로컬 파일 생성/수정/삭제 및 기본 파일 시스템 명령어(`mkdir`, `mv`, `cp` 등)를 대화형 프롬프트 없이 자동 승인(Auto-approve)해 줍니다. 하지만 네트워크 요청을 동반하는 커맨드나 일반 임의 쉘 명령어(Shell execution/Network requests)는 파일 수정 범주를 벗어나므로 `acceptEdits`만으로 자동 승인되지 않으며, 별도의 `--allowedTools` 명시 또는 `permissions.allow` 규칙 설정이 필요합니다.

**문제 상황 분석:**
- CI 작업에서 non-interactive 실행을 위해 `claude -p ... --permission-mode acceptEdits`를 사용했습니다.
- 파일 수정(File edits) 작업은 프롬프트 없이 자동 승인되어 정상 처리되었습니다.
- 의존성 목록을 가져오는 네트워크 요청 시도 시 대화형 프롬프트를 띄울 수 없는 CI 환경 특성상 실행이 중단(abort)되었습니다.

**B번이 정답인 이유:**
- `acceptEdits` 모드가 커버하는 자동 승인 범위는 파일 편집 및 기본 파일 시스템 동작으로 제한됩니다.
- 네트워크 요청 및 일반 임의 쉘 명령어는 추가적인 보안 위험 요소로 분류되어 `acceptEdits` 스코프에 포함되지 않으므로, `--allowedTools`나 `permissions.allow`를 통해 명시적으로 권한을 허용하지 않으면 비대화형 CI 환경에서 거부되거나 중단됩니다.

**오답 분석:**
- Option A (오답): `acceptEdits`가 첫 번째 툴 호출에만 적용된다는 규칙은 없으며, 모든 파일 편집 횟수에 상관없이 정상 작동합니다.
- Option C (오답): 프린트 모드(`-p`)라고 해서 모든 네트워크 요청이 무조건 차단되는 것은 아닙니다. 권한 정책에 따라 허용할 수 있습니다.
- Option D (오답): `acceptEdits` 옵션이 동작하기 위해 `--bare` 플래그가 필수적이라는 조건은 전적으로 거짓입니다.

---

# 101.

**1. 문제 원문**

A CI pipeline needs to parse Claude's response programmatically to decide whether a build step should fail. The team wants the reply to always match a specific JSON shape, for example an object with a boolean `passed` field and an array of `issues` strings, regardless of how Claude phrases its reasoning. Which invocation achieves this?

A) `claude -p "review this diff and reply only with passed:true/false and a list of issues" --output-format text`

B) `claude -p "review this diff" --output-format json --json-schema '{"type":"object","properties":{"passed":{"type":"boolean"},"issues":{"type":"array","items":{"type":"string"}}},"required":["passed","issues"]}'`

C) `claude -p "review this diff" --output-format stream-json --verbose --include-partial-messages`

D) `claude -p "review this diff" --append-system-prompt "Always respond with valid JSON matching {passed, issues}"`

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A CI pipeline needs**
CI 파이프라인이 필요로 합니다

**to parse Claude's response**
Claude의 응답을 파싱하는 것을

**programmatically**
프로그램 방식으로 (코드상에서)

**to decide**
결정하기 위해

**whether a build step**
빌드 단계가 ~해야 하는지를

**should fail.**
실패해야 하는지를.

**The team wants**
팀은 원합니다

**the reply to always match**
응답이 항상 일치하기를

**a specific JSON shape,**
특정한 JSON 형태에,

**for example**
예를 들어

**an object with a boolean `passed` field**
부울(boolean) 형식의 `passed` 필드와

**and an array of `issues` strings,**
문자열 배열 형태의 `issues` 필드를 가진 객체와 같은,

**regardless of**
~에 상관없이

**how Claude phrases its reasoning.**
Claude가 추론 과정을 어떻게 표현하든.

**Which invocation**
어떤 호출 명령이

**achieves this?**
이를 달성합니까?

---

**OPTIONS:**

**Option A:**

**`claude -p "review this diff and reply only with passed:true/false and a list of issues"`**
`claude -p "review this diff and reply only with passed:true/false and a list of issues"`

**`--output-format text`**
`--output-format text`

**Option B:**

**`claude -p "review this diff"`**
`claude -p "review this diff"`

**`--output-format json`**
`--output-format json`

**`--json-schema '{"type":"object","properties":{"passed":{"type":"boolean"},"issues":{"type":"array","items":{"type":"string"}}},"required":["passed","issues"]}'`**
`--json-schema '{"type":"object","properties":{"passed":{"type":"boolean"},"issues":{"type":"array","items":{"type":"string"}}},"required":["passed","issues"]}'`

**Option C:**

**`claude -p "review this diff"`**
`claude -p "review this diff"`

**`--output-format stream-json`**
`--output-format stream-json`

**`--verbose --include-partial-messages`**
`--verbose --include-partial-messages`

**Option D:**

**`claude -p "review this diff"`**
`claude -p "review this diff"`

**`--append-system-prompt "Always respond with valid JSON matching {passed, issues}"`**
`--append-system-prompt "Always respond with valid JSON matching {passed, issues}"`

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**B번**: `claude -p "review this diff" --output-format json --json-schema '{"type":"object","properties":{"passed":{"type":"boolean"},"issues":{"type":"array","items":{"type":"string"}}},"required":["passed","issues"]}'`

**정답 및 해설:**


**핵심 개념**: **Claude CLI의 Structured Output (JSON Schema / Output Formatting)**
Claude CLI에서 모델의 답변 형태를 프로그램(CI/CD 등)에서 파싱하기 적합한 구조로 고정하려면 `--output-format json` 및 `--json-schema` 플래그를 결합하여 사용해야 합니다. JSON Schema를 보장(Structured Outputs)함으로써 LLM 답변의 자유로운 서술 방식이나 추론 문장에 상관없이 명확히 정의된 스키마 구조 형태의 JSON만을 반환하도록 강제할 수 있습니다.

**문제 상황 분석:**
- CI 파이프라인에서 빌드 성공/실패 여부를 자동화 로직으로 판단해야 함.
- 모델의 자연어 표현 방식에 영향을 받지 않고, 항상 정해진 스키마(`passed` boolean, `issues` string array)의 JSON 응답을 신뢰성 있게 수신해야 함.
- 완벽히 구조화된 JSON 출력을 보장할 수 있는 CLI 옵션 구성이 필요함.

**B번이 정답인 이유:**
- `--output-format json` 옵션을 지정하여 전체 출력 형식을 JSON으로 설정하고, `--json-schema`를 제공하여 모델이 요구사항에 정확히 부합하는 객체 구조(`passed`: boolean, `issues`: array of strings, `required`: ["passed", "issues"])를 출력하도록 스키마 차원에서 엄격하게 강제(Enforce)합니다.

**오답 분석:**
- Option A (오답): 프롬프트 텍스트에만 형식 지시를 넣고 `--output-format text`로 호출하면 모델이 자연어 수식어나 마크다운 텍스트를 포함할 가능성이 매우 높아 파싱 실패 위험이 큽니다.
- Option C (오답): `stream-json` 및 `stream` 관련 옵션은 모델 답변의 실시간 생성 이벤트 및 메타데이터 이벤트를 스트리밍받는 용도이며, 특정 데이터를 구조화된 단일 JSON 형태로 스키마 고정하는 역할을 하지 않습니다.
- Option D (오답): 시스템 프롬프트에 "valid JSON" 문구를 추가하는 것만으로는 문법적 오류나 스키마 불일치가 발생할 여지가 있으며, 스키마 검증 및 강제(Structured Output) 기능을 대체할 수 없습니다.

---

# 102.

**1. 문제 원문**

A product architect wants Claude Code to build a real-time collaborative document editor, a domain the architect has not built in before. Before any implementation starts, the architect wants to surface cache invalidation strategy, conflict resolution approach, and failure handling considerations that might not be obvious up front. What technique should the architect use?

A) Give Claude a brief description of the feature and ask it to interview the architect using the AskUserQuestion tool, exploring technical implementation, edge cases, and tradeoffs before writing a spec.

B) Write a complete technical specification personally covering every design decision, including cache invalidation, conflict resolution, and failure handling, then hand it to Claude as a fixed set of implementation instructions.

C) Search for an open-source collaborative editor, instruct Claude to replicate its architecture, and assume that following its design patterns will automatically surface appropriate cache invalidation and conflict resolution strategies.

D) Ask Claude to implement a minimal collaborative editor, then use every bug and edge case found through manual testing as the primary source to surface cache invalidation, conflict resolution, and failure handling design considerations.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A product architect wants**
한 프로덕트 아키텍트가 원합니다

**Claude Code to build**
Claude Code가 구축하기를

**a real-time collaborative document editor,**
실시간 협업 문서 편집기를,

**a domain**
도메인인

**the architect has not built in before.**
아키텍트가 이전에 구축해 본 적이 없는.

**Before any implementation starts,**
어떠한 구현이 시작되기 전에,

**the architect wants to surface**
아키텍트는 도출하기를 원합니다

**cache invalidation strategy,**
캐시 무효화 전략,

**conflict resolution approach,**
충돌 해결 방식,

**and failure handling considerations**
그리고 실패 처리 고려 사항들을

**that might not be obvious up front.**
선뜻 명확히 드러나지 않을 수 있는.

**What technique**
어떤 기법을

**should the architect use?**
아키텍트가 사용해야 합니까?

---

**OPTIONS:**

**Option A:**

**Give Claude a brief description**
Claude에게 간략한 설명을 제공하고

**of the feature**
기능에 대한

**and ask it to interview the architect**
아키텍트를 인터뷰하도록 요청합니다

**using the AskUserQuestion tool,**
AskUserQuestion 도구를 사용하여,

**exploring technical implementation,**
기술적 구현,

**edge cases, and tradeoffs**
엣지 케이스, 그리고 트레이드오프를 탐색하면서

**before writing a spec.**
명세서를 작성하기 전에.

**Option B:**

**Write a complete technical specification**
완벽한 기술 명세서를 직접 작성합니다

**personally covering every design decision,**
모든 설계 결정을 직접 작성하면서,

**including cache invalidation,**
캐시 무효화,

**conflict resolution, and failure handling,**
충돌 해결, 그리고 실패 처리를 포함하여,

**then hand it to Claude**
그런 다음 이를 Claude에게 전달합니다

**as a fixed set of implementation instructions.**
고정된 구현 지침 세트로.

**Option C:**

**Search for an open-source collaborative editor,**
오픈 소스 협업 편집기를 검색하고,

**instruct Claude to replicate its architecture,**
Claude에게 그 아키텍처를 복제하도록 지시하며,

**and assume that**
~라고 가정합니다

**following its design patterns**
그 디자인 패턴을 따르는 것이

**will automatically surface**
자동으로 도출해 줄 것이라고

**appropriate cache invalidation**
적절한 캐시 무효화 및

**and conflict resolution strategies.**
충돌 해결 전략을.

**Option D:**

**Ask Claude to implement**
Claude에게 구현하도록 요청합니다

**a minimal collaborative editor,**
최소한의 협업 편집기를,

**then use every bug and edge case**
그런 다음 모든 버그와 엣지 케이스를 사용합니다

**found through manual testing**
수동 테스트를 통해 발견된

**as the primary source**
주요 출처로

**to surface cache invalidation,**
캐시 무효화,

**conflict resolution,**
충돌 해결,

**and failure handling design considerations.**
그리고 실패 처리 설계 고려 사항을 도출하기 위한.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**A번**: Give Claude a brief description of the feature and ask it to interview the architect using the AskUserQuestion tool, exploring technical implementation, edge cases, and tradeoffs before writing a spec.

**정답 및 해설:**


**핵심 개념**: **AI 상호 인터뷰 패턴 및 요구사항 도출 (Interactive Requirement Gathering via AskUserQuestion)**
도메인 지식이 부족하거나 초기에 직관적으로 떠올리기 어려운 엣지 케이스, 캐시 무효화, 충돌 해결 등의 복잡한 트레이드오프를 도출해야 할 때, Claude에게 아키텍트(사용자)를 역으로 인터뷰(Interview)하도록 지시하는 패턴을 활용합니다. Claude는 `AskUserQuestion` 도구를 사용해 구조화된 질문을 던지며 사용자가 놓치기 쉬운 기술적 고려사항과 설계 방침을 명세 작성 전에 수면 위로 끌어올릴 수 있습니다.

**문제 상황 분석:**
- 아키텍트는 '실시간 협업 문서 편집기' 도메인을 이전에 구축해 본 경험이 없음.
- 캐시 무효화, 충돌 해결, 실패 처리 등 사전에 명확히 드러나지 않는 문제들을 명세/구현 전에 미리 도출하고자 함.
- 경험이 부족한 도메인에서 빠르고 명확하게 기술적 설계 고려사항을 발굴하는 최선의 프롬프팅/도구 활용 방안이 필요함.

**A번이 정답인 이유:**
- Claude에게 간략한 요구사항을 주고 `AskUserQuestion` 도구를 이용해 아키텍트에게 역으로 질문을 던지게 함으로써, 아키텍트가 미처 생각지 못한 엣지 케이스, 기술적 선택지, 트레이드오프를 능동적으로 파악할 수 있습니다.
- 사전에 충분한 기술 인터뷰를 거친 후 명세를 작성하므로 완성도 높은 설계를 도출할 수 있습니다.

**오답 분석:**
- Option B (오답): 아키텍트는 해당 도메인 경험이 없으므로, 모든 설계 결정과 기술 명세를 스스로 완벽하게 작성하는 것 자체가 불가능하거나 수많은 누락이 발생합니다.
- Option C (오답): 오픈소스 아키텍처를 단순히 복제하는 것만으로는 현재 시스템이 처한 특수한 환경의 캐시 무효화나 충돌 해결 전략이 자동으로 해결되지 않으며, 사전에 트레이드오프를 검토하는 과정이 생략됩니다.
- Option D (오답): 무작정 최소 기능부터 구현한 뒤 수동 테스트로 버그를 찾아가며 아키텍처를 뒤늦게 수정하는 방식은 엄청난 재작업(Rework) 비용을 발생시키며 선제적인 설계 도출 전략이 아닙니다.

---

# 103.

**1. 문제 원문**

In a monorepo, Team A's rules live under `packages/team-a/.claude/rules/` and Team B's rules live under `packages/team-b/.claude/rules/`. An engineer on Team A working exclusively in `packages/team-a/` wants to avoid Team B's rules entering context while still letting Team A's own path-scoped rules load conditionally as normal. What should the engineer configure?

A) Delete the `paths` frontmatter from every rule in `packages/team-a/.claude/rules/` so those rules load unconditionally at launch, which the engineer hopes also stops `packages/team-b/.claude/rules/` files from ever being discovered.

B) Add a `claudeMdExcludes` entry in settings pointing to `packages/team-b/.claude/rules/**`, which skips Team B's rules files without affecting how Team A's path-scoped rules conditionally load based on their own paths frontmatter.

C) Set `autoMemoryEnabled` to false in the engineer's local project settings, which the engineer expects stops Claude Code from discovering any `.claude/rules/` directory located outside the current package.

D) Ensure that all rule files in `packages/team-b/.claude/rules/` include a `paths` frontmatter key with glob patterns scoped to `packages/team-b/`. Path-scoped rules only load when Claude works on matching files, so Team B's rules will not be triggered while the engineer works exclusively in `packages/team-a/`.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**In a monorepo,**
모노레포에서,

**Team A's rules live under**
팀 A의 규칙들이 위치합니다

**`packages/team-a/.claude/rules/`**
`packages/team-a/.claude/rules/` 아래에

**and Team B's rules live under**
그리고 팀 B의 규칙들은 위치합니다

**`packages/team-b/.claude/rules/`.**
`packages/team-b/.claude/rules/` 아래에.

**An engineer on Team A**
팀 A의 한 엔지니어가

**working exclusively**
전적으로(독점적으로) 작업하는

**in `packages/team-a/`**
`packages/team-a/` 안에서

**wants to avoid**
피하고 싶어 합니다

**Team B's rules entering context**
팀 B의 규칙이 컨텍스트에 들어오는 것을

**while still letting**
동시에 허용하면서도

**Team A's own path-scoped rules**
팀 A 자신의 경로 범위 규칙들이

**load conditionally**
조건부로 로드되도록

**as normal.**
평소처럼.

**What should the engineer configure?**
엔지니어는 무엇을 설정해야 합니까?

---

**OPTIONS:**

**Option A:**

**Delete the `paths` frontmatter**
`paths` 프론트매터를 삭제합니다

**from every rule**
모든 규칙으로부터

**in `packages/team-a/.claude/rules/`**
`packages/team-a/.claude/rules/` 안의

**so those rules load unconditionally**
그 규칙들이 무조건적으로 로드되도록

**at launch,**
시작 시에,

**which the engineer hopes also stops**
엔지니어가 또한 차단되기를 바라는

**`packages/team-b/.claude/rules/` files**
`packages/team-b/.claude/rules/` 파일들이

**from ever being discovered.**
발견조차 되지 않도록.

**Option B:**

**Add a `claudeMdExcludes` entry**
`claudeMdExcludes` 항목을 추가합니다

**in settings**
설정에

**pointing to `packages/team-b/.claude/rules/**`,**
`packages/team-b/.claude/rules/**`를 가리키는,

**which skips Team B's rules files**
팀 B의 규칙 파일들을 건너뛰는

**without affecting**
영향을 주지 않고

**how Team A's path-scoped rules**
팀 A의 경로 범위 규칙들이 어떻게

**conditionally load**
조건부로 로드되는지에

**based on their own paths frontmatter.**
자신의 `paths` 프론트매터를 기반으로.

**Option C:**

**Set `autoMemoryEnabled` to false**
`autoMemoryEnabled`를 false로 설정합니다

**in the engineer's local project settings,**
엔지니어의 로컬 프로젝트 설정에서,

**which the engineer expects**
엔지니어가 기대하는

**stops Claude Code**
Claude Code가 중단하도록

**from discovering any `.claude/rules/` directory**
어떤 `.claude/rules/` 디렉터리도 발견하는 것을

**located outside the current package.**
현재 패키지 외부에 위치한.

**Option D:**

**Ensure that all rule files**
모든 규칙 파일이 ~하도록 보장합니다

**in `packages/team-b/.claude/rules/`**
`packages/team-b/.claude/rules/` 안의

**include a `paths` frontmatter key**
`paths` 프론트매터 키를 포함하도록

**with glob patterns**
글로브 패턴과 함께

**scoped to `packages/team-b/`.**
`packages/team-b/`로 범위가 지정된.

**Path-scoped rules only load**
경로 범위 규칙은 오직 로드됩니다

**when Claude works on matching files,**
Claude가 일치하는 파일에서 작업할 때만,

**so Team B's rules will not be triggered**
따라서 팀 B의 규칙은 트리거되지 않습니다

**while the engineer works exclusively**
엔지니어가 전적으로 작업하는 동안

**in `packages/team-a/`.**
`packages/team-a/`에서.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: Ensure that all rule files in `packages/team-b/.claude/rules/` include a `paths` frontmatter key with glob patterns scoped to `packages/team-b/`. Path-scoped rules only load when Claude works on matching files, so Team B's rules will not be triggered while the engineer works exclusively in `packages/team-a/`.

**정답 및 해설:**


**핵심 개념**: **Claude Code의 경로 기반 규칙 (Path-Scoped Rules) 및 프론트매터 메커니즘**
Claude Code에서 `.claude/rules/*.md` 내에 존재하는 규칙 파일들은 YAML 프론트매터의 `paths` 글로브 패턴에 따라 동작합니다. `paths` 속성이 올바르게 지정된 규칙은 Claude가 해당 경로 패턴에 일치하는 파일(Read/Edit 등)을 조작하거나 탐색할 때만 컨텍스트에 동적으로 로드됩니다.

**문제 상황 분석:**
- 모노레포 환경에서 팀 A의 엔지니어는 `packages/team-a/` 내부 파일만 전적으로 수정 및 작성 중입니다.
- 팀 B의 규칙이 컨텍스트를 불필요하게 차지하는 것을 방지하고자 합니다.
- 팀 A 자체의 조건부 규칙(path-scoped rules)은 의도대로 정상 로드되어야 합니다.

**D번이 정답인 이유:**
- 팀 B의 규칙 파일들에 `paths: ["packages/team-b/**"]` 형태의 경로 범위를 프론트매터로 지정해 두면, Claude Code는 엔지니어가 `packages/team-b/` 하위 파일에 접근할 때만 해당 규칙을 로드합니다.
- 팀 A 엔지니어가 `packages/team-a/`에서만 작업하는 동안에는 팀 B의 파일 경로에 일치(Match)하지 않으므로, 팀 B의 규칙들이 컨텍스트로 진입하지 않게 되며, 팀 A 고유의 경로 범위 규칙은 원래대로 정상 조건부 로드됩니다.

**오답 분석:**
- Option A (오답): 팀 A 규칙에서 `paths` 프론트매터를 삭제하면 해당 규칙들이 시작 시 무조건(전역) 로드되는 규칙으로 변경될 뿐이며, 팀 B 규칙 파일의 탐색 및 로딩을 차단하지 못합니다.
- Option B (오답): `claudeMdExcludes` 항목은 특정 `CLAUDE.md` 파일들의 로딩을 예외 처리/건너뛰기 위해 제공되는 설정 필드이며, `.claude/rules/*.md` 마크다운 규칙 파일들을 제외하는 공식 지원 방식이 아닙니다.
- Option C (오답): `autoMemoryEnabled`는 AI의 자동 기억/학습 기능(`MEMORY.md`)에 관한 설정일 뿐이며, 작업 영역 외부의 `.claude/rules/` 디렉터리 탐색 및 스캔을 제한하는 설정이 아닙니다.