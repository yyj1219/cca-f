## 40번 문제

**1. 문제 원문**

An architect asks Claude Code to write a function that converts free-form date strings like `next Tuesday`, `03/04/25`, and `the 1st of March` into ISO 8601 dates. The architect has specified that ambiguous numeric inputs in `MM/DD/YY` format should be interpreted month-first, so `03/04/25` must mean March 4, 2025. Early attempts still inconsistently interpret ambiguous formats such as `03/04/25`. What is the most effective next step to fix this transformation ambiguity?

A) Provide 2-3 example input strings, including `03/04/25`, each paired with the exact ISO 8601 output the architect expects (e.g., `03/04/25` -> `2025-03-04`), so the ambiguous format resolves to a concrete rule.

B) Ask Claude to throw an exception whenever an ambiguous format like `03/04/25` is encountered, preventing any ambiguous conversion and requiring the user to provide an explicit format specification.

C) Rewrite the prompt using more forceful language demanding that Claude "get the dates right this time" without adding any new information, but emphasize that ambiguous formats must be resolved consistently.

D) Add a comment in the code explaining that date parsing is a hard problem and ask Claude to use its best judgment, for example by defaulting to month-first when formats like `03/04/25` are ambiguous.

---

**2. 구간별 직독직해 번역**

**QUESTION:**
**An architect asks Claude Code**
한 아키텍트가 Claude Code에 요청합니다

**to write a function**
함수를 작성하도록

**that converts free-form date strings**
자유 형식의 날짜 문자열을 변환하는

**like `next Tuesday`, `03/04/25`, and `the 1st of March`**
`next Tuesday`, `03/04/25`, `the 1st of March`와 같은

**into ISO 8601 dates.**
ISO 8601 날짜 형식으로.

**The architect has specified**
아키텍트는 명시했습니다

**that ambiguous numeric inputs**
모호한 숫자 입력값이

**in `MM/DD/YY` format**
`MM/DD/YY` 형식인

**should be interpreted month-first,**
월(month)을 먼저 해석해야 함을,

**so `03/04/25` must mean March 4, 2025.**
따라서 `03/04/25`는 2025년 3월 4일을 의미해야 합니다.

**Early attempts still inconsistently interpret**
초기 시도에서는 여전히 일관성 없이 해석합니다

**ambiguous formats such as `03/04/25`.**
`03/04/25`와 같은 모호한 형식을.

**What is the most effective next step**
가장 효과적인 다음 조치는 무엇인가요

**to fix this transformation ambiguity?**
이 변환의 모호성을 해결하기 위한?

---

**OPTIONS:**

**Option A:**
**Provide 2-3 example input strings,**
2~3개의 예시 입력 문자열을 제공합니다,

**including `03/04/25`,**
`03/04/25`를 포함하여,

**each paired with the exact ISO 8601 output**
각각 아키텍트가 기대하는 정확한 ISO 8601 출력과 쌍을 이루는

**the architect expects (e.g., `03/04/25` -> `2025-03-04`),**
(예: `03/04/25` -> `2025-03-04`),

**so the ambiguous format resolves**
따라서 모호한 형식이 해결되도록

**to a concrete rule.**
구체적인 규칙으로.

**Option B:**
**Ask Claude to throw an exception**
Claude에게 예외를 발생시키도록 요청합니다

**whenever an ambiguous format like `03/04/25` is encountered,**
`03/04/25`와 같은 모호한 형식을 마주칠 때마다,

**preventing any ambiguous conversion**
어떤 모호한 변환도 방지하고

**and requiring the user to provide**
사용자에게 제공하도록 요구합니다

**an explicit format specification.**
명시적인 형식 사양을.

**Option C:**
**Rewrite the prompt using more forceful language**
더 강력한 어조를 사용하여 프롬프트를 다시 작성합니다

**demanding that Claude "get the dates right this time"**
Claude에게 "이번에는 날짜를 올바르게 처리해라"라고 요구하면서

**without adding any new information,**
어떠한 새로운 정보도 추가하지 않고,

**but emphasize that ambiguous formats**
하지만 모호한 형식이

**must be resolved consistently.**
일관되게 해결되어야 함을 강조합니다.

**Option D:**
**Add a comment in the code**
코드에 주석을 추가합니다

**explaining that date parsing is a hard problem**
날짜 파싱은 어려운 문제라고 설명하는

**and ask Claude to use its best judgment,**
그리고 Claude에게 최선의 판단을 내리도록 요청합니다,

**for example by defaulting to month-first**
예를 들어 월(month) 우선을 기본값으로 설정하도록

**when formats like `03/04/25` are ambiguous.**
`03/04/25`와 같은 형식이 모호할 때.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**A번**: Provide 2-3 example input strings, including `03/04/25`, each paired with the exact ISO 8601 output the architect expects (e.g., `03/04/25` -> `2025-03-04`), so the ambiguous format resolves to a concrete rule.

**정답 및 해설:**

**핵심 개념**: 퓨샷 프롬프팅 (Few-Shot Prompting / Few-Shot Examples)
LLM 및 AI 코딩 에이전트(Claude Code)에게 모호하거나 복잡한 변환 규칙을 지시할 때, 구체적인 입출력 예시(Input-Output pairs)를 함께 제공하는 것이 단순 자연어 설명보다 명확하고 일관된 결과를 유도하는 가장 효과적인 프롬프트 엔지니어링 기법입니다.

**문제 상황 분석:**
- 아키텍트가 `MM/DD/YY` 형태의 모호한 날짜 입력은 '월 우선(month-first)'으로 해석하라는 규칙을 제시했으나, Claude가 이를 일관성 있게 처리하지 못하고 있습니다.
- 단순 텍스트 지시만으로는 모호성을 완전히 해결하지 못하는 모호성(Ambiguity) 문제가 발생하고 있습니다.
- 따라서 LLM이 규칙을 패턴으로 정확히 인식할 수 있도록 명확하고 구체적인 예시를 제시하는 개선책이 필요합니다.

**A번이 정답인 이유:**
명확한 퓨샷 예시(`03/04/25` -> `2025-03-04`)를 명시적 입출력 쌍으로 제공하면, 모델은 단순 지시문을 넘어 기대하는 변환 로직과 출력 형태를 정확히 파악하게 됩니다. 이는 모호한 날짜 파싱 문제를 해결할 때 가장 직접적이고 효과적인 방법입니다.

**오답 분석:**

- Option A (정답): 입출력 예시를 명확히 제시하여 모호성을 해결합니다.
- Option B (오답): 예외(Exception)를 발생시키고 사용자 입력 형식을 강제하는 것은 자유 형식 문자열을 자동 변환하려는 원본 기능 요구사항에 위배됩니다.
- Option C (오답): 새로운 정보나 예시 없이 단지 "이번엔 제대로 해라"와 같이 강한 어조(forceful language)만 추가하는 방식은 프롬프트 엔지니어링에서 효과가 없는 잘못된 접근법입니다.
- Option D (오답): 코드 주석에 "최선의 판단을 내려라(best judgment)"라는 애매한 지시를 남기는 것은 일관성 문제를 해결하지 못하며 오히려 모델의 자율성에 의존하여 불확실성을 높입니다.

---

## 41번 문제

**1. 문제 원문**

A payments engineer asks Claude Code to implement a discount calculation function and wants to minimize back-and-forth correction cycles from the outset. Order totals must round to the nearest cent using banker's rounding, and stacking discounts must apply in a specific order that is easy to get wrong. What should the engineer do before Claude writes any implementation code?

A) Describe the rounding and stacking rules in a single long paragraph of prose and trust that a sufficiently detailed paragraph will be interpreted correctly the first time

B) Write a test suite first that encodes the banker's rounding behavior and the discount stacking order with concrete cases, then have Claude implement against it and iterate on any failures

C) Ask Claude to implement the function using whatever rounding and stacking order seems most common in e-commerce systems, then correct it if the checkout totals look wrong later

D) Have Claude implement the function without tests, then manually spot-check three or four representative orders by hand in a spreadsheet

---

**2. 구간별 직독직해 번역**

**QUESTION:**
**A payments engineer asks Claude Code**
결제 엔지니어가 Claude Code에 요청합니다

**to implement a discount calculation function**
할인 계산 함수를 구현하도록

**and wants to minimize**
그리고 최소화하기를 원합니다

**back-and-forth correction cycles**
주고받는 수정 주기(반복 수정)를

**from the outset.**
처음부터.

**Order totals must round**
주문 총액은 반올림되어야 합니다

**to the nearest cent**
가장 가까운 센트 단위로

**using banker's rounding,**
오사오입(banker's rounding)을 사용하여,

**and stacking discounts must apply**
그리고 중복 할인 적용은 적용되어야 합니다

**in a specific order**
특정한 순서로

**that is easy to get wrong.**
잘못 처리하기 쉬운.

**What should the engineer do**
엔지니어는 무엇을 해야 하나요

**before Claude writes**
Claude가 작성하기 전에

**any implementation code?**
어떠한 구현 코드라도?

---

**OPTIONS:**

**Option A:**
**Describe the rounding and stacking rules**
반올림 및 중복 적용 규칙을 설명합니다

**in a single long paragraph of prose**
줄글로 된 하나의 긴 단락으로

**and trust that a sufficiently detailed paragraph**
그리고 충분히 상세한 단락이

**will be interpreted correctly**
올바르게 해석될 것이라고 믿습니다

**the first time**
처음에 바로

**Option B:**
**Write a test suite first**
테스트 수트를 먼저 작성합니다

**that encodes the banker's rounding behavior**
오사오입(banker's rounding) 동작과

**and the discount stacking order**
할인 중복 적용 순서를 코드로 구현한

**with concrete cases,**
구체적인 케이스들과 함께,

**then have Claude implement against it**
그런 다음 Claude가 이를 통과하도록 구현하게 하고

**and iterate on any failures**
실패하는 부분에 대해 반복 개선합니다

**Option C:**
**Ask Claude to implement the function**
Claude에게 함수를 구현하도록 요청합니다

**using whatever rounding and stacking order**
어떤 반올림 및 중복 순서든 사용하여

**seems most common in e-commerce systems,**
이커머스 시스템에서 가장 일반적으로 보이는,

**then correct it**
그런 다음 이를 수정합니다

**if the checkout totals look wrong later**
나중에 결제 총액이 잘못되어 보인다면

**Option D:**
**Have Claude implement the function**
Claude가 함수를 구현하도록 합니다

**without tests,**
테스트 없이,

**then manually spot-check**
그런 다음 수동으로 샘플 점검합니다

**three or four representative orders**
3~4개의 대표적인 주문을

**by hand in a spreadsheet**
스프레드시트에서 손으로 직접

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**B번**: Write a test suite first that encodes the banker's rounding behavior and the discount stacking order with concrete cases, then have Claude implement against it and iterate on any failures

**정답 및 해설:**

**핵심 개념**: 테스트 주도 개발 (TDD) 및 명확한 검증 기준 제공 (Test-Driven AI Development)
AI 코딩 에이전트(Claude Code)에게 복잡하거나 잘못 처리하기 쉬운 도메인 로직(예: 오사오입, 중복 할인 순서)을 지시할 때는 줄글 형태의 명세서보다 실행 가능한 테스트 코드(Test Suite)를 먼저 제공하는 것이 가장 효과적입니다. 이를 통해 AI가 스스로 코드를 실행하고 테스트 통과 여부를 검증하며 오류를 반복 수정할 수 있습니다.

**문제 상황 분석:**
- 엔지니어는 오사오입(Banker's rounding) 및 복잡한 할인 순서 로직 구현 시 불필요한 재작업(back-and-forth)을 최소화하고자 합니다.
- 사람의 자연어 설명만으로는 예외 케이스나 반올림 경계 조건에서 오해가 생기기 쉽습니다.
- Claude가 작성한 구현을 자동으로 평가하고 스스로 피드백 루프를 돌 수 있는 객관적인 검증 수단이 사전에 필요합니다.

**B번이 정답인 이유:**
구체적인 케이스가 포함된 테스트 수트를 미리 작성해 주면, Claude Code는 구현 과정에서 생성한 코드가 비즈니스 로직을 정확히 만족하는지 즉시 실행해 보고 실패한 테스트를 스스로 수정(iterate)할 수 있습니다. 이는 피드백 주기를 단축시키고 수정 횟수를 최소화하는 가장 확실한 방법입니다.

**오답 분석:**

- Option A (오답): 자연어 줄글로 아무리 길고 상세하게 설명하더라도, 경계 조건이나 세부 로직 해석에 오류가 발생할 수 있어 주고받는 수정 주기를 줄이지 못합니다.
- Option B (정답): 실행 가능한 테스트 코드 기반으로 자율적인 피드백 루프를 형성합니다.
- Option C (오답): 일반적인 방식을 임의로 추측하게 한 뒤 나중에 수정하는 것은 불필요한 수정 주기를 극대화하는 방식입니다.
- Option D (오답): 테스트 없이 구현한 뒤 스프레드시트로 수동 점검하는 방식은 비효율적이며 AI가 스스로 디버깅하고 개선할 수 있는 피드백 루프를 제공하지 못합니다.

---

## 42번 문제

**1. 문제 원문**

A maintainer writes a project `CLAUDE.md` that includes the line `See @README for project overview and @package.json for available npm commands.`. They also want to mention the file `@docs/legacy-notes.md` in a sentence purely as a pointer for humans, without Claude actually importing and loading its contents into context. How should they write that second reference?

A) Place it inside an HTML comment `<!-- @docs/legacy-notes.md -->`, since HTML comments are rendered as plain visible text but excluded from imports

B) Prefix it with a double `@@docs/legacy-notes.md`, since a doubled `@` symbol tells the parser to render the path without importing it

C) Wrap it in backticks as `` `@docs/legacy-notes.md` ``, since import parsing skips content inside code spans and treats it as literal text

D) Put a space between the `@` and the path like `@ docs/legacy-notes.md`, since only paths immediately adjacent to `@` with no space are treated as imports

---

**2. 구간별 직독직해 번역**

**QUESTION:**
**A maintainer writes**
한 메인테이너가 작성합니다

**a project `CLAUDE.md`**
프로젝트 `CLAUDE.md` 파일을

**that includes the line**
다음 문장을 포함하는

**`See @README for project overview`**
`프로젝트 개요는 @README를 참조하고`

**`and @package.json for available npm commands.`**
`사용 가능한 npm 명령어는 @package.json을 참조하세요.`

**They also want to mention**
그들은 또한 언급하기를 원합니다

**the file `@docs/legacy-notes.md`**
`@docs/legacy-notes.md` 파일을

**in a sentence**
한 문장 안에서

**purely as a pointer for humans,**
순전히 사람을 위한 안내 용도로만,

**without Claude actually importing**
Claude가 실제로 임포트하지 않고

**and loading its contents into context.**
그 내용을 컨텍스트로 로드하지 않도록.

**How should they write**
그들은 어떻게 작성해야 하나요

**that second reference?**
그 두 번째 참조를?

---

**OPTIONS:**

**Option A:**
**Place it inside an HTML comment**
HTML 주석 안에 배치합니다

**`<!-- @docs/legacy-notes.md -->`,**
`<!-- @docs/legacy-notes.md -->`와 같이,

**since HTML comments are rendered**
HTML 주석은 렌더링되고

**as plain visible text**
일반적인 보이고 있는 텍스트로

**but excluded from imports**
임포트 대상에서는 제외되기 때문에

**Option B:**
**Prefix it with a double**
이중 기호를 앞에 붙입니다

**`@@docs/legacy-notes.md`,**
`@@docs/legacy-notes.md`와 같이,

**since a doubled `@` symbol**
이중 `@` 기호는

**tells the parser**
파서에게 알려주기 때문에

**to render the path**
경로를 표시하도록

**without importing it**
임포트하지 않고

**Option C:**
**Wrap it in backticks**
백틱(backticks)으로 감쌉니다

**as `` `@docs/legacy-notes.md` ``,**
`` `@docs/legacy-notes.md` ``와 같이,

**since import parsing skips**
임포트 파싱은 건너뛰기 때문에

**content inside code spans**
코드 스팬(인라인 코드) 내부의 내용을

**and treats it as literal text**
그리고 이를 리터럴 텍스트로 처리하기 때문에

**Option D:**
**Put a space**
공백을 둡니다

**between the `@` and the path**
`@`와 경로 사이에

**like `@ docs/legacy-notes.md`,**
`@ docs/legacy-notes.md`와 같이,

**since only paths**
오직 경로들만

**immediately adjacent to `@`**
`@`에 즉시 인접한

**with no space**
공백이 없이

**are treated as imports**
임포트로 처리되기 때문에

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**C번**: Wrap it in backticks as `` `@docs/legacy-notes.md` ``, since import parsing skips content inside code spans and treats it as literal text

**정답 및 해설:**

**핵심 개념**: `CLAUDE.md` 파싱 규칙 및 인라인 코드(Backtick) 처리
Claude Code 시스템은 `CLAUDE.md` 또는 지시문 파일 내의 `@path/to/file` 형태의 파일 참조 문법을 감지하여 자동으로 해당 파일의 내용을 컨텍스트로 불러옵니다(Auto-import). 그러나 인라인 코드 스팬(백틱 `` ` ``)으로 둘러싸인 텍스트는 파일 임포트 대상 파싱에서 제외되어 파일 내용을 로드하지 않고 일반 리터럴 텍스트로 다뤄집니다.

**문제 상황 분석:**
- 메인테이너는 `CLAUDE.md` 파일 내에 인간 개발자를 위한 참고용으로 파일 경로(`@docs/legacy-notes.md`)를 기재하고자 합니다.
- 파일 참조 기호인 `@`를 그대로 사용하면 Claude Code가 파일 내용을 자동으로 컨텍스트에 읽어들여 불필요한 토큰 소비가 발생합니다.
- 따라서 자동으로 파일 내용이 컨텍스트로 로드되는 것을 방지하는 정확한 마크다운/파싱 이스케이프 기법이 필요합니다.

**C번이 정답인 이유:**
마크다운의 백틱(`` ` ``) 기호로 `@filepath`를 감싸면 파서는 해당 부분을 실행/임포트 명령이 아닌 코드 리터럴(Literal) 문자로 인식합니다. 따라서 Claude Code의 자동 임포터(Import parser)가 이 구문을 건너뛰어 실제로 파일 내용을 로드하지 않고 사람에게 표시하는 용도로만 안전하게 활용할 수 있습니다.

**오답 분석:**

- Option A (오답): HTML 주석(`<!-- -->`) 내에 포함된 텍스트는 사람에게 보이는 텍스트로 렌더링되지 않으며(숨겨짐), 파서에 따라 내부 `@` 참조 처리 규칙이 다를 수 있어 사람을 위한 눈에 보이는 안내 용도로 부적합합니다.
- Option B (오답): 이중 `@` 기호(`@@`)는 파서가 이스케이프하거나 임포트를 무시하는 표준 문법 규칙이 아닙니다.
- Option C (정답): 백틱으로 감싸 인라인 코드로 처리함으로써 자동 임포트를 방지합니다.
- Option D (오답): `@`와 경로 사이에 공백을 두면 마크다운 렌더링 시 보기 어색해지며, 코드 파싱 이스케이프의 표준적인 해결책이 아닙니다.

---

## 43번 문제

**1. 문제 원문**

In a monorepo, an engineer working exclusively on `packages/web` finds that Claude Code's context is cluttered at startup with CLAUDE.md content from `packages/admin-dashboard` and several `packages/legacy-*` packages. This happens because the monorepo's root CLAUDE.md file uses import statements to load all subpackage CLAUDE.md files. The engineer wants these excluded from their own sessions without affecting other teammates who might work in those packages. What should they do?

A) Delete the CLAUDE.md files from packages/admin-dashboard and packages/legacy-* directly, since unused files should be removed from the repository

B) Add `claudeMdExcludes` patterns for those packages to the committed `.claude/settings.json` at the repository root, since exclusions can only be configured at the project scope

C) Ask the admin-dashboard and legacy package owners to move their CLAUDE.md files into `.claude/rules/`, since only root-level CLAUDE.md files are loaded across package boundaries

D) Add `claudeMdExcludes` patterns for those packages to `.claude/settings.local.json`, since local settings apply only to that engineer's machine

---

**2. 구간별 직독직해 번역**

**QUESTION:**
**In a monorepo,**
모노레포 환경에서,

**an engineer working exclusively**
전담하여 작업하는 엔지니어가

**on `packages/web`**
`packages/web` 패키지만을

**finds that Claude Code's context**
Claude Code의 컨텍스트가 ~함을 발견합니다

**is cluttered at startup**
시작 시 복잡해져 있는 것을

**with CLAUDE.md content**
CLAUDE.md 파일 내용으로

**from `packages/admin-dashboard`**
`packages/admin-dashboard` 및

**and several `packages/legacy-*` packages.**
여러 `packages/legacy-*` 패키지들로부터 온.

**This happens because**
이러한 현상이 발생하는 이유는

**the monorepo's root CLAUDE.md file**
모노레포 루트의 CLAUDE.md 파일이

**uses import statements**
임포트(import) 구문을 사용하기 때문입니다

**to load all subpackage CLAUDE.md files.**
모든 서브패키지의 CLAUDE.md 파일을 불러오도록.

**The engineer wants these excluded**
엔지니어는 이 파일들이 제외되기를 원합니다

**from their own sessions**
자신의 작업 세션에서만

**without affecting other teammates**
다른 팀원들에게 영향을 주지 않고

**who might work in those packages.**
해당 패키지에서 작업할 수도 있는.

**What should they do?**
엔지니어는 무엇을 해야 하나요?

---

**OPTIONS:**

**Option A:**
**Delete the CLAUDE.md files**
CLAUDE.md 파일들을 삭제합니다

**from packages/admin-dashboard**
packages/admin-dashboard 및

**and packages/legacy-* directly,**
packages/legacy-* 에서 직접,

**since unused files**
사용하지 않는 파일들은

**should be removed from the repository**
저장소에서 제거되어야 하기 때문에

**Option B:**
**Add `claudeMdExcludes` patterns**
`claudeMdExcludes` 패턴을 추가합니다

**for those packages**
해당 패키지들에 대한

**to the committed `.claude/settings.json`**
커밋 대상인 `.claude/settings.json`에

**at the repository root,**
저장소 루트에 위치한,

**since exclusions can only be configured**
제외 설정은 오직 구성될 수만 있기 때문에

**at the project scope**
프로젝트 스코프에서만

**Option C:**
**Ask the admin-dashboard and legacy package owners**
admin-dashboard 및 legacy 패키지 담당자들에게 요청합니다

**to move their CLAUDE.md files**
그들의 CLAUDE.md 파일들을 이동시키도록

**into `.claude/rules/`,**
`.claude/rules/` 디렉터리로,

**since only root-level CLAUDE.md files**
오직 루트 수준의 CLAUDE.md 파일들만

**are loaded across package boundaries**
패키지 경계를 넘어 로드되기 때문에

**Option D:**
**Add `claudeMdExcludes` patterns**
`claudeMdExcludes` 패턴을 추가합니다

**for those packages**
해당 패키지들에 대한

**to `.claude/settings.local.json`,**
`.claude/settings.local.json` 파일에,

**since local settings apply**
로컬 설정은 적용되기 때문에

**only to that engineer's machine**
해당 엔지니어의 개인 기기에만

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: Add `claudeMdExcludes` patterns for those packages to `.claude/settings.local.json`, since local settings apply only to that engineer's machine

**정답 및 해설:**

**핵심 개념**: 로컬 환경 설정 및 `claudeMdExcludes` (Local Settings & Scope Management)
Claude Code 환경 설정은 공용 프로젝트 설정(`.claude/settings.json`)과 개발자 개별 환경 설정(`.claude/settings.local.json`)으로 나뉩니다. 특정 패키지의 `CLAUDE.md` 파일이 자동 로드되는 것을 제외하려면 `claudeMdExcludes` 설정을 사용하며, 팀원 전체에 영향을 주지 않고 본인의 기기(세션)에만 적용하려면 Git 커밋 대상에서 제외되는 `.claude/settings.local.json`에 해당 설정을 추가해야 합니다.

**문제 상황 분석:**
- 루트 `CLAUDE.md`에서 모든 하위 패키지의 `CLAUDE.md`를 불러와 컨텍스트가 불필요한 정보로 오염되고 있습니다.
- 엔지니어는 자신이 담당하지 않는 패키지(`admin-dashboard`, `legacy-*`)의 컨텍스트 로드를 차단하려 합니다.
- 조건: 다른 팀원들의 공유 환경이나 세션에는 영향을 주지 않고 **자신의 개발 세션에만** 제외 규칙을 적용해야 합니다.

**D번이 정답인 이유:**
`.claude/settings.local.json`은 개인 로컬 환경 전용 설정 파일입니다. 여기에 `claudeMdExcludes` 패턴(예: `["packages/admin-dashboard/**", "packages/legacy-*/**"]`)을 명시하면, 해당 엔지니어의 로컬 실행 환경에서만 지정된 경로의 `CLAUDE.md` 로드가 차단되며 타 팀원의 설정에는 아무런 영향을 주지 않습니다.

**오답 분석:**

- Option A (오답): 타 팀원이 해당 패키지를 작업할 때 필요한 `CLAUDE.md` 파일 자체를 공유 저장소에서 삭제하는 것은 적절하지 못합니다.
- Option B (오답): 커밋 대상인 루트 `.claude/settings.json`에 제외 패턴을 추가하면 프로젝트를 공유하는 다른 모든 팀원들의 세션에서도 해당 패키지의 컨텍스트 로드가 차단됩니다.
- Option C (오답): 파일을 다른 디렉터리로 이동시키는 것은 팀 차원의 구조 변경을 수반하며, 로컬 환경만 격리하려는 목적에 부합하지 않습니다.
- Option D (정답): 개인 환경 전용 설정 파일인 `.claude/settings.local.json`에 제외 패턴을 기재합니다.

---

## 44번 문제

**1. 문제 원문**

A codebase currently has separate CLAUDE.md files inside src/api/tests/, src/web/tests/, and packages/shared/tests/, each repeating nearly identical testing conventions. These per-directory CLAUDE.md files only load when Claude happens to read a file inside that specific subdirectory. The team wants one definition of testing conventions that reliably applies to every test file, no matter which directory it is added to in the future. What is the best solution?

A) Keep the three subdirectory CLAUDE.md files as the definitive source for testing rules because subdirectory CLAUDE.md files automatically apply to any test file created within those directories and will cascade to new test subdirectories over time.

B) Rename the three subdirectory CLAUDE.md files to CLAUDE.local.md so they are excluded from version control while still loading automatically whenever Claude navigates into those specific testing directories, providing consistent test conventions locally.

C) Consolidate the three files into a single top-level CLAUDE.md so the shared testing conventions load into every session and are applied proactively whenever a test-related context is detected, regardless of directory structure.

D) Replace the three subdirectory CLAUDE.md files with one .claude/rules/testing.md file using a paths glob that matches the project's test file naming (e.g., "**/*.test.*" or "**/*.spec.*"), so the convention applies by file type across the repo rather than depending on directory structure.

---

**2. 구간별 직독직해 번역**

**QUESTION:**
**A codebase currently has**
코드베이스가 현재 가지고 있습니다

**separate CLAUDE.md files**
별도의 CLAUDE.md 파일들을

**inside `src/api/tests/`,**
`src/api/tests/` 내부,

**`src/web/tests/`,**
`src/web/tests/`, 그리고

**and `packages/shared/tests/`,**
`packages/shared/tests/` 내부에,

**each repeating nearly identical**
각각 거의 동일한 내용을 반복하면서

**testing conventions.**
테스트 컨벤션(규칙)을.

**These per-directory CLAUDE.md files**
이러한 디렉터리별 CLAUDE.md 파일들은

**only load**
오직 로드됩니다

**when Claude happens to read**
Claude가 우연히 읽을 때에만

**a file inside that specific subdirectory.**
해당 특정 서브디렉터리 내부의 파일을.

**The team wants one definition**
팀은 하나의 정의를 원합니다

**of testing conventions**
테스트 컨벤션에 대한

**that reliably applies**
확실하게 적용되는

**to every test file,**
모든 테스트 파일에,

**no matter which directory**
어느 디렉터리에

**it is added to in the future.**
향후 추가되더라도.

**What is the best solution?**
가장 좋은 해결책은 무엇인가요?

---

**OPTIONS:**

**Option A:**
**Keep the three subdirectory CLAUDE.md files**
3개의 서브디렉터리 CLAUDE.md 파일들을 유지합니다

**as the definitive source**
확정적인 소스(기준)로

**for testing rules**
테스트 규칙을 위한

**because subdirectory CLAUDE.md files**
서브디렉터리 CLAUDE.md 파일들은 ~하기 때문에

**automatically apply**
자동으로 적용되고

**to any test file created**
생성된 어떤 테스트 파일에든

**within those directories**
해당 디렉터리들 내부에서

**and will cascade**
연쇄적으로 적용될 것이기 때문에

**to new test subdirectories over time.**
시간이 지남에 따라 새로운 테스트 서브디렉터리로.

**Option B:**
**Rename the three subdirectory CLAUDE.md files**
3개의 서브디렉터리 CLAUDE.md 파일의 이름을 변경합니다

**to `CLAUDE.local.md`**
`CLAUDE.local.md`로

**so they are excluded**
제외되도록

**from version control**
버전 관리(Git)에서

**while still loading automatically**
여전히 자동으로 로드되면서

**whenever Claude navigates into**
Claude가 이동할 때마다

**those specific testing directories,**
해당 특정 테스트 디렉터리로,

**providing consistent test conventions locally.**
로컬에서 일관된 테스트 컨벤션을 제공하며.

**Option C:**
**Consolidate the three files**
3개의 파일을 하나로 통합합니다

**into a single top-level CLAUDE.md**
단일 최상위(top-level) CLAUDE.md 파일로

**so the shared testing conventions load**
공유 테스트 컨벤션이 로드되도록

**into every session**
모든 세션에

**and are applied proactively**
그리고 사전에 적용되도록

**whenever a test-related context is detected,**
테스트 관련 컨텍스트가 감지될 때마다,

**regardless of directory structure.**
디렉터리 구조와 관계없이.

**Option D:**
**Replace the three subdirectory CLAUDE.md files**
3개의 서브디렉터리 CLAUDE.md 파일들을 대체합니다

**with one `.claude/rules/testing.md` file**
하나의 `.claude/rules/testing.md` 파일로

**using a paths glob**
경로 글로브(paths glob)를 사용하여

**that matches the project's test file naming**
프로젝트의 테스트 파일 명명 규칙과 일치하는

**(e.g., `"**/*.test.*"` or `"**/*.spec.*"`),**
(예: `"**/*.test.*"` 또는 `"**/*.spec.*"`),

**so the convention applies by file type**
컨벤션이 파일 유형별로 적용되도록

**across the repo**
저장소 전체에 걸쳐

**rather than depending on directory structure.**
디렉터리 구조에 의존하기보다.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: Replace the three subdirectory CLAUDE.md files with one `.claude/rules/testing.md` file using a paths glob that matches the project's test file naming (e.g., `"**/*.test.*"` or `"**/*.spec.*"`), so the convention applies by file type across the repo rather than depending on directory structure.

**정답 및 해설:**

**핵심 개념**: `.claude/rules/` 및 경로 패턴 매칭 (`paths` glob)
Claude Code에서는 특정 파일 패턴에 따라 모듈화된 규칙을 적용할 수 있는 모듈형 규칙 시스템(`.claude/rules/`)을 지원합니다. 규칙 파일 헤더에 `paths` 글로브 패턴(예: `paths: ["**/*.test.*"]`)을 정의하면 디렉터리 위치와 상관없이 해당 확장자나 패턴에 일치하는 모든 파일이 작업 대상이 될 때 자동으로 지정된 컨벤션 규칙이 적용됩니다.

**문제 상황 분석:**
- 여러 디렉터리에 동일한 내용의 `CLAUDE.md` 파일이 파편화되어 중복 작성되어 있습니다.
- 서브디렉터리의 `CLAUDE.md`는 해당 디렉터리 내의 파일을 읽을 때만 조건부로 로드되므로, 새로운 디렉터리에 테스트 파일이 추가되면 규칙이 누락될 위험이 있습니다.
- 특정 디렉터리 구조에 종속되지 않고, 저장소 전체의 모든 테스트 파일(`*.test.*`, `*.spec.*`)에 일관되게 적용되는 단일 규칙 정의가 필요합니다.

**D번이 정답인 이유:**
`.claude/rules/testing.md` 경로에 단일 규칙 파일을 생성하고 글로브 패턴(`paths: ["**/*.test.*"]`)을 설정하면 디렉터리 구조와 무관하게 모든 테스트 파일에 컨벤션이 정확히 적용됩니다. 중복을 제거함과 동시에 향후 새로운 디렉터리에 추가되는 테스트 파일에도 자동으로 규칙이 반영되는 가장 모범적이고 유지보수성이 높은 솔루션입니다.

**오답 분석:**

- Option A (오답): 중복된 서브디렉터리 파일들을 그대로 유지하는 것은 파편화 문제를 해결하지 못하며, 새로운 디렉터리가 생성될 때마다 매번 `CLAUDE.md`를 복사/생성해야 하는 번거로움이 유지됩니다.
- Option B (오답): `CLAUDE.local.md`는 버전 관리에 포함되지 않는 개인 로컬용 파일입니다. 팀 전체에 일관된 테스트 규칙을 강제/공유하려는 목적에 어긋납니다.
- Option C (오답): 모든 테스트 규칙을 최상위 `CLAUDE.md`에 넣으면 테스트 작업을 하지 않는 세션에도 불필요한 테스트 컨벤션 토큰이 상시 로드되어 컨텍스트 윈도우(Context Window)를 낭비하게 됩니다. 세부 규칙은 `.claude/rules/`로 분리하는 것이 바람직합니다.
- Option D (정답): `.claude/rules/` 및 글로브 패턴을 활용하여 파일 유형별로 유연하게 로드시킵니다.

---

## 45번 문제

**1. 문제 원문**

A team configures a single long-running Claude Code session that both implements a feature and then reviews its own diff before opening the PR. QA notices this workflow catches noticeably fewer bugs than an independent reviewer would. What is the underlying reason a self-review in the same session tends to be weaker?

A) The session runs out of available context window tokens by the time review begins, forcing Claude to skip reading large portions of the diff

B) Claude Code caches tool call results per session, causing the review step to reuse stale file contents from before the edits were made

C) The session retains the reasoning and assumptions used while writing the code, so it tends to re-apply the same blind spots when checking its own output instead of evaluating it fresh

D) The --print flag disables the Read tool after the first turn, so the reviewing pass cannot re-open files it already edited

---

**2. 구간별 직독직해 번역**

**QUESTION:**
**A team configures**
한 팀이 설정합니다

**a single long-running Claude Code session**
단일 장기 실행 Claude Code 세션을

**that both implements a feature**
기능을 구현하고

**and then reviews its own diff**
그런 다음 자체 변경 사항(diff)을 검토하는

**before opening the PR.**
PR을 생성하기 전에.

**QA notices**
QA 팀이 알게 됩니다

**this workflow catches noticeably fewer bugs**
이 워크플로가 눈에 띄게 더 적은 버그를 잡아낸다는 점을

**than an independent reviewer would.**
독립적인 리뷰어가 잡아낼 버그보다.

**What is the underlying reason**
근본적인 이유는 무엇인가요

**a self-review in the same session**
동일한 세션에서의 자체 검토가

**tends to be weaker?**
더 취약해지는 경향이 있는?

---

**OPTIONS:**

**Option A:**
**The session runs out of**
세션에 부족해집니다

**available context window tokens**
사용 가능한 컨텍스트 윈도우 토큰이

**by the time review begins,**
검토가 시작될 즈음에는,

**forcing Claude to skip**
Claude가 건너뛰도록 강제하면서

**reading large portions of the diff**
diff의 큰 부분을 읽는 것을

**Option B:**
**Claude Code caches tool call results**
Claude Code는 도구 호출 결과를 캐싱합니다

**per session,**
세션별로,

**causing the review step**
검토 단계로 하여금 ~하게 만들면서

**to reuse stale file contents**
오래된 파일 내용을 재사용하도록

**from before the edits were made**
수정이 이루어지기 전의

**Option C:**
**The session retains**
세션이 유지합니다

**the reasoning and assumptions**
논리와 가설(전제)들을

**used while writing the code,**
코드를 작성하는 동안 사용된,

**so it tends to re-apply**
따라서 다시 적용하는 경향이 있습니다

**the same blind spots**
동일한 사각지대를

**when checking its own output**
자신의 출력물을 확인할 때

**instead of evaluating it fresh**
새로운 관점에서 평가하는 대신

**Option D:**
**The `--print` flag disables**
`--print` 플래그가 비활성화합니다

**the Read tool after the first turn,**
첫 번째 턴 이후 Read 도구를,

**so the reviewing pass**
따라서 검토 패스가

**cannot re-open files**
파일을 다시 열 수 없습니다

**it already edited**
이미 수정한

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**C번**: The session retains the reasoning and assumptions used while writing the code, so it tends to re-apply the same blind spots when checking its own output instead of evaluating it fresh

**정답 및 해설:**

**핵심 개념**: 편향 유지 및 독립적 검토 세션 (Context Bias & Independent Review Sessions)
LLM 및 AI 에이전트는 동일한 세션(대화 맥락) 안에서 코드 작성과 코드 리뷰를 연속해서 수행할 경우, 이전 대화 맥락에 포함된 **자신의 추론 과정, 편향(Bias), 잘못된 전제 조건(Blind spots)**을 그대로 유지하게 됩니다. 이를 극복하고 객관적이고 엄격한 검토를 수행하려면 별도의 독립된 세션(Fresh Session)이나 독립된 프롬프트 컨텍스트에서 리뷰를 진행해야 합니다.

**문제 상황 분석:**
- 단일 Claude Code 세션에서 코드 구현과 PR 전 셀프 리뷰(Self-review)를 모두 진행하고 있습니다.
- 독립적인 리뷰어에 비해 셀프 리뷰 세션의 버그 감지율이 눈에 띄게 떨어집니다.
- 동일 세션 맥락(Context)을 공유함으로 인해 발생하는 LLM의 인지적 사각지대(Blind Spot) 유지가 원인입니다.

**C번이 정답인 이유:**
이전 턴에서 코드를 작성할 때 사용했던 논리 구조와 전제 조건들이 컨텍스트 윈도우에 남아있기 때문에, 스스로의 코드를 검토할 때 동일한 논리적 오류나 사각지대를 재인지하지 못하고 "자신이 작성한 코드가 올바르다"는 편향된 방향으로 검토를 수행하게 됩니다. 따라서 신선한 관점(Fresh perspective)에서 검토하지 못하므로 검토 능력이 떨어지게 됩니다.

**오답 분석:**

- Option A (오답): 세션 컨텍스트가 길어지면 자동 요약(Summarization)이나 자르기(Truncation)가 발생할 수 있지만, "리뷰 시작 시 diff의 대다수를 건너뛴다"는 것은 셀프 리뷰가 취약해지는 근본적인 이유가 아닙니다.
- Option B (오답): Claude Code는 파일이 수정되면 도구 호출 결과를 재조회하거나 업데이트된 컨텍스트를 사용하므로, 수정 전의 오래된(stale) 파일 내용만 고정으로 재사용한다는 설명은 사실이 아닙니다.
- Option C (정답): 작성 당시의 추론과 사각지대가 세션 맥락에 그대로 남아있어 편향이 발생합니다.
- Option D (오답): `--print` 플래그는 출력을 터미널에 인쇄하고 세션을 종료하는 비상호작용 모드 플래그일 뿐, 첫 턴 이후 Read 도구를 강제로 비활성화하는 동작을 하지 않습니다.

---

## 46번 문제

**1. 문제 원문**

During a multi-phase refactor, Claude needs to grep across hundreds of files to catalog every usage of a deprecated helper function before deciding what to do next. The architect is worried this discovery output will consume most of the main conversation's context window. What should the architect do to prevent this?

A) Delegate the cataloging work to an Explore subagent, which runs the searches in its own context and returns a condensed summary to the conversation

B) Have Claude paste the full grep output directly into the conversation and then manually delete the least relevant lines from the transcript afterward

C) Switch the main session into plan mode, which is specifically designed to automatically summarize all grep output before it reaches the conversation

D) Raise the configured context window limit for the session so the raw search results no longer need to be trimmed at all before use

---

**2. 구간별 직독직해 번역**

**QUESTION:**
**During a multi-phase refactor,**
다단계 리팩터링 동안,

**Claude needs to grep**
Claude는 grep을 수행해야 합니다

**across hundreds of files**
수백 개의 파일에 걸쳐

**to catalog every usage**
모든 사용 사례를 목록화하기 위해

**of a deprecated helper function**
더 이상 사용되지 않는(deprecated) 헬퍼 함수의

**before deciding what to do next.**
다음으로 무엇을 할지 결정하기 전에.

**The architect is worried**
아키텍트는 걱정하고 있습니다

**this discovery output will consume**
이 탐색 결과물이 소비할까 봐

**most of the main conversation's**
주 대화 세션의 대부분을

**context window.**
컨텍스트 윈도우에서.

**What should the architect do**
아키텍트는 무엇을 해야 하나요

**to prevent this?**
이를 방지하기 위해?

---

**OPTIONS:**

**Option A:**
**Delegate the cataloging work**
목록화 작업을 위임합니다

**to an Explore subagent,**
Explore 서브에이전트에게,

**which runs the searches**
탐색(검색)을 실행하는

**in its own context**
자체 컨텍스트 내에서

**and returns a condensed summary**
그리고 요약된 결과를 반환하는

**to the conversation**
메인 대화 세션으로

**Option B:**
**Have Claude paste**
Claude가 붙여넣도록 합니다

**the full grep output directly**
전체 grep 결과를 직접

**into the conversation**
대화 세션 내부로

**and then manually delete**
그런 다음 수동으로 삭제합니다

**the least relevant lines**
관련성이 가장 적은 줄들을

**from the transcript afterward**
나중에 대화 기록에서

**Option C:**
**Switch the main session**
메인 세션을 전환합니다

**into plan mode,**
플랜 모드(plan mode)로,

**which is specifically designed**
특수하게 설계된

**to automatically summarize**
자동으로 요약하도록

**all grep output**
모든 grep 결과를

**before it reaches the conversation**
대화 세션에 도달하기 전에

**Option D:**
**Raise the configured context window limit**
설정된 컨텍스트 윈도우 제한을 높입니다

**for the session**
해당 세션에 대해

**so the raw search results**
원본 검색 결과가

**no longer need to be trimmed**
더 이상 다듬어질 필요가 없도록

**at all before use**
사용 전에 전혀

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**A번**: Delegate the cataloging work to an Explore subagent, which runs the searches in its own context and returns a condensed summary to the conversation

**정답 및 해설:**

**핵심 개념**: 서브에이전트(Subagent) 활용을 통한 컨텍스트 윈도우 효율화 (Context Isolation)
Claude Code 환경에서는 대용량 파일 탐색, 코드베이스 분석 등 방대한 정보 조회가 필요할 때 서브에이전트(예: Explore subagent)를 생성하여 독립된 컨텍스트 내에서 작업을 수행하게 할 수 있습니다. 이를 통해 원본 검색 데이터는 하위 에이전트 내부에서만 처리되고 메인 대화 세션에는 최종적으로 정리된 요약 결과만 전달되므로 메인 세션의 컨텍스트 윈도우 낭비를 막을 수 있습니다.

**문제 상황 분석:**
- 리팩터링 대상인 헬퍼 함수의 사용 위치를 찾기 위해 수백 개 파일에 걸친 `grep` 조회가 필요한 상황입니다.
- Raw 데이터 형태로 수많은 검색 결과가 메인 대화에 직접 들어가면 컨텍스트 윈도우 토큰이 대량으로 소모되어 대화의 질이 떨어집니다.
- 메인 세션의 컨텍스트를 깨끗하게 유지하면서 핵심 분석 요약 정보만 받아볼 수 있는 구조적 해결책이 필요합니다.

**A번이 정답인 이유:**
Explore 서브에이전트에게 탐색 업무를 위임(Delegate)하면, 서브에이전트는 독립된 별도의 컨텍스트 윈도우에서 `grep` 명령 및 분석을 수행합니다. 메인 세션에는 정제되고 축약된 요약 결과(Condensed summary)만 반환되므로 메인 세션의 토큰 소모를 최소화할 수 있습니다.

**오답 분석:**

- Option A (정답): 별도의 하위 서브에이전트 컨텍스트에서 조회를 실행하여 요약본만 넘겨받습니다.
- Option B (오답): 대화 트랜스크립트에서 수동으로 줄을 삭제하는 것은 매우 비효율적일 뿐만 아니라 이미 사용된 컨텍스트 토큰 소모를 근본적으로 방지하지 못합니다.
- Option C (오답): 플랜 모드(Plan mode)는 코드를 직접 변경하기 전에 단계별 계획을 수립하는 기능일 뿐, `grep` 명령의 출력을 자동으로 intercept 하여 요약해 주는 기능을 수행하지 않습니다.
- Option D (오답): 컨텍스트 윈도우 제한을 무작정 올리는 것은 비효율적인 토큰 비용 발생을 야기하며, 검색 결과 수천 줄이 여전히 컨텍스트에 포함되어 노이즈(Noise)를 유발한다는 문제를 해결하지 못합니다.

---

## 47번 문제

**1. 문제 원문**

A team wants a rule requiring input validation and a standard error response format to apply automatically whenever Claude works on any file under `src/api/`, but they don't want this rule to consume context space during frontend or infrastructure work. Which `.claude/rules/` configuration satisfies this?

A) A rules file with no frontmatter, placed inside `src/api/.claude/rules/` so its location alone scopes it to that one directory

B) A rules file with `paths` set to the literal string `"src/api"` with no glob wildcard, treated as matching every file underneath recursively

C) A rules file with YAML frontmatter `paths: ["src/api/**/*"]`, so it only enters context when Claude reads a matching file

D) A rules file named `src-api-only.md`, since Claude Code infers scoping from filename prefixes matching directory names

---

**2. 구간별 직독직해 번역**

**QUESTION:**
**A team wants a rule**
한 팀이 규칙을 원합니다

**requiring input validation**
입력 검증을 요구하는

**and a standard error response format**
그리고 표준 오류 응답 형식을

**to apply automatically**
자동으로 적용되도록

**whenever Claude works on**
Claude가 작업할 때마다

**any file under `src/api/`,**
`src/api/` 하위의 모든 파일에서,

**but they don't want this rule**
하지만 그들은 이 규칙이 ~하기를 원하지 않습니다

**to consume context space**
컨텍스트 공간을 소비하는 것을

**during frontend or infrastructure work.**
프론트엔드나 인프라 작업 동안에.

**Which `.claude/rules/` configuration**
어떤 `.claude/rules/` 설정이

**satisfies this?**
이를 충족하나요?

---

**OPTIONS:**

**Option A:**
**A rules file with no frontmatter,**
프론트매터(frontmatter)가 없는 규칙 파일,

**placed inside `src/api/.claude/rules/`**
`src/api/.claude/rules/` 내부에 위치한

**so its location alone**
위치 자체만으로

**scopes it to that one directory**
해당 하나의 디렉터리로 범위를 제한하도록

**Option B:**
**A rules file with `paths` set**
`paths`가 설정된 규칙 파일

**to the literal string `"src/api"`**
리터럴 문자열 `"src/api"`로

**with no glob wildcard,**
글로브 와일드카드(glob wildcard) 없이,

**treated as matching every file**
모든 파일과 일치하는 것으로 처리되는

**underneath recursively**
하위 디렉터리에 재귀적으로

**Option C:**
**A rules file with YAML frontmatter**
YAML 프론트매터가 포함된 규칙 파일

**`paths: ["src/api/**/*"]`,**
`paths: ["src/api/**/*"]` 와 같은,

**so it only enters context**
따라서 컨텍스트에 포함되도록

**when Claude reads a matching file**
Claude가 일치하는 파일을 읽을 때만

**Option D:**
**A rules file named `src-api-only.md`,**
`src-api-only.md`라는 이름의 규칙 파일,

**since Claude Code infers scoping**
Claude Code가 스코핑(범위)을 추론하기 때문에

**from filename prefixes**
파일명 접두사로부터

**matching directory names**
디렉터리 이름과 일치하는

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**C번**: A rules file with YAML frontmatter `paths: ["src/api/**/*"]`, so it only enters context when Claude reads a matching file

**정답 및 해설:**

**핵심 개념**: `.claude/rules/` YAML Frontmatter 및 `paths` 경로 조건부 로드
Claude Code의 모듈형 규칙 시스템(`.claude/rules/`)에서는 규칙 파일 상단에 YAML 프론트매터(Frontmatter) 형식으로 `paths` 구문을 지정할 수 있습니다. `paths`에 글로브 패턴(Glob pattern)을 명시하면, Claude가 해당 경로 패턴에 일치하는 파일(예: `src/api/**/*`)을 읽거나 작업할 때만 조건부로 규칙을 컨텍스트 윈도우에 로드하여 불필요한 토큰 소비를 방지합니다.

**문제 상황 분석:**
- `src/api/` 하위 파일 작업 시에만 입력 검증 및 에러 응답 규격 규칙이 자동으로 적용되어야 합니다.
- 프론트엔드나 인프라 작업 시에는 이 규칙이 로드되지 않아 컨텍스트 공간(Context window)을 낭비하지 않아야 합니다.
- 조건부 컨텍스트 로드를 구현하기 위한 올바른 `.claude/rules/` 경로 및 프론트매터 설정 방법을 찾아야 합니다.

**C번이 정답인 이유:**
규칙 파일 헤더에 YAML 프론트매터로 `paths: ["src/api/**/*"]`를 정의하면, Claude Code는 `src/api/` 경로 내의 임의의 깊이에 있는 파일이 작업에 포함될 때만 해당 규칙을 컨텍스트로 읽어들입니다. 따라서 프론트엔드/인프라 작업 중에는 컨텍스트를 전혀 차지하지 않아 조건에 완벽히 부합합니다.

**오답 분석:**

- Option A (오답): `.claude/rules/` 디렉터리는 통상 프로젝트 루트(`.claude/rules/`)에 배치하여 관리하며, 서브디렉터리에 설치하고 프론트매터를 생략한다고 해서 경로 스코핑이 자동으로 적용되지 않습니다.
- Option B (오답): 글로브 와일드카드(`**/*`) 없이 리터럴 문자열 `"src/api"`만 지정하면 하위 파일 및 디렉터리에 재귀적으로 일치하지 않으므로 올바른 글로브 패턴이 아닙니다.
- Option C (정답): YAML frontmatter의 `paths` 글로브 패턴을 활용한 조건부 로드 방식을 선언합니다.
- Option D (오답): Claude Code는 파일명의 접두사(Prefix)나 디렉터리 이름 매칭을 통해 적용 범위를 자동으로 추론하지 않으며, 반드시 `paths` 프론트매터를 명시적으로 선언해야 합니다.

---

## 48번 문제

**1. 문제 원문**

A platform engineer maintains a monorepo where test files (matching `*.test.ts`) are scattered across `src/`, `lib/`, `packages/*/tests/`, and `e2e/` directories. The engineer wants testing conventions to load automatically whenever Claude works on any test file, without duplicating the same instructions across multiple subdirectory CLAUDE.md files. Which approach best achieves this?

A) Add a CLAUDE.md file to each subdirectory that contains test files, duplicating the testing conventions text in every location so Claude picks up the guidance regardless of which directory it is currently exploring.

B) Create a single `.claude/rules/testing.md` file with `paths: ["**/*.test.ts"]` in its YAML frontmatter, so the rule loads whenever Claude reads a file matching that pattern anywhere in the repo.

C) Move the testing conventions into the root-level CLAUDE.md file so they are loaded into context at the start of every session, ensuring Claude always has the guidance available before touching any file.

D) Configure `claudeMdExcludes` in project settings to point at each test directory, which causes Claude to load the testing conventions only when those specific paths are excluded from the default context.

---

**2. 구간별 직독직해 번역**

**QUESTION:**
**A platform engineer maintains**
플랫폼 엔지니어가 관리합니다

**a monorepo**
모노레포를

**where test files (matching `*.test.ts`)**
테스트 파일들(`*.test.ts`와 일치하는)이

**are scattered across**
흩어져 있는

**`src/`, `lib/`, `packages/*/tests/`,**
`src/`, `lib/`, `packages/*/tests/`,

**and `e2e/` directories.**
그리고 `e2e/` 디렉터리들에.

**The engineer wants**
엔지니어는 원합니다

**testing conventions to load automatically**
테스트 컨벤션이 자동으로 로드되기를

**whenever Claude works on any test file,**
Claude가 임의의 테스트 파일에서 작업할 때마다,

**without duplicating the same instructions**
동일한 지침을 중복하지 않고

**across multiple subdirectory CLAUDE.md files.**
여러 서브디렉터리의 CLAUDE.md 파일들에 걸쳐.

**Which approach best achieves this?**
어떤 접근 방식이 이를 가장 잘 달성하나요?

---

**OPTIONS:**

**Option A:**
**Add a CLAUDE.md file**
CLAUDE.md 파일을 추가합니다

**to each subdirectory**
각 서브디렉터리에

**that contains test files,**
테스트 파일을 포함하는,

**duplicating the testing conventions text**
테스트 컨벤션 텍스트를 복사 중복하면서

**in every location**
모든 위치에

**so Claude picks up the guidance**
Claude가 지침을 받아들일 수 있도록

**regardless of which directory**
어느 디렉터리를

**it is currently exploring.**
현재 탐색 중인지와 관계없이.

**Option B:**
**Create a single `.claude/rules/testing.md` file**
단일 `.claude/rules/testing.md` 파일을 생성합니다

**with `paths: ["**/*.test.ts"]`**
`paths: ["**/*.test.ts"]` 설정과 함께

**in its YAML frontmatter,**
YAML 프론트매터 내에,

**so the rule loads**
규칙이 로드되도록

**whenever Claude reads a file**
Claude가 파일을 읽을 때마다

**matching that pattern**
해당 패턴과 일치하는

**anywhere in the repo.**
저장소 내 어디에서나.

**Option C:**
**Move the testing conventions**
테스트 컨벤션을 이동시킵니다

**into the root-level CLAUDE.md file**
루트 수준의 CLAUDE.md 파일 안으로

**so they are loaded into context**
컨텍스트로 로드되도록

**at the start of every session,**
모든 세션의 시작 시점에,

**ensuring Claude always has the guidance available**
Claude가 항상 사용할 수 있는 지침을 갖도록 보장하면서

**before touching any file.**
어떠한 파일이라도 다루기 전에.

**Option D:**
**Configure `claudeMdExcludes`**
`claudeMdExcludes`를 설정합니다

**in project settings**
프로젝트 설정에서

**to point at each test directory,**
각 테스트 디렉터리를 가리키도록,

**which causes Claude to load**
것은 Claude가 로드하도록 만듭니다

**the testing conventions**
테스트 컨벤션을

**only when those specific paths**
오직 해당 특정 경로들이

**are excluded from the default context.**
기본 컨텍스트에서 제외될 때만.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**B번**: Create a single `.claude/rules/testing.md` file with `paths: ["**/*.test.ts"]` in its YAML frontmatter, so the rule loads whenever Claude reads a file matching that pattern anywhere in the repo.

**정답 및 해설:**

**핵심 개념**: `.claude/rules/` 및 YAML Frontmatter `paths` 글로브 매칭
Claude Code 환경에서는 `.claude/rules/` 디렉터리 내에 개별 규칙 파일(예: `testing.md`)을 작성하고, 상단 YAML frontmatter 영역에 `paths` 글로브 패턴(예: `paths: ["**/*.test.ts"]`)을 선언할 수 있습니다. 이를 활용하면 저장소 내 디렉터리 구조와 무관하게 해당 패턴에 부합하는 파일이 활성화될 때만 조건부로 규칙을 로드하여 중복 저장을 방지하고 효율적으로 컨텍스트를 관리할 수 있습니다.

**문제 상황 분석:**
- 다양한 디렉터리(`src/`, `lib/`, `packages/*/tests/`, `e2e/`)에 `*.test.ts` 파일들이 흩어져 있습니다.
- 여러 서브디렉터리에 동일한 내용의 `CLAUDE.md` 파일을 번거롭게 중복 생성하지 않고 단일 지점에서 관리하고자 합니다.
- 테스트 파일을 열어서 작업할 때만 조건부로 컨벤션이 자동으로 로드되어야 합니다.

**B번이 정답인 이유:**
`.claude/rules/testing.md` 파일에 YAML frontmatter로 `paths: ["**/*.test.ts"]` 구문을 선언하면, 프로젝트 내의 어떠한 디렉터리에 있는 테스트 파일이든 Claude Code가 이를 읽어들일 때 자동으로 해당 규칙을 컨텍스트로 불러옵니다. 단 하나의 규칙 파일로 관리할 수 있어 유지보수가 용이하며 중복 생성을 완벽히 방지합니다.

**오답 분석:**

- Option A (오답): 각 서브디렉터리마다 `CLAUDE.md`를 추가하여 동일한 지침을 복사 및 중복 작성하는 방식은 문제 조건("without duplicating...")에 직접적으로 위배됩니다.
- Option B (정답): 단일 `.claude/rules/testing.md` 파일과 `paths: ["**/*.test.ts"]` 프론트매터를 구성합니다.
- Option C (오답): 루트 수준의 `CLAUDE.md`에 테스트 지침을 넣으면, 테스트와 상관없는 모든 개발 세션의 시작 시점부터 컨텍스트 토큰을 상시 소모하므로 자원이 낭비됩니다.
- Option D (오답): `claudeMdExcludes`는 `CLAUDE.md` 파일의 자동 로드를 **제외(차단)**하기 위한 설정이며, 해당 경로를 제외할 때 컨벤션을 로드하도록 만드는 기능이 아닙니다.

---

## 49번 문제

**1. 문제 원문**

A security team wants a nightly Claude Code job to behave identically no matter which self-hosted runner picks it up, without being affected by a stray MCP server defined in one runner's shared `.mcp.json` or a hook left in a teammate's `~/.claude` directory. Which combination of choices best achieves this reproducibility goal?

A) Invoke claude with `--output-format stream-json`, and pipe the event stream through a filter that discards any hook or MCP-related events from `.mcp.json` or `~/.claude`, so only the intended assistant content appears in the final output.

B) Invoke claude with `--bare` in print mode and pass only the explicit flags needed (such as `--append-system-prompt` or `--settings`) so nothing is auto-discovered from the working directory or home folder.

C) Invoke claude in ordinary `-p` mode, and set `--max-turns` to a low number such as 2, which limits any hook or MCP server from `.mcp.json` or `~/.claude` to at most two interactions, preventing them from altering the final output.

D) Invoke claude with `--continue` and provide a pre-recorded session file that was created in a clean environment, so the job picks up that exact conversation state instead of auto-discovering any hooks or MCP servers from the runner.

---

**2. 구간별 직독직해 번역**

**QUESTION:**
**A security team wants**
보안 팀이 원합니다

**a nightly Claude Code job**
야간 Claude Code 작업이

**to behave identically**
동일하게 동작하기를

**no matter which self-hosted runner**
어느 자체 호스팅 러너(runner)가

**picks it up,**
이를 가져가 수행하든,

**without being affected by**
영향을 받지 않고

**a stray MCP server defined**
남겨진 MCP 서버에 의해 정의된

**in one runner's shared `.mcp.json`**
한 러너의 공유 `.mcp.json` 내에

**or a hook left**
또는 남겨진 훅(hook)에 의해

**in a teammate's `~/.claude` directory.**
팀원의 `~/.claude` 디렉터리에.

**Which combination of choices**
어떤 선택의 조합이

**best achieves**
가장 잘 달성하나요

**this reproducibility goal?**
이 재현성 목표를?

---

**OPTIONS:**

**Option A:**
**Invoke claude with `--output-format stream-json`,**
`--output-format stream-json`과 함께 claude를 실행합니다,

**and pipe the event stream**
그리고 이벤트 스트림을 파이프로 연결합니다

**through a filter**
필터를 통해

**that discards any hook**
모든 훅을 폐기하는

**or MCP-related events**
또는 MCP 관련 이벤트를

**from `.mcp.json` or `~/.claude`,**
`.mcp.json` 또는 `~/.claude`로부터 발생한,

**so only the intended assistant content**
의도한 아시스턴트 내용만

**appears in the final output.**
최종 출력에 나타나도록.

**Option B:**
**Invoke claude with `--bare` in print mode**
프린트 모드에서 `--bare` 플래그와 함께 claude를 실행합니다

**and pass only the explicit flags needed**
그리고 필요한 명시적 플래그만 전달합니다

**(such as `--append-system-prompt` or `--settings`)**
(`--append-system-prompt` 또는 `--settings`와 같은)

**so nothing is auto-discovered**
아무것도 자동 탐지되지 않도록

**from the working directory or home folder.**
작업 디렉터리나 홈 폴더로부터.

**Option C:**
**Invoke claude in ordinary `-p` mode,**
일반적인 `-p` 모드로 claude를 실행합니다,

**and set `--max-turns` to a low number such as 2,**
그리고 `--max-turns`를 2와 같은 낮은 숫자로 설정합니다,

**which limits any hook or MCP server**
이는 모든 훅이나 MCP 서버를 제한합니다

**from `.mcp.json` or `~/.claude`**
`.mcp.json` 또는 `~/.claude`로부터의

**to at most two interactions,**
최대 2회의 상호작용으로,

**preventing them from altering the final output.**
이들이 최종 출력을 변경하는 것을 방지하면서.

**Option D:**
**Invoke claude with `--continue`**
`--continue`와 함께 claude를 실행합니다

**and provide a pre-recorded session file**
그리고 사전 기록된 세션 파일을 제공합니다

**that was created in a clean environment,**
깨끗한 환경에서 생성된,

**so the job picks up**
따라서 작업이 이어서 시작하도록

**that exact conversation state**
정확히 해당 대화 상태를

**instead of auto-discovering any hooks**
어떠한 훅이나 MCP 서버를 자동 탐지하는 대신

**or MCP servers from the runner.**
러너 환경으로부터.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**B번**: Invoke claude with `--bare` in print mode and pass only the explicit flags needed (such as `--append-system-prompt` or `--settings`) so nothing is auto-discovered from the working directory or home folder.

**정답 및 해설:**

**핵심 개념**: `--bare` 플래그를 통한 격리 및 자동 탐지 비활성화 (Environment Isolation)
Claude Code CLI 실행 시 `--bare` 플래그를 지정하면 로컬 작업 디렉터리나 홈 디렉터리(`~/.claude`, `.mcp.json` 등)에서 자동으로 환경 설정, 훅(Hooks), MCP 서버, 프로젝트 전용 규칙 등을 탐지하고 로드하는 동작을 완전히 차단합니다. 이를 통해 외부 환경 요소를 격리하여 일관되고 재현 가능한(Reproducible) 실행 상태를 보장할 수 있습니다.

**문제 상황 분석:**
- 여러 자체 호스팅 러너(Self-hosted runners) 환경에서 CI/CD 배치 작업을 수행할 때 환경에 따른 불일치가 발생할 수 있습니다.
- 러너 기기의 공유 `.mcp.json`이나 개발자 홈 디렉터리(`~/.claude`)에 남아있는 임의의 훅 및 MCP 서버가 실행 과정에 개입하여 출력을 오염시킬 위험이 있습니다.
- 실행 환경의 외부 요소 자동 탐지를 원천적으로 차단하고, 필요한 명시적 플래그만 지정하여 작업을 독립적으로 수행할 수 있는 방법이 필요합니다.

**B번이 정답인 이유:**
`--bare` 플래그는 작업 디렉터리 및 홈 폴더에 존재하는 설정 파일, 훅, MCP 서버 등의 자동 탐지(Auto-discovery) 기능을 비활성화합니다. 여기에 필요한 명시적 옵션(`--settings`, `--append-system-prompt` 등)만 플래그로 넘겨주면 어떠한 러너 기기에서 실행되더라도 환경 노이즈 없이 100% 동일한 결과와 동작을 보장합니다.

**오답 분석:**

- Option A (오답): 출력을 후처리(Post-filtering)하는 방식은 이미 실행 중에 훅이나 MCP 서버가 도구 실행 및 동작을 변경했거나 부작용(Side effect)을 일으킨 이후이므로 근본적인 동작 오염을 막지 못합니다.
- Option B (정답): `--bare` 플래그를 사용해 로컬 설정 파일 및 훅의 자동 탐지를 차단합니다.
- Option C (오답): `--max-turns`로 대화 회수를 제한하더라도 턴 내에서 여전히 로드된 훅이나 MCP 서버가 동작하므로 불필요한 개입이나 오류를 완전히 방지할 수 없습니다.
- Option D (오답): `--continue` 옵션은 기존 대화 기록을 이어서 진행할 뿐, 현재 실행되는 러너 환경의 훅이나 MCP 서버 자동 로드를 차단해 주지 않습니다.