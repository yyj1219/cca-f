**10번 문제**

**1. 문제 원문**

A large repository has a shared set of code-review rule files that several separate repositories across the organization should reuse verbatim, updated from one source of truth so all repositories stay in sync automatically when the source changes. An architect proposes placing symlinks inside each repository's `.claude/rules/` directory pointing back to a central rules folder maintained outside those repositories. Is this a supported way to organize rules, and what should the architect verify?

A) It is supported, but only when the shared rules sit in the same git repo as a submodule; symlinks to a separate repository never resolve

B) It is not supported; `.claude/rules/` only discovers regular files, so symlinked entries are silently ignored, forcing physical copies

C) It is supported only for whole symlinked directories, not individual files, so a single shared file like security.md cannot be linked alone

D) It is supported; `.claude/rules/` resolves symlinks normally with circular detection, so verify each repo's symlinks target the shared source

---

**2. 구간별 직독직해 번역**

**QUESTION / SCENARIO:**

**A large repository has**
대형 저장소는 가지고 있습니다

**a shared set of code-review rule files**
공유된 코드 리뷰 규칙 파일 세트를

**that several separate repositories across the organization**
조직 내의 여러 별도 저장소들이

**should reuse verbatim,**
그대로 재사용해야 하는,

**updated from one source of truth**
단 하나의 단일 진실 공급원으로부터 업데이트되며

**so all repositories stay in sync automatically**
모든 저장소가 자동으로 동기화 상태를 유지하도록

**when the source changes.**
소스가 변경될 때.

**An architect proposes**
한 아키텍트가 제안합니다

**placing symlinks inside each repository's `.claude/rules/` directory**
각 저장소의 `.claude/rules/` 디렉터리 내부에 심볼릭 링크를 배치하는 것을

**pointing back to a central rules folder**
중앙 규칙 폴더를 가리키는

**maintained outside those repositories.**
해당 저장소들 외부에서 유지 관리되는.

**Is this a supported way to organize rules,**
이것이 규칙을 조직하는 지원되는 방법인가요,

**and what should the architect verify?**
그리고 아키텍트는 무엇을 검증해야 합니까?

**OPTIONS:**

**A) It is supported,**
지원됩니다,

**but only when the shared rules sit**
하지만 공유 규칙이 위치할 때만

**in the same git repo as a submodule;**
서브모듈로서 동일한 git 저장소에;

**symlinks to a separate repository never resolve**
별도 저장소에 대한 심볼릭 링크는 절대 해결되지 않습니다

**B) It is not supported;**
지원되지 않습니다;

**`.claude/rules/` only discovers regular files,**
`.claude/rules/`는 일반 파일만 검색하므로,

**so symlinked entries are silently ignored,**
심볼릭 링크된 항목들은 조용히 무시되며,

**forcing physical copies**
물리적 복사를 강제합니다

**C) It is supported only for whole symlinked directories,**
전체 심볼릭 링크 디렉터리에 대해서만 지원되며,

**not individual files,**
개별 파일은 안 되므로,

**so a single shared file like security.md**
security.md와 같은 단일 공유 파일은

**cannot be linked alone**
단독으로 링크될 수 없습니다

**D) It is supported;**
지원됩니다;

**`.claude/rules/` resolves symlinks normally with circular detection,**
`.claude/rules/`는 순환 감지와 함께 심볼릭 링크를 정상적으로 해결하므로,

**so verify each repo's symlinks target the shared source**
각 저장소의 심볼릭 링크가 공유 소스를 가리키는지 검증하세요

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**


**D번**: It is supported; `.claude/rules/` resolves symlinks normally with circular detection, so verify each repo's symlinks target the shared source

**정답 및 해설:**


**핵심 개념**: Claude Code의 `.claude/rules/` 디렉터리에서 심볼릭 링크(symlink) 지원 여부 및 순환 감지(circular detection) 동작 메커니즘.

**문제 상황 분석:**
- 여러 저장소에서 중앙의 단일 규칙 소스를 공유하기 위해 저장소 간 규칙 파일의 동기화가 필요한 상황임.
- 아키텍트가 각 저장소의 `.claude/rules/` 내부에 외부 중앙 폴더를 가리키는 심볼릭 링크 배치를 제안함.
- 해당 접근 방식이 Claude Code 시스템에서 유효하게 지원되는지 검증이 필요함.

**D번이 정답인 이유:**
Claude Code의 규칙 디렉터리(`.claude/rules/`)는 심볼릭 링크를 정상적으로 지원하며, 순환 감지 기능을 포함하여 안전하게 경로를 해결합니다. 따라서 중앙 집중화된 규칙 소스를 심볼릭 링크로 연결하는 것은 유효한 패턴이며, 아키텍트는 각 저장소의 심볼릭 링크 타겟이 올바른 공유 소스를 정확히 가리키고 있는지 확인하면 됩니다.

**오답 분석:**

- Option A (오답): 서브모듈로 제한되지 않으며, 별도의 외부 저장소나 폴더에 대한 심볼릭 링크도 정상적으로 처리될 수 있습니다.
- Option B (오답): `.claude/rules/`가 일반 파일만 검색하여 심볼릭 링크를 무시한다는 설명은 사실과 다릅니다.
- Option C (오답): 디렉터리뿐만 아니라 심볼릭 링크 처리 방식 전반이 지원되므로 개별 파일 링크가 불가능하다는 설명은 틀렸습니다.

---

**11번 문제**

**1. 문제 원문**

A developer creates a `report-generator` skill and wants to ensure it can only write and edit files, without the ability to run shell commands or delete files. They configure `allowed-tools: Write Edit` in the `SKILL.md` frontmatter without changing the permission mode. Which statement about this approach is correct?

A) This configuration will effectively restrict the skill to only file write operations, preventing any destructive tool invocations.

B) Using `allowed-tools` is the recommended method to limit a skill's capabilities in compliance with least privilege principles.

C) Setting `allowed-tools` to `Write Edit` automatically sandboxes the skill, disabling all tools except file operations.

D) The `allowed-tools` field bypasses user permission prompts for the listed tools but does not restrict which tools are available to the skill; other tools like Bash can still be invoked.

---

**2. 구간별 직독직해 번역**

**QUESTION / SCENARIO:**

**A developer creates**
개발자가 생성합니다

**a `report-generator` skill**
`report-generator` 스킬을

**and wants to ensure**
그리고 보장하고 싶어 합니다

**it can only write and edit files,**
오직 파일을 작성하고 편집할 수만 있도록,

**without the ability**
능력 없이

**to run shell commands or delete files.**
쉘 명령어를 실행하거나 파일을 삭제하는.

**They configure `allowed-tools: Write Edit`**
그들은 설정합니다 `allowed-tools: Write Edit`를

**in the `SKILL.md` frontmatter**
`SKILL.md` frontmatter 내에서

**without changing the permission mode.**
권한 모드를 변경하지 않고.

**Which statement about this approach is correct?**
이 접근 방식에 관한 어떤 진술이 옳은가요?

**OPTIONS:**

**A) This configuration will effectively restrict**
이 설정은 효과적으로 제한할 것입니다

**the skill to only file write operations,**
스킬을 오직 파일 쓰기 작업으로만,

**preventing any destructive tool invocations.**
어떤 파괴적인 도구 호출도 방지하면서.

**B) Using `allowed-tools`**
`allowed-tools`를 사용하는 것은

**is the recommended method**
권장되는 방법입니다

**to limit a skill's capabilities**
스킬의 기능을 제한하기 위한

**in compliance with least privilege principles.**
최소 권한 원칙을 준수하여.

**C) Setting `allowed-tools` to `Write Edit`**
`allowed-tools`를 `Write Edit`로 설정하는 것은

**automatically sandboxes the skill,**
자동으로 스킬을 샌드박스 처리하고,

**disabling all tools**
모든 도구를 비활성화합니다

**except file operations.**
파일 작업을 제외하고.

**D) The `allowed-tools` field**
`allowed-tools` 필드는

**bypasses user permission prompts**
사용자 권한 프롬프트를 우회합니다

**for the listed tools**
나열된 도구들에 대해

**but does not restrict**
그러나 제한하지는 않습니다

**which tools are available to the skill;**
스킬에서 어떤 도구를 사용할 수 있는지를;

**other tools like Bash can still be invoked.**
Bash와 같은 다른 도구들은 여전히 호출될 수 있습니다.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**


**D번**: The `allowed-tools` field bypasses user permission prompts for the listed tools but does not restrict which tools are available to the skill; other tools like Bash can still be invoked.

**정답 및 해설:**


**핵심 개념**: Claude Code 스킬의 `allowed-tools` frontmatter 속성 동작 방식과 권한 프롬프트 우회 메커니즘.

**문제 상황 분석:**
- 개발자가 `report-generator` 스킬이 파일 쓰기와 편집만 수행하도록 제한하고자 합니다.
- `SKILL.md` 파일의 frontmatter에 `allowed-tools: Write Edit`를 설정하여 보안 제한을 적용하려 합니다.
- 스킬의 권한 모드를 변경하지 않은 상태에서 이 설정이 의도한 대로 도구 사용을 제한하는지 여부를 파악해야 합니다.

**D번이 정답인 이유:**
`allowed-tools` 필드는 보안상의 접근 제한(Restrict) 목적이 아니라, 나열된 도구에 대해 사용자 승인 프롬프트(permission prompts)를 생략(자동 허용)하기 위한 용도로 사용됩니다. 따라서 이 설정은 특정 도구의 사용을 차단하거나 제한하지 않으며, Bash 등 다른 도구들도 여전히 호출될 수 있습니다.

**오답 분석:**

- Option A (오답): `allowed-tools`는 도구 사용을 제한하는 보안 필터가 아니라 승인 프롬프트를 우회하는 용도이므로 파괴적인 도구 호출을 막지 못합니다.
- Option B (오답): 최소 권한 원칙을 준수하기 위해 스킬의 기능을 제한하는 올바른 권장 보안 설정 방법이 아닙니다.
- Option C (오답): 자동으로 스킬을 샌드박스 처리하거나 파일 작업 외의 도구를 원천 비활성화하지 않습니다.

---

**12번 문제**

**1. 문제 원문**

An architect finishes a plan mode session where Claude proposed a design for restructuring a shared utilities package used by 12 services. The architect wants Claude to now make the changes exactly as proposed, without re-explaining the reasoning behind each file edit. What should the architect do next?

A) Stay in plan mode indefinitely and have Claude describe each proposed file edit in the chat instead of ever actually making the edit

B) Approve the plan and choose an option that switches the session into an editing mode, so Claude proceeds directly with the approved design

C) Exit the session without approving anything and instead manually perform the twelve-service restructuring by hand rather than having Claude do it

D) Start an entirely new session from scratch and re-describe the utilities package restructuring in full before implementation begins

---

**2. 구간별 직독직해 번역**

**QUESTION / SCENARIO:**

**An architect finishes**
아키텍트가 마칩니다

**a plan mode session**
계획 모드 세션을

**where Claude proposed a design**
Claude가 설계를 제안했던

**for restructuring**
구조 조정을 위한

**a shared utilities package**
공유 유틸리티 패키지의

**used by 12 services.**
12개의 서비스에 의해 사용되는.

**The architect wants**
아키텍트는 원합니다

**Claude to now make the changes**
Claude가 이제 변경 사항을 만들기를

**exactly as proposed,**
제안된 대로 정확히,

**without re-explaining**
다시 설명하지 않고

**the reasoning behind each file edit.**
각 파일 편집 뒤에 있는 추론 과정을.

**What should the architect do next?**
아키텍트는 다음에 무엇을 해야 합니까?

**OPTIONS:**

**A) Stay in plan mode indefinitely**
무기한 계획 모드에 머무르면서

**and have Claude describe**
Claude가 설명하게 합니다

**each proposed file edit in the chat**
채팅에서 제안된 각 파일 편집을

**instead of ever actually making the edit**
실제로 편집을 수행하는 대신

**B) Approve the plan**
계획을 승인하고

**and choose an option**
옵션을 선택합니다

**that switches the session into an editing mode,**
세션을 편집 모드로 전환하는,

**so Claude proceeds directly**
그래서 Claude가 직접 진행하도록 합니다

**with the approved design**
승인된 설계와 함께

**C) Exit the session**
세션을 종료합니다

**without approving anything**
아무것도 승인하지 않고

**and instead manually perform**
대신 수동으로 수행합니다

**the twelve-service restructuring by hand**
12개 서비스 구조 조정을 손으로 직접

**rather than having Claude do it**
Claude가 그것을 하도록 하는 것보다

**D) Start an entirely new session from scratch**
처음부터 완전히 새로운 세션을 시작하고

**and re-describe**
다시 설명합니다

**the utilities package restructuring in full**
유틸리티 패키지 구조 조정을 전체적으로

**before implementation begins**
구현이 시작되기 전에

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**


**B번**: Approve the plan and choose an option that switches the session into an editing mode, so Claude proceeds directly with the approved design

**정답 및 해설:**


**핵심 개념**: Claude Code의 **Plan Mode(계획 모드)**와 승인 후 **Editing Mode(편집 모드)** 간의 전환 및 실행 흐름.

**문제 상황 분석:**
- 아키텍트가 12개 서비스에서 사용하는 공유 유틸리티 패키지 구조 조정을 위한 계획 모드 세션을 완료함.
- 추가적인 설명이나 추론 과정의 반복 없이, 제안된 설계 그대로 코드를 직접 변경하고 싶어 함.
- 계획된 내용을 효율적으로 구현 단계로 전환하기 위한 올바른 절차를 선택해야 함.

**B번이 정답인 이유:**
계획(Plan)을 승인하고 세션을 편집 모드로 전환하는 옵션을 선택하면, 이미 검증되고 합의된 설계를 바탕으로 Claude가 지연 없이 곧바로 실제 파일 수정 작업(Implementation)을 진행할 수 있기 때문입니다.

**오답 분석:**

- Option A (오답): 계획 모드에 무기한 머무르면 실제 코드 수정은 이루어지지 않고 채팅으로 설명만 반복되므로 목적에 어긋납니다.
- Option C (오답): 작업을 승인하지 않고 세션을 종료한 뒤 수동으로 직접 코드를 수정하는 것은 Claude의 자동화 기능을 활용하지 않는 비효율적인 방법입니다.
- Option D (오답): 이미 완성된 설계를 버리고 처음부터 새로운 세션을 시작하여 전체 내용을 다시 설명하는 것은 불필요한 중복 작업입니다.

---

**13번 문제**

**1. 문제 원문**

A developer asks Claude to add a check that rejects an expiration date earlier than today's date inside one existing form-validation function. The function and its surrounding file are already well understood by the team. What is the most efficient way to handle this request?

A) Delegate the task to an Explore subagent to catalog every date-handling function in the repository before writing the conditional

B) Enter plan mode first to explore whether other validation functions in the codebase should also be restructured

C) Treat the request as a multi-file migration and draft a phased plan covering all forms in the application

D) Proceed with direct execution, since the change is confined to one function with a clear, well-scoped requirement

---

**2. 구간별 직독직해 번역**

**QUESTION / SCENARIO:**

**A developer asks**
개발자가 요청합니다

**Claude to add a check**
Claude에게 검사 로직을 추가해 달라고

**that rejects an expiration date**
만료일을 거부하는

**earlier than today's date**
오늘 날짜보다 이른

**inside one existing form-validation function.**
기존의 하나의 폼 검증 함수 내에서.

**The function and its surrounding file**
해당 함수와 그 주변 파일은

**are already well understood**
이미 잘 파악되어 있습니다

**by the team.**
팀에 의해.

**What is the most efficient way**
가장 효율적인 방법은 무엇인가요?

**to handle this request?**
이 요청을 처리하기 위한?

**OPTIONS:**

**A) Delegate the task**
작업을 위임합니다

**to an Explore subagent**
Explore 서브에이전트에게

**to catalog every date-handling function**
모든 날짜 처리 함수를 분류하도록

**in the repository**
저장소 내의

**before writing the conditional**
조건문을 작성하기 전에

**B) Enter plan mode first**
먼저 계획 모드에 진입합니다

**to explore whether other validation functions**
다른 검증 함수들도 그런지 탐색하기 위해

**in the codebase should also be restructured**
코드베이스 내에서 구조 조정되어야 하는지

**C) Treat the request**
요청을 처리합니다

**as a multi-file migration**
다중 파일 마이그레이션으로

**and draft a phased plan**
그리고 단계별 계획을 초안으로 작성합니다

**covering all forms in the application**
애플리케이션 내의 모든 폼을 다루는

**D) Proceed with direct execution,**
직접 실행으로 진행합니다,

**since the change is confined**
변경 사항이 국한되어 있으므로

**to one function**
하나의 함수에

**with a clear, well-scoped requirement**
명확하고 범위가 잘 지정된 요구사항과 함께

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**


**D번**: Proceed with direct execution, since the change is confined to one function with a clear, well-scoped requirement

**정답 및 해설:**


**핵심 개념**: AI 코딩 어시스턴트 사용 시 작업의 복잡도에 따른 효율적인 실행 방식 선택 (Direct Execution vs. Plan Mode).

**문제 상황 분석:**
- 변경 대상이 기존의 단일 폼 검증 함수 하나로 명확히 제한되어 있음.
- 해당 함수와 주변 파일의 구조가 이미 팀원들에게 잘 파악되어 있어 추가적인 대규모 탐색이나 설계가 불필요함.
- 최소한의 노력으로 가장 효율적으로 작업을 처리할 수 있는 접근 방식을 선택해야 함.

**D번이 정답인 이유:**
요구사항의 범위가 좁고 명확하며 단일 함수 내의 수정으로 한정되어 있으므로, 복잡한 계획 모드나 서브에이전트 위임 없이 곧바로 직접 실행(Direct Execution)하는 것이 가장 빠르고 효율적이기 때문입니다.

**오답 분석:**

- Option A (오답): 이미 잘 파악된 단일 함수 수정에 불필요하게 Explore 서브에이전트를 동원해 전체 저장소를 탐색하는 것은 비효율적입니다.
- Option B (오답): 단일 함수 수정 요청에 대해 불필요하게 계획 모드(plan mode)에 진입하여 다른 함수들까지 확장하려 하는 것은 과도한 조치입니다.
- Option C (오답): 단일 파일/함수 수정 건을 대규모 다중 파일 마이그레이션으로 과장하여 처리하는 것은 적절하지 않습니다.

---

**14번 문제**

**1. 문제 원문**

An API team wants a rule requiring input validation and OpenAPI comments to apply only to TypeScript files inside `src/api/`, including deeply nested route handler subfolders, but not to TypeScript files elsewhere in the repo such as `src/utils/` or `src/components/`. Which paths frontmatter correctly scopes the rule to this requirement?

A) paths:
   - "src/api/**/*.ts"


B) paths:
   - "src/**/*.ts"


C) paths:
   - "api/**/*.ts"


D) paths:
   - "src/api/*.ts"

---

**2. 구간별 직독직해 번역**

**QUESTION / SCENARIO:**

**An API team wants**
API 팀이 원합니다

**a rule requiring**
요구하는 규칙을

**input validation and OpenAPI comments**
입력 유효성 검증과 OpenAPI 주석이

**to apply only to TypeScript files**
오직 TypeScript 파일에만 적용되도록

**inside `src/api/`,**
`src/api/` 내부의,

**including deeply nested route handler subfolders,**
깊게 중첩된 라우트 핸들러 서브폴더를 포함하여,

**but not to TypeScript files elsewhere in the repo**
저장소 내 다른 곳의 TypeScript 파일에는 적용되지 않도록

**such as `src/utils/` or `src/components/`.**
`src/utils/` 또는 `src/components/` 같은.

**Which paths frontmatter**
어떤 paths 프론트매터가

**correctly scopes the rule**
규칙의 범위를 올바르게 지정합니까?

**to this requirement?**
이 요구사항에 맞게?

**OPTIONS:**

**A) paths:**
paths:

**- "src/api/**/*.ts"**
- "src/api/**/*.ts"

**B) paths:**
paths:

**- "src/**/*.ts"**
- "src/**/*.ts"

**C) paths:**
paths:

**- "api/**/*.ts"**
- "api/**/*.ts"

**D) paths:**
paths:

**- "src/api/*.ts"**
- "src/api/*.ts"

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**


**A번**: `paths: - "src/api/**/*.ts"`

**정답 및 해설:**


**핵심 개념**: 글로브(Glob) 패턴을 활용한 디렉터리 경로 및 중첩 폴더 타겟팅 규칙 설정.

**문제 상황 분석:**
- API 팀은 `src/api/` 폴더 내부의 파일에만 특정 규칙을 적용하고자 합니다.
- 하위의 깊게 중첩된(deeply nested) 서브폴더까지 모두 포함되어야 합니다.
- 반면 `src/utils/`나 `src/components/` 같은 다른 경로의 파일들은 제외되어야 합니다.

**A번이 정답인 이유:**
`src/api/` 경로 아래에 있는 모든 하위 디렉터리의 중첩된 구조(`**`)와 모든 TypeScript 파일(`.ts`)을 정확하게 타겟팅할 수 있는 올바른 글로브 패턴(`src/api/**/*.ts`)을 사용했기 때문입니다.

**오답 분석:**

- Option B (오답): `src/**/*.ts`는 `src/` 하위의 모든 TypeScript 파일을 대상으로 하므로 `src/utils/`나 `src/components/`까지 포함되어 요구사항을 벗어납니다.
- Option C (오답): `api/**/*.ts`는 루트 기준의 `src/api/` 경로와 정확히 일치하지 않아 의도대로 범위를 좁히지 못할 수 있습니다.
- Option D (오답): 단일 와일드카드(`*`)인 `src/api/*.ts`는 `src/api/` 직하위의 파일만 매칭하고 중첩된 서브폴더 내부의 파일은 포함하지 못합니다.

---

**15번 문제**

**1. 문제 원문**

An architect is deciding whether a new set of database-migration conventions should live in a per-directory CLAUDE.md inside `src/db/` or as a path-scoped rule under the repository root's `.claude/rules/` with `paths: ["**/migrations/**"]`. The migration conventions need to apply to migration files scattered across several unrelated subsystems, not just one directory. Which structure fits this requirement better, and why?

A) A per-directory CLAUDE.md in `src/db/`, because path-scoped rules can only match a single exact directory, never recursive glob wildcards

B) Neither approach works for scattered files; the only option is duplicating the same CLAUDE.md manually into every directory with migrations

C) A per-directory CLAUDE.md in `src/db/`, because directory files always take precedence over rules no matter how scattered the matches are

D) A path-scoped rule under `.claude/rules/` with the migrations glob, since it targets files by pattern tree-wide rather than one directory

---

**2. 구간별 직독직해 번역**

**QUESTION / SCENARIO:**

**An architect is deciding**
아키텍트가 결정하고 있습니다

**whether a new set of database-migration conventions**
새로운 데이터베이스 마이그레이션 규칙 세트가

**should live in a per-directory CLAUDE.md**
디렉터리별 CLAUDE.md에 위치해야 하는지

**inside `src/db/`**
`src/db/` 내부의

**or as a path-scoped rule**
또는 경로 범위 지정된 규칙으로서

**under the repository root's `.claude/rules/`**
저장소 루트의 `.claude/rules/` 아래에서

**with `paths: ["**/migrations/**"]`.**
`paths: ["**/migrations/**"]`와 함께.

**The migration conventions need to apply**
마이그레이션 규칙은 적용되어야 합니다

**to migration files scattered**
흩어져 있는 마이그레이션 파일들에

**across several unrelated subsystems,**
여러 무관한 서브시스템들에 걸쳐서,

**not just one directory.**
단 하나의 디렉터리가 아니라.

**Which structure fits this requirement better,**
어떤 구조가 이 요구사항에 더 잘 부합합니까,

**and why?**
그리고 그 이유는 무엇입니까?

**OPTIONS:**

**A) A per-directory CLAUDE.md in `src/db/`,**
`src/db/` 내의 디렉터리별 CLAUDE.md는,

**because path-scoped rules can only match**
경로 범위 규칙은 오직 매칭할 수 있기 때문에

**a single exact directory,**
단 하나의 정확한 디렉터리만,

**never recursive glob wildcards**
재귀적 글로브 와일드카드는 절대 안 됨

**B) Neither approach works**
두 접근법 모두 작동하지 않습니다

**for scattered files;**
흩어진 파일들에 대해서는;

**the only option is duplicating**
유일한 선택지는 복제하는 것입니다

**the same CLAUDE.md manually**
동일한 CLAUDE.md를 수동으로

**into every directory with migrations**
마이그레이션이 있는 모든 디렉터리에

**C) A per-directory CLAUDE.md in `src/db/`,**
`src/db/` 내의 디렉터리별 CLAUDE.md는,

**because directory files always take precedence**
디렉터리 파일이 항상 우선순위를 갖기 때문에

**over rules**
규칙보다

**no matter how scattered the matches are**
매치가 얼마나 흩어져 있든 상관없이

**D) A path-scoped rule**
경로 범위 지정된 규칙은

**under `.claude/rules/`**
`.claude/rules/` 아래의

**with the migrations glob,**
마이그레이션 글로브와 함께,

**since it targets files by pattern tree-wide**
패턴으로 트리 전역에서 파일을 타겟팅하므로

**rather than one directory**
하나의 디렉터리가 아니라

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**


**D번**: A path-scoped rule under `.claude/rules/` with the migrations glob, since it targets files by pattern tree-wide rather than one directory

**정답 및 해설:**


**핵심 개념**: Claude Code에서 디렉터리별 `CLAUDE.md`와 루트 `.claude/rules/`의 경로 범위 지정(path-scoped rules) 및 글로브 패턴 활용 차이점.

**문제 상황 분석:**
- 데이터베이스 마이그레이션 파일들이 단일 디렉터리가 아닌 여러 개의 독립된 서브시스템에 걸쳐 산재해 있음.
- 특정 단일 디렉터리에만 국한되지 않고 프로젝트 전반의 흩어진 파일들에 공통 규칙을 적용해야 하는 상황임.
- 다중 디렉터리에 분산된 파일들을 효율적으로 커버할 수 있는 설정 방식을 선택해야 함.

**D번이 정답인 이유:**
`.claude/rules/` 하위에서 `paths: ["**/migrations/**"]`와 같은 재귀적 글로브(glob) 패턴을 사용하면, 특정 하나의 디렉터리에 묶이지 않고 저장소 전체(tree-wide)에 걸쳐 패턴과 일치하는 마이그레이션 파일을 타겟팅할 수 있으므로 산재된 파일들에 규칙을 적용하는 요구사항에 가장 적합합니다.

**오답 분석:**

- Option A (오답): 경로 범위 규칙(path-scoped rules)은 재귀적 글로브 와일드카드(`**`)를 지원하며 단일 디렉터리만 매칭한다는 설명은 틀렸습니다.
- Option B (오답): 글로브 패턴을 지원하는 경로 범위 규칙을 활용하면 되므로 수동으로 파일을 중복 복제할 필요가 없습니다.
- Option C (오답): 디렉터리별 `CLAUDE.md`는 `src/db/` 외부로 흩어져 있는 마이그레이션 파일들을 포괄할 수 없으므로 부적절합니다.

---

**16번 문제**

**1. 문제 원문**

A developer sets `context: fork` and `agent: Explore` on a `pr-summary` skill that fetches PR data and summarizes it. They notice the summary never reflects conventions written in the project's `CLAUDE.md`. Why not?

A) `context: fork` enforces strict isolation by removing project-level files like `CLAUDE.md` from every subagent's context, so even when using `agent: Explore`, the agent never sees the conventions and cannot apply them.


B) The `pr-summary` skill requires the `allowed-tools: Read` permission to include `CLAUDE.md` in the forked subagent's context, and without that permission, the file is not loaded even when the project has conventions.


C) The built-in `Explore` agent skips loading `CLAUDE.md` at startup to keep its context small, so a forked skill using that agent only sees the skill content and the agent's own system prompt.


D) `CLAUDE.md` conventions are only loaded when a skill is invoked without arguments, and since the `pr-summary` skill receives PR data as an argument, the conventions are omitted from the forked subagent's context.

---

**2. 구간별 직독직해 번역**

**QUESTION / SCENARIO:**

**A developer sets**
개발자가 설정합니다

**`context: fork` and `agent: Explore`**
`context: fork`와 `agent: Explore`를

**on a `pr-summary` skill**
`pr-summary` 스킬에

**that fetches PR data and summarizes it.**
PR 데이터를 가져와서 요약하는.

**They notice**
그들은 알아차립니다

**the summary never reflects conventions**
요약이 규칙들을 전혀 반영하지 않는다는 것을

**written in the project's `CLAUDE.md`.**
프로젝트의 `CLAUDE.md`에 작성된.

**Why not?**
그 이유는 무엇입니까?

**OPTIONS:**

**A) `context: fork` enforces strict isolation**
`context: fork`는 엄격한 격리를 강제합니다

**by removing project-level files like `CLAUDE.md`**
`CLAUDE.md`와 같은 프로젝트 수준의 파일을 제거함으로써

**from every subagent's context,**
모든 서브에이전트의 컨텍스트로부터,

**so even when using `agent: Explore`,**
따라서 `agent: Explore`를 사용할 때조차도,

**the agent never sees the conventions**
에이전트는 규칙들을 전혀 보지 못하며

**and cannot apply them.**
적용할 수 없습니다.

**B) The `pr-summary` skill requires**
`pr-summary` 스킬은 요구합니다

**the `allowed-tools: Read` permission**
`allowed-tools: Read` 권한을

**to include `CLAUDE.md`**
`CLAUDE.md`를 포함하기 위해

**in the forked subagent's context,**
포크된 서브에이전트의 컨텍스트에,

**and without that permission,**
그리고 그 권한이 없이는,

**the file is not loaded**
파일이 로드되지 않습니다

**even when the project has conventions.**
프로젝트에 규칙이 존재할 때조차도.

**C) The built-in `Explore` agent**
내장된 `Explore` 에이전트는

**skips loading `CLAUDE.md` at startup**
시작 시 `CLAUDE.md` 로딩을 건너뜁니다

**to keep its context small,**
컨텍스트를 작게 유지하기 위해,

**so a forked skill using that agent**
따라서 해당 에이전트를 사용하는 포크된 스킬은

**only sees the skill content**
스킬 내용과

**and the agent's own system prompt.**
에이전트 자체의 시스템 프롬프트만을 봅니다.

**D) `CLAUDE.md` conventions are only loaded**
`CLAUDE.md` 규칙들은 오직 로드됩니다

**when a skill is invoked without arguments,**
스킬이 인자 없이 호출될 때,

**and since the `pr-summary` skill receives PR data as an argument,**
그리고 `pr-summary` 스킬이 PR 데이터를 인자로 받으므로,

**the conventions are omitted**
규칙들은 생략됩니다

**from the forked subagent's context.**
포크된 서브에이전트의 컨텍스트에서.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**


**C번**: The built-in `Explore` agent skips loading `CLAUDE.md` at startup to keep its context small, so a forked skill using that agent only sees the skill content and the agent's own system prompt.

**정답 및 해설:**


**핵심 개념**: Claude Code 스킬의 `context: fork` 및 내장 `Explore` 에이전트 동작 시 `CLAUDE.md` 로딩 제외 메커니즘.

**문제 상황 분석:**
- 개발자가 `pr-summary` 스킬에 `context: fork`와 `agent: Explore` 설정을 적용함.
- 실행 결과, 생성된 요약문이 프로젝트의 `CLAUDE.md`에 정의된 코딩 컨벤션을 반영하지 못하는 현상이 발생함.
- 특정 에이전트 환경에서 프로젝트 컨벤션 파일이 누락되는 이유를 규명해야 함.

**C번이 정답인 이유:**
내장된 `Explore` 에이전트는 컨텍스트 크기를 최적화하고 작게 유지하기 위해 시작 시 `CLAUDE.md` 로딩 과정을 건너뛰도록 설계되어 있습니다. 이로 인해 `context: fork`와 `agent: Explore`를 함께 사용하는 스킬은 프로젝트의 컨벤션 정보를 전달받지 못하고, 오직 스킬의 내용과 에이전트 자체의 시스템 프롬프트만 참조하게 됩니다.

**오답 분석:**

- Option A (오답): `context: fork`가 프로젝트 수준의 파일을 무조건 제거하여 격리하는 것이 아니라, 에이전트 자체의 초기화 특성에 기인한 것입니다.
- Option B (오답): `allowed-tools: Read` 권한은 파일 읽기 권한 제어를 위한 것이며 `CLAUDE.md` 자동 로드 여부를 결정하는 직접적인 요인이 아닙니다.
- Option D (오답): 인자(arguments)의 유무에 따라 `CLAUDE.md` 로딩 여부가 결정된다는 설명은 사실과 다릅니다.

---

**17번 문제**

**1. 문제 원문**

A CLAUDE.md file has grown past 300 lines because it contains detailed conventions for GraphQL resolvers, database migrations, and CSS modules, each relevant only to a specific part of the codebase. Session-start context usage is climbing and adherence is dropping. What is the most effective way to address this using path-specific rules?

A) Copy the same GraphQL, migration, and CSS sections into a new CLAUDE.local.md file so it loads alongside the existing CLAUDE.md, giving Claude two overlapping sources of the identical guidance to reinforce adherence at every session start.


B) Wrap the GraphQL, migration, and CSS sections in HTML maintainer comments within CLAUDE.md so they are stripped before injection, then re-insert each section manually into the conversation whenever Claude happens to work on that area.


C) Move the entire CLAUDE.md content into a single .claude/rules/ file without a paths field, which shrinks the file on disk but keeps every topic's guidance loading into every session exactly as it did before the move.


D) Split each topic into its own .claude/rules/ file with a paths frontmatter scoped to the relevant file types, so each set of conventions only enters context when Claude works on matching files instead of loading all of them every session.

---

**2. 구간별 직독직해 번역**

**QUESTION / SCENARIO:**

**A CLAUDE.md file has grown past 300 lines**
CLAUDE.md 파일이 300줄을 넘겨 커졌습니다

**because it contains detailed conventions**
상세한 컨벤션들을 포함하고 있기 때문에

**for GraphQL resolvers, database migrations, and CSS modules,**
GraphQL 리졸버, 데이터베이스 마이그레이션, 그리고 CSS 모듈을 위한,

**each relevant only to a specific part of the codebase.**
각각 코드베이스의 특정 부분에만 관련이 있는.

**Session-start context usage is climbing**
세션 시작 시 컨텍스트 사용량이 증가하고 있으며

**and adherence is dropping.**
준수율(따르는 비율)은 떨어지고 있습니다.

**What is the most effective way**
가장 효과적인 방법은 무엇인가요?

**to address this using path-specific rules?**
경로 지정 규칙을 사용하여 이를 해결하기 위한?

**OPTIONS:**

**A) Copy the same GraphQL, migration, and CSS sections**
동일한 GraphQL, 마이그레이션, CSS 섹션을 복사합니다

**into a new CLAUDE.local.md file**
새로운 CLAUDE.local.md 파일로

**so it loads alongside the existing CLAUDE.md,**
기존 CLAUDE.md와 나란히 로드되도록,

**giving Claude two overlapping sources of the identical guidance**
동일한 지침의 겹치는 두 소스를 Claude에게 제공하여

**to reinforce adherence at every session start.**
매 세션 시작 시 준수를 강화하기 위해.

**B) Wrap the GraphQL, migration, and CSS sections**
GraphQL, 마이그레이션, CSS 섹션을 감싸줍니다

**in HTML maintainer comments within CLAUDE.md**
CLAUDE.md 내의 HTML 관리자 주석으로

**so they are stripped before injection,**
주입 전에 제거되도록,

**then re-insert each section manually into the conversation**
그런 다음 각 섹션을 대화에 수동으로 다시 삽입합니다

**whenever Claude happens to work on that area.**
Claude가 우연히 해당 영역을 작업할 때마다.

**C) Move the entire CLAUDE.md content**
전체 CLAUDE.md 내용을 이동합니다

**into a single .claude/rules/ file**
단일 .claude/rules/ 파일로

**without a paths field,**
paths 필드 없이,

**which shrinks the file on disk**
디스크상의 파일 크기는 줄여주지만

**but keeps every topic's guidance loading into every session**
모든 주제의 지침이 모든 세션에 로드되도록 유지합니다

**exactly as it did before the move.**
이동 전과 정확히 똑같이.

**D) Split each topic into its own .claude/rules/ file**
각 주제를 개별 .claude/rules/ 파일로 분할합니다

**with a paths frontmatter**
paths 프론트매터와 함께

**scoped to the relevant file types,**
관련 파일 유형으로 범위를 지정하여,

**so each set of conventions only enters context**
따라서 각 컨벤션 세트는 컨텍스트에 들어옵니다

**when Claude works on matching files**
Claude가 일치하는 파일을 작업할 때만

**instead of loading all of them every session.**
매 세션마다 모든 것을 로드하는 대신에.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**


**D번**: Split each topic into its own .claude/rules/ file with a paths frontmatter scoped to the relevant file types, so each set of conventions only enters context when Claude works on matching files instead of loading all of them every session.

**정답 및 해설:**


**핵심 개념**: Claude Code의 경로 기반 규칙(`.claude/rules/` 및 `paths` 프론트매터)을 이용한 컨텍스트 최적화 및 모듈화 관리.

**문제 상황 분석:**
- `CLAUDE.md` 파일이 300줄 이상으로 비대해지면서 모든 세션 시작 시 불필요한 컨텍스트 소비가 증가함.
- GraphQL, 데이터베이스 마이그레이션, CSS 등 각기 다른 영역의 컨벤션이 하나의 파일에 뭉쳐 있어 규칙 준수율(adherence)이 떨어짐.
- 특정 파일이나 경로를 작업할 때만 해당 컨벤션이 선택적으로 로드되도록 구조 개선이 필요함.

**D번이 정답인 이유:**
각 주제별로 개별 `.claude/rules/` 파일을 만들고 `paths` 프론트매터를 사용하여 관련 파일 유형으로 범위를 지정하면, 매 세션마다 모든 규칙을 통째로 로드하는 대신 Claude가 실제로 일치하는 파일을 작업할 때만 해당 컨벤션이 선택적으로 컨텍스트에 주입되므로 컨텍스트 과부하를 방지하고 규칙 준수율을 높일 수 있습니다.

**오답 분석:**

- Option A (오답): 동일한 내용을 중복해서 로드하는 `CLAUDE.local.md`를 추가하면 컨텍스트 사용량이 오히려 더 늘어나 문제 상황을 악화시킵니다.
- Option B (오답): HTML 주석으로 감싸서 수동으로 대화마다 다시 삽입하는 방식은 비효율적이며 자동화된 규칙 관리 목적에 맞지 않습니다.
- Option C (오답): `paths` 필드 없이 단일 `.claude/rules/` 파일로 이동하기만 하면 디스크 파일 크기만 줄어들 뿐 모든 세션에 여전히 모든 지침이 로드되어 문제가 해결되지 않습니다.

---

**18번 문제**

**1. 문제 원문**

A product architect wants Claude Code to build a real-time collaborative document editor, a domain the architect has not built in before. Before any implementation starts, the architect wants to surface cache invalidation strategy, conflict resolution approach, and failure handling considerations that might not be obvious up front. What technique should the architect use?

A) Search for an open-source collaborative editor, instruct Claude to replicate its architecture, and assume that following its design patterns will automatically surface appropriate cache invalidation and conflict resolution strategies.

B) Write a complete technical specification personally covering every design decision, including cache invalidation, conflict resolution, and failure handling, then hand it to Claude as a fixed set of implementation instructions.

C) Ask Claude to implement a minimal collaborative editor, then use every bug and edge case found through manual testing as the primary source to surface cache invalidation, conflict resolution, and failure handling design considerations.

D) Give Claude a brief description of the feature and ask it to interview the architect using the AskUserQuestion tool, exploring technical implementation, edge cases, and tradeoffs before writing a spec.

---

**2. 구간별 직독직해 번역**

**QUESTION / SCENARIO:**

**A product architect wants**
제품 아키텍트가 원합니다

**Claude Code to build**
Claude Code가 구축하기를

**a real-time collaborative document editor,**
실시간 협업 문서 편집기를,

**a domain**
도메인을

**the architect has not built in before.**
아키텍트가 이전에 구축해 본 적 없는.

**Before any implementation starts,**
구현이 시작되기 전에,

**the architect wants to surface**
아키텍트는 표면화(발견)하고자 합니다

**cache invalidation strategy,**
캐시 무효화 전략,

**conflict resolution approach,**
충돌 해결 접근 방식, 그리고

**and failure handling considerations**
실패 처리 고려 사항들을

**that might not be obvious up front.**
사전에 명확하지 않을 수 있는.

**What technique should the architect use?**
아키텍트는 어떤 기법을 사용해야 합니까?

**OPTIONS:**

**A) Search for an open-source collaborative editor,**
오픈소스 협업 편집기를 검색하고,

**instruct Claude to replicate its architecture,**
Claude에게 해당 아키텍처를 복제하도록 지시하고,

**and assume that following its design patterns**
디자인 패턴을 따르는 것이

**will automatically surface**
자동으로 표면화할 것이라고 가정합니다

**appropriate cache invalidation**
적절한 캐시 무효화 및

**and conflict resolution strategies.**
충돌 해결 전략을.

**B) Write a complete technical specification personally**
완전한 기술 명세서를 직접 작성합니다

**covering every design decision,**
모든 설계 결정을 다루는,

**including cache invalidation, conflict resolution, and failure handling,**
캐시 무효화, 충돌 해결, 실패 처리를 포함하여,

**then hand it to Claude**
그런 다음 그것을 Claude에게 전달합니다

**as a fixed set of implementation instructions.**
고정된 구현 지침 세트로서.

**C) Ask Claude to implement**
Claude에게 구현하도록 요청합니다

**a minimal collaborative editor,**
최소한의 협업 편집기를,

**then use every bug and edge case**
그런 다음 모든 버그와 엣지 케이스를 사용합니다

**found through manual testing**
수동 테스트를 통해 발견된

**as the primary source**
주요 소스로서

**to surface cache invalidation, conflict resolution,**
캐시 무효화, 충돌 해결, 그리고

**and failure handling design considerations.**
실패 처리 설계 고려 사항을 표면화하기 위한.

**D) Give Claude a brief description**
Claude에게 간단한 설명을 제공합니다

**of the feature**
기능에 대한

**and ask it to interview the architect**
그리고 아키텍트를 인터뷰하도록 요청합니다

**using the AskUserQuestion tool,**
AskUserQuestion 도구를 사용하여,

**exploring technical implementation, edge cases, and tradeoffs**
기술적 구현, 엣지 케이스, 트레이드오프를 탐색하면서

**before writing a spec.**
명세서를 작성하기 전에.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**


**D번**: Give Claude a brief description of the feature and ask it to interview the architect using the AskUserQuestion tool, exploring technical implementation, edge cases, and tradeoffs before writing a spec.

**정답 및 해설:**


**핵심 개념**: 복잡하고 생소한 도메인에서 사전 구현 분석을 위한 Claude Code의 `AskUserQuestion` 도구를 활용한 대화형 요구사항 탐색 기법.

**문제 상황 분석:**
- 아키텍트가 경험이 없는 실시간 협업 문서 편집기 개발을 앞두고 있음.
- 캐시 무효화, 충돌 해결, 실패 처리 등 사전에 명확히 드러나지 않는 복잡한 설계 고려 사항을 표면화해야 함.
- 본격적인 코드 구현이나 고정된 명세서 작성 전에 숨겨진 요구사항과 트레이드오프를 도출할 효과적인 접근이 필요함.

**D번이 정답인 이유:**
`AskUserQuestion` 도구를 활용해 Claude가 개발자(아키텍트)를 인터뷰하도록 유도하면, 익숙하지 않은 도메인에서 발생할 수 있는 엣지 케이스와 기술적 트레이드오프를 사전에 철저히 탐색하고 구체적인 스펙을 작성할 수 있으므로 최선의 접근 방식입니다.

**오답 분석:**

- Option A (오답): 오픈소스 아키텍처를 단순히 모방하는 것은 특정 프로젝트의 숨겨진 캐시 무효화나 충돌 해결 전략을 자동으로 보장하지 못합니다.
- Option B (오답): 아키텍트가 직접 모든 설계를 완벽하게 작성하는 것은 사전에 드러나지 않는 고려 사항들을 발견하는 목적에 부합하지 않으며 상호작용의 이점을 살리지 못합니다.
- Option C (오답): 일단 구현부터 하고 버그를 통해 문제를 발견하는 방식은 사전 설계 단계에서 리스크를 예방하려는 요구에 역행합니다.

---

**19번 문제**

**1. 문제 원문**

A development team is migrating their `claude-code-action` workflow from beta to v1.0. They previously relied on `direct_prompt`, `custom_instructions`, `mode`, and `max_turns`. Which of the following best explains why the v1.0 action no longer uses these parameters in the same way?

A) The v1.0 action removed support for custom instructions and turn limits entirely because they are now managed automatically by the Anthropic API.

B) The parameters are still available but have been renamed to `prompt_text`, `instructions`, `execution_mode`, and `max_steps` for improved clarity.

C) The v1.0 action unifies prompt inputs and aligns with the Claude Code CLI by using a single `prompt` parameter and a `claude_args` array for other settings, while auto-detecting the execution mode.

D) The v1.0 action requires all configuration to be placed in a single `config.yml` file located in the repository root, which replaces environment variables.

---

**2. 구간별 직독직해 번역**

**QUESTION / SCENARIO:**

**A development team is migrating**
개발팀이 마이그레이션하고 있습니다

**their `claude-code-action` workflow**
자신들의 `claude-code-action` 워크플로우를

**from beta to v1.0.**
베타 버전에서 v1.0으로.

**They previously relied on**
그들은 이전에 의존했습니다

**`direct_prompt`, `custom_instructions`, `mode`, and `max_turns`.**
`direct_prompt`, `custom_instructions`, `mode`, 그리고 `max_turns`에.

**Which of the following best explains**
다음 중 무엇이 가장 잘 설명합니까?

**why the v1.0 action no longer uses**
v1.0 액션이 더 이상 사용하지 않는 이유를

**these parameters in the same way?**
이 매개변수들을 같은 방식으로?

**OPTIONS:**

**A) The v1.0 action removed support**
v1.0 액션은 지원을 제거했습니다

**for custom instructions and turn limits entirely**
커스텀 지침과 턴 제한에 대한 전면적인

**because they are now managed automatically**
이제 자동으로 관리되기 때문에

**by the Anthropic API.**
Anthropic API에 의해.

**B) The parameters are still available**
매개변수들은 여전히 사용 가능하지만

**but have been renamed**
이름이 변경되었습니다

**to `prompt_text`, `instructions`, `execution_mode`, and `max_steps`**
`prompt_text`, `instructions`, `execution_mode`, 그리고 `max_steps`로

**for improved clarity.**
향상된 명확성을 위해.

**C) The v1.0 action unifies prompt inputs**
v1.0 액션은 프롬프트 입력을 통합하고

**and aligns with the Claude Code CLI**
Claude Code CLI와 일치시킵니다

**by using a single `prompt` parameter**
단일 `prompt` 매개변수와

**and a `claude_args` array for other settings,**
기타 설정을 위한 `claude_args` 배열을 사용함으로써,

**while auto-detecting the execution mode.**
실행 모드를 자동 감지하면서.

**D) The v1.0 action requires**
v1.0 액션은 요구합니다

**all configuration to be placed**
모든 설정이 배치되도록

**in a single `config.yml` file**
단일 `config.yml` 파일에

**located in the repository root,**
저장소 루트에 위치한,

**which replaces environment variables.**
환경 변수를 대체하는.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**


**C번**: The v1.0 action unifies prompt inputs and aligns with the Claude Code CLI by using a single `prompt` parameter and a `claude_args` array for other settings, while auto-detecting the execution mode.

**정답 및 해설:**


**핵심 개념**: `claude-code-action` 베타 버전에서 v1.0으로의 전환에 따른 파라미터 구조 개편 및 Claude Code CLI와의 인터페이스 통합.

**문제 상황 분석:**
- 개발팀이 기존 베타 버전에서 사용하던 여러 개별 파라미터(`direct_prompt`, `custom_instructions`, `mode`, `max_turns`)들이 v1.0에서는 다르게 다루어짐을 발견함.
- v1.0 액션 업데이트에 따른 설정 인터페이스 변경 사유를 파악해야 함.

**C번이 정답인 이유:**
`claude-code-action` v1.0은 프롬프트 관련 입력을 단일화하고 Claude Code CLI 표준과 정렬하기 위해, 기존의 파분산된 설정들을 단일 `prompt` 파라미터와 기타 설정을 담는 `claude_args` 배열 형태로 통합했으며 실행 모드 또한 자동 감지하도록 설계되었기 때문입니다.

**오답 분석:**

- Option A (오답): 커스텀 지침이나 제한 기능이 완전히 제거되어 API가 자동 관리한다는 설명은 사실과 다릅니다.
- Option B (오답): 해당 파라미터들이 단순히 이름만 바뀌어 유지되는 것이 아니라 구조 자체가 통합 및 변경되었습니다.
- Option D (오답): 모든 설정을 루트의 단일 `config.yml` 파일로 강제한다는 설명은 액션의 파라미터 변경 구조와 일치하지 않습니다.