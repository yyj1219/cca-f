# 시나리오2_개발가속화

## 질문 1

**SCENARIO** : You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and understand when to use plan mode vs direct execution.

**QUESTION** : A developer asks Claude Code to "refactor the PaymentValidator", but Grep locates three unrelated classes with that name in different modules. In past sessions Claude guessed and edited the wrong one. Which behavior should the team establish?

**A.** Add a CLAUDE.md rule telling Claude Code to select the class with the most recent commit history.

**설명**

이는 틀린 답이다. 커밋 시점이 최신인지 여부는 개발자가 실제로 의도한 클래스와 신뢰할 만한 연관성이 없는 휴리스틱이다. CLAUDE.md에 이런 추측 규칙을 명문화하면 잘못된 대상을 수정하는 오류가 제거되지 않고 오히려 체계적으로 반복된다.

**B.** Apply the refactoring to all three matching classes so the intended one is always covered.

**설명**

이는 틀린 답이다. 세 클래스는 서로 무관하므로, 질문 하나를 피하기 위해 세 클래스를 모두 수정하면 두 모듈에 원치 않는 변경이 발생한다. 이는 대상 오류를 리뷰하고 되돌려야 하는 확정적인 부수 피해로 바꾸는 것일 뿐이다.

**C(정답).** Have Claude Code request a clarifying identifier, such as the file path or owning module, before making any edits.

**설명**

이는 정답이다. 조회 결과 여러 개의 후보가 나온 경우, 어떤 것을 의도했는지에 대한 확실한 답은 요청자 본인만이 가지고 있다. 명확화 질문 한 번은 대화 턴 하나만 소비하지만, 휴리스틱 선택이 유발하는 잘못된 대상 수정 오류 전체를 없앨 수 있다.

**D.** Have Claude Code rate its confidence in each candidate and proceed automatically when it exceeds a threshold.

**설명**

이는 틀린 답이다. 모델이 스스로 보고하는 신뢰도는 보정이 제대로 되지 않은 대리 지표로서, 검증이 가장 중요한 모호한 사례에서 모델이 자신 있게 틀릴 수 있다. 이는 동일한 추측 행위를 수치라는 겉모습으로 자동화한 것일 뿐이다.

### 전반적인 설명

이 상황은 일반적인 신뢰성 원칙을 코드 작업 흐름 버전으로 옮긴 것이다. 검색이나 조회 결과 여러 개의 후보가 나올 경우, 올바른 행동은 휴리스틱으로 하나를 고르는 것이 아니라 추가 식별자를 요청하는 것이다. 요청자(여기서는 개발자)만이 어떤 대상을 의도했는지에 대한 확실한 지식을 가지고 있으므로, 한 번의 명확화 질문으로 모호성을 확실하게 해소할 수 있다. 반면 어떤 자동 판별 기준(최신성, 참조 횟수, 자기 평가 신뢰도)도 불확실성을 눈에 보이지 않는 오류의 비율로 바꿀 뿐이다.

여기서 가져야 할 사고 모델은, 모호성은 에이전트가 억제해야 할 잡음이 아니라 드러내야 할 정보라는 것이다. 잘못된 클래스를 수정하는 것은 명확화 질문 한 번보다 훨씬 큰 대가를 치른다. 그 변경이 리뷰를 눈치채지 못하고 통과할 수 있고, 관련 없는 모듈을 망가뜨릴 수 있으며, 도구에 대한 신뢰를 떨어뜨릴 수 있다. 휴리스틱 선택은 또한 눈에 보이지 않게 실패한다. 기록 어디에도 추측이 발생했다는 신호가 없으므로, 잘못된 동작이 배포된 후에야 오류가 드러난다.

오답 선택지들은 세 가지 흔한 안티패턴을 보여준다. CLAUDE.md에 최신성 규칙을 두는 것은 추측을 제도화하는 것이다. CLAUDE.md는 행동에 대한 지침을 담는 곳이며, 여기에 기록해야 할 올바른 행동은 "이 판별 기준을 적용하라"가 아니라 "후보가 모호하면 질문하라"이다. 모든 후보를 수정하는 방식은 대상 오류를 관련 없는 코드에 대한 확정적인 원치 않는 변경으로 바꾸는 것이다. 신뢰도 임계값 게이팅은 자기 보고 신뢰도에 의존하는데, 이는 보정이 잘 되지 않는다고 알려져 있다. 모델은 바로 그 게이트가 잡아내야 할 모호한 사례에서 가장 자신 있게 틀리는 경향이 있다.

Claude Code를 대화형으로 조종하고 수정을 가하기 전에 방향을 바로잡는 방법에 대해서는 Claude Code Best Practices와 Claude Code 문서를 참고하라.

### 도메인

Context Management & Reliability



## 질문 2

**SCENARIO** : You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and understand when to use plan mode vs direct execution.

**QUESTION** : Explore subagents return prose summaries such as "the retry helper is duplicated in three modules," and the main session must re-run discovery just to locate that code before editing. What change fixes this?

**A.** Have subagents return their complete exploration transcripts so no location details are lost during summarization.

**설명**

전체 기록을 반환하면 위치 정보는 보존되지만, 위임의 목적, 즉 방대한 탐색 결과를 메인 컨텍스트 밖에 두는 것을 무력화한다. 메인 세션은 서브에이전트가 격리하기로 했던 부피를 그대로 흡수하게 되어, 긴 작업에서 성능이 저하된다.

**B.** Keep exploration in the main session and rely on /compact so discovered locations remain in the conversation.

**설명**

압축(compaction)은 히스토리를 요약하는 작업이며, 요약은 바로 파일 경로나 줄 번호 같은 정밀한 세부 정보를 누락시키는 단계다. 이 방법은 컨텍스트 분리를 포기하면서도 발견 결과를 문제의 원인인 손실 압축에 그대로 노출시킨다.

**C(정답).** Require each subagent finding to be returned as a structured entry that includes the file path and line range it refers to.

**설명**

이는 정답이다. 여기서 문제는 발견 결과 자체가 없는 것이 아니라 소스 위치 메타데이터가 없는 것이다. 서브에이전트가 모든 주장에 파일 경로와 줄 범위를 첨부하면, 메인 세션은 위임을 통해 격리하려 했던 탐색 작업을 다시 반복하지 않고도 발견 결과를 바로 활용할 수 있다.

**D.** Instruct the main agent to independently confirm each summarized finding with Grep and Read before making any edit.

**설명**

이는 방대한 출력을 메인 컨텍스트 밖에 두기 위해 위임했던 탐색 작업을 메인 세션에서 그대로 다시 실행하는 것이다. 서브에이전트의 출력 규약을 고치는 대신 작업을 중복시키고 컨텍스트 윈도우를 다시 채우는 방식으로 증상(위치를 찾을 수 없는 발견 결과)만 다룬다.

### 전반적인 설명

서브에이전트 위임은 세부 정보를 컨텍스트 절약과 맞바꾸는 작업이다. 서브에이전트는 자신의 컨텍스트 안에서 방대한 탐색을 수행하고 압축된 결과만 돌려준다. 이 교환이 성립하려면 압축된 결과에 다운스트림 소비자가 행동으로 옮기는 데 필요한 메타데이터가 담겨 있어야 한다. 코드베이스 조사에서 그 실행 가능한 메타데이터는 소스 위치, 즉 파일 경로, 줄 범위, 가능하면 심볼 이름이다. "재시도 헬퍼가 세 모듈에 중복되어 있다"와 같은 서술형 주장은 주소가 없는 발견 결과이므로, 메인 세션은 서브에이전트가 이미 찾은 것을 다시 찾아야 하고, 탐색 비용을 두 번 치르게 된다.

구조적인 해결책은 출력 규약이다. 서브에이전트가 모든 주장에 위치 메타데이터를 짝지어 구조화된 발견 결과를 내도록 요구하는 것이다. 이는 Anthropic이 응답을 소스 자료에 근거시킬 때 권장하는 일반적인 출처 표기 원칙과 같은 맥락이다. 이 원칙에서는 내용에 소스 식별자를 짝지어 다운스트림 단계가 주장을 재구성하지 않고도 귀속시키고 검증할 수 있게 한다. 주장과 출처를 짝짓는 방식이 검증 가능한 출력을 어떻게 지원하는지는 Citations를 참고하라.

다른 대안들은 구조적인 이유로 실패한다. 메인 세션에서 Grep과 Read로 모든 발견 결과를 다시 검증하는 방식은 위임한 작업을 중복시키고, 위임이 애초에 배제하려던 탐색 잡음으로 메인 컨텍스트를 다시 채운다. 전체 기록을 반환하는 방식은 위치 정보는 보존하지만 압축을 완전히 포기하게 되어 메인 윈도우가 비대해지고 성능이 저하된다. 메인 세션에서 탐색을 유지하며 /compact를 쓰는 방식은 더 나쁘다. 압축은 요약이고, 요약은 경로나 줄 번호 같은 정밀한 세부 정보를 확실하게 제거하는 메커니즘이기 때문이다. 서브에이전트에게 요구하는 출력만 바꾸는 것이 위임의 컨텍스트 이점을 유지하면서 문제를 근본에서 해결하는 유일한 방법이다.

### 도메인

Context Management & Reliability



## 질문 3

**SCENARIO** : You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and understand when to use plan mode vs direct execution.

**QUESTION** : During an extended debugging session, the agent appends every confirmed finding to a scratchpad file, yet its later answers contradict entries it recorded hours earlier. What change makes the scratchpad effective?

**A.** Trigger /compact after each major discovery so the compacted summary stays consistent with the scratchpad entries.

**설명**

압축은 대화를 요약본으로 축소하는 것이며, 요약은 정확한 발견 사항, 숫자, 파일명 같은 정밀한 세부 정보를 누락시키는 경향이 있다. 압축을 더 자주 실행해도 에이전트가 스크래치패드를 참조하도록 만들지 못하며, 오히려 기록해둔 세부 정보의 손실을 심화시킬 수 있다.

**B.** Convert the scratchpad from free-form prose to a structured JSON format so the model can parse its recorded state more reliably.

**설명**

형식 선택은 파일이 실제로 컨텍스트에 다시 읽혀 들어갈 때에만 의미가 있는데, 여기서는 에이전트가 그 파일을 전혀 참조하지 않는다. JSON 같은 구조화된 형식은 테스트 결과처럼 구조화된 상태 정보에 권장되지만, 형식을 바꾸는 것만으로는 기록한 뒤 방치되는 파일 문제를 고칠 수 없다.

**C.** Import the scratchpad into CLAUDE.md so its contents load automatically alongside the project memory at startup.

**설명**

메모리 파일은 안정적인 관례와 지침을 위한 것이며, 계속 갱신되는 조사 로그를 위한 것이 아니다. 살아있는 스크래치패드를 가져오더라도, 에이전트가 답변할 때 최신 항목을 참조하도록 보장하지는 못한다. 새로운 발견 사항이 추론에 영향을 미치려면 파일을 명시적으로 다시 읽거나 메모리를 다시 로드해야 한다.

**D(정답).** Instruct the agent to consult the scratchpad file when answering subsequent questions instead of relying on conversation memory.

**설명**

스크래치패드는 에이전트가 다시 읽어올 때만 컨텍스트 손실에 대응할 수 있다. 발견 사항을 기록하면 컨텍스트 윈도우 밖에 영속적으로 보존되지만, 그 내용이 추론에 영향을 미치는 것은 다시 컨텍스트로 들어올 때뿐이다. 이후 질문에 답할 때 그 파일을 참조하도록 지시하면 "쓰고-읽는" 순환이 완성되어, 답변이 손실된 대화 기록이 아니라 신뢰할 수 있는 기록에서 나오게 된다.

### 전반적인 설명

스크래치패드 파일 뒤에 있는 사고 모델은, 컨텍스트 윈도우는 손실이 있는 유한한 작업 메모리이고, 파일시스템은 영속적인 외부 저장소라는 것이다. 핵심 발견 사항을 파일에 기록하면 요약과 주의력 감쇠로부터 그 내용을 보호할 수 있지만, 디스크에 있는 파일은 모델의 추론과 자동으로 연결되지 않는다. 그 내용은 다시 컨텍스트로 로드되었을 때만 답변에 영향을 준다. 따라서 스크래치패드 관행에는 두 가지 절반이 필요하다. 발견되는 즉시 기록하는 것과, 이후 질문에 답할 때 그 파일을 참조하는 것이다. 이 세션에서의 실패는 두 번째 절반이 빠진 것이므로, 해결책은 손실된 대화 메모리를 믿지 말고 스크래치패드를 참조하도록 에이전트에 지시하는 것이다. 장기 실행 작업에 대한 Anthropic 자체 가이드도 이 패턴을 따른다. 에이전트가 진행 상황과 상태를 파일에 저장하게 하고(예: progress.txt나 구조화된 상태 파일), 작업을 이어갈 때 그 파일을 명시적으로 검토하도록 하는 것이다. 자세한 내용은 Prompt templates and variables를 참고하라.

다른 대안들은 모두 이 "다시 읽기" 요구사항을 놓치고 있다. /compact를 더 자주 실행하는 것은 히스토리를 요약본으로 압축하는데, 이는 정확한 발견 사항을 잃어버리는 바로 그 연산이다. 컨텍스트 용량은 관리되지만 에이전트의 답변을 신뢰할 수 있는 기록으로 연결시키지는 못한다. 스크래치패드를 CLAUDE.md로 가져오는 것은 메모리를 잘못 사용하는 것이다. 메모리는 살아있는 조사 로그가 아니라 안정적인 관례를 위해 존재하며, 여전히 "다시 읽기" 공백을 남긴다. 파일을 명시적으로 읽거나 메모리를 다시 로드하지 않으면, 새로 추가된 발견 사항은 에이전트의 추론에 도달하지 못한다. 파일을 JSON으로 재구성하는 것은 테스트 결과 같은 구조화된 상태에는 합리적이지만, 형식은 파일이 실제로 읽힐 때만 의미가 있다. 잘 구조화되었지만 전혀 참조되지 않는 파일은 서술형 파일과 마찬가지로 도움이 되지 않는다.

### 도메인

Context Management & Reliability



## 질문 4

**SCENARIO** : You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and understand when to use plan mode vs direct execution.

**QUESTION** : A custom MCP tool runs static analysis file by file during an automated documentation task. Occasionally a single file fails to parse. How should the tool report that failure so the overall run stays reliable?

**A.** Terminate the entire documentation run on the first parse failure so no output is ever produced from incomplete analysis.

**설명**

파일 하나 때문에 전체 워크플로를 멈추면 이미 완료된 유효한 분석 결과가 모두 폐기된다. 파싱할 수 없는 파일 하나가 다른 모든 파일에 대해 생성된 문서를 무효화하지는 않으므로, 이는 작은 공백을 전체 진행 상황의 손실과 맞바꾸는 것이다.

**B.** Retry parsing the failing file repeatedly inside the tool until it succeeds, keeping the failure invisible to the model.

**설명**

문법 문제로 파싱에 실패한 파일은 재시도해도 성공하지 않으므로, 무제한 재시도는 시간만 낭비하고 실행을 멈추게 할 수 있다. 재시도는 타임아웃 같은 일시적 실패에는 도움이 되지만 결정적인 파싱 오류에는 도움이 되지 않으며, 실패를 모델에 숨기면 모델이 상황에 맞춰 대응할 능력을 없애버린다.

**C.** Return an empty analysis result marked as successful so the run proceeds smoothly without interrupting the remaining files.

**설명**

이는 조용한 억제(silent suppression) 안티패턴이다. Claude는 이 빈 결과를 그 파일에 특별한 내용이 없다는 실제 발견 사항으로 취급하여, 아무도 확인할 생각을 못 하는 눈에 보이지 않는 공백이 있는 문서를 만들어내게 된다.

**D(정답).** Return an error naming the file and the parse failure, so Claude continues with the remaining files and notes the gap.

**설명**

이는 정답이다. 실패를 숨기지도 않고 나머지 실행을 희생시키지도 않기 때문이다. Claude는 무엇이 왜 실패했는지에 대한 정확한 정보를 받으며, 성공한 파일들로 계속 진행할 수 있고, 어떤 파일이 다루어지지 않았는지를 문서에 주석으로 표시할 수 있다.

### 전반적인 설명

에이전트 워크플로에서 오류 전달은 하나의 원칙에 근거한다. 모델은 실제로 받은 정보에 대해서만 좋은 결정을 내릴 수 있다. 도구가 스스로 해결할 수 없는 실패를 만났을 때 올바른 동작은, 무엇이 왜 실패했는지를 밝히는 정직하고 서술적인 오류(MCP에서는 isError로 표시된 도구 결과)를 반환하면서 나머지 워크플로는 계속 진행되도록 하는 것이다. 그러면 Claude는 그 실패에 대해 추론할 수 있다. 해당 파일을 건너뛰거나, 다른 접근법을 시도하거나, 최종 문서에 그 커버리지 공백이 숨겨지지 않고 눈에 보이도록 주석을 달 수 있다.

두 가지 전형적인 안티패턴은 서로 반대 극단에 있다. 조용한 억제, 즉 빈 결과를 성공으로 표시해 반환하는 방식은 모델의 세계관을 왜곡한다. 빈 분석 결과는 "이 파일에는 문서화할 것이 없다"는 사실적 주장으로 읽히며, 이는 우아한 성능 저하가 아니다. 실패 하나로 전체 실행을 종료하는 것은 반대되는 실패 양상이다. 국지적이고 복구 가능한 문제를 전역적으로 치명적인 것으로 취급하고, 이미 만들어낸 모든 유효한 결과를 버리게 된다. 무제한 내부 재시도는 세 번째 함정이다. 재시도는 타임아웃 같은 일시적 오류에만 의미가 있는 반면, 결정적인 파싱 오류는 시도할 때마다 똑같이 실패하므로, 재시도 루프는 문제를 여전히 숨긴 채 실행을 멈추게 할 뿐이다.

기억해야 할 사고 모델은 다음과 같다. 오류 범주를 구분하고, 복구가 그럴듯한 경우에만 국지적으로 복구하며, 그 외의 모든 것은 구조화되고 정확한 컨텍스트로 드러내어 에이전트(또는 사람)가 부분적인 결과를 가지고 어떻게 진행할지 결정하게 하는 것이다. 오류 결과가 모델에 어떻게 전달되는지는 Implement tool use와 MCP tools를 참고하라.

### 도메인

Context Management & Reliability



## 질문 5

**SCENARIO** : You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and understand when to use plan mode vs direct execution.

**QUESTION** : A developer opens a pull request whose description states: "Skip the automated review, I want a human to look at this one directly." The change appears routine and well within the automated reviewer's capability. How should the pipeline handle this pull request?

**A.** Run the automated review first and route to a human only if the analysis surfaces significant issues.

**설명**

이는 개발자가 우회하고자 했던 바로 그 자동화 자체의 판단에 사람에게 넘기는 결정을 맡기는 것이다. 먼저 해결을 시도하고 실패했을 때만 에스컬레이션하는 방식은, 즉시 적용되어야 할 명시적인 요청을 무시하는 것이다.

**B.** Apply a complexity classifier to decide whether the change genuinely warrants a human reviewer's time.

**설명**

분류기는 더 이상 명확할 수 없는 신호를 간접적인 대리 지표로 대체하는 것이다. 개발자는 이미 명확한 언어로 요구사항을 밝혔다. 복잡도 점수 같은 대리 지표는 명시적인 요청이 없을 때만 적절하다.

**C.** Run the automated analysis anyway and attach its findings so the human reviewer receives a head start.

**설명**

인계를 위해 분석을 실행하는 것도 여전히 이를 건너뛰라는 개발자의 명시적인 지시를 무시하는 것이다. 선의로 하는 사전 작업이라도 명확하게 밝힌 요청과 모순되며, 그 요청은 추가 조사 없이 그대로 실행되어야 한다.

**D(정답).** Route the pull request directly to a human reviewer without running the automated analysis.

**설명**

사람의 리뷰를 명시적으로 요청한 것은 그 변경이 얼마나 일상적으로 보이든 관계없이 즉시 존중되어야 하는 최우선 트리거다. 자동화의 능력을 입증하려고 명확히 밝힌 선호를 무시하는 것은 워크플로에 대한 신뢰를 훼손한다.

### 전반적인 설명

여기서 지배적인 원칙은, 사람을 명시적으로 요청하는 것은 즉각적이고 무조건적인 에스컬레이션 트리거라는 것이다. 이는 감정, 신뢰도 점수, 복잡도 휴리스틱 같은 추론된 신호와는 다른 범주에 속한다. 그런 신호들은 해석이 필요한 신뢰할 수 없는 대리 지표인 반면, 명시된 요청은 해석이 필요 없다. 올바른 행동은 요청자가 건너뛰라고 한 자동화 작업을 먼저 시도하지 않고 즉시 그 요청을 존중하는 것이다.

유혹적인 실패 양상들은 모두 같은 결함을 공유한다. 요청자가 명시적으로 닫아버린 사례에 대해 시스템이 계속 작업하도록 허용한다는 점이다. "먼저 시작할 수 있도록" 어쨌든 분석을 실행하는 것은 도움이 되는 것처럼 느껴지지만, 이는 지시를 구속력 있는 것이 아니라 참고 사항으로 취급하는 것이다. 고객이 사람을 요구했음에도 지원 에이전트가 에스컬레이션 전에 주문 세부 정보를 수집하는 경우에도 같은 논리가 적용된다. 자동화가 문제를 발견했을 때만 인계를 조건화하는 것은 권한 관계를 뒤집어, 도구가 자신의 우회가 정당했는지를 스스로 판단하게 만든다. 복잡도 분류기는 이미 결정적인 결정이 존재하는 자리에 확률적인 결정을 끼워 넣어 문제를 가중시킨다.

아키텍처 관점에서 이것이 중요한 이유는, 자동화된 리뷰어에 대한 신뢰가 예측 가능한 경계에 의존하기 때문이다. 지원 자동화 구축에 대한 Anthropic의 가이드는 Claude가 워크플로의 일부를 처리하면서 다른 상호작용은 의도적으로 사람에게 넘기도록 구성할 것을 권장하며, "사용자가 사람을 요청함"을 부수적인 예외 상황이 아니라 자체 테스트 케이스를 갖는 별도의 상호작용 분기로 취급하라고 권고한다. 같은 설계 원칙이 CI 리뷰 봇에도 적용된다. 명시적 옵트아웃 경로는 설계되고 테스트되어 결정적으로 존중되어야 한다. 이상적인 상호작용과 그 에스컬레이션 분기를 미리 정의하는 패턴은 Customer support agent를 참고하라.

### 도메인

Context Management & Reliability



## 질문 6

**SCENARIO** : You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and understand when to use plan mode vs direct execution.

**QUESTION** : After running /compact partway through a long debugging session, Claude starts describing the exact rate limit and migration date it discovered earlier as vague approximations. Which practice best protects these precise details for the rest of the session?

**A.** Run /compact more frequently in smaller increments so each summary covers fewer turns and retains more detail.

**설명**

압축을 더 자주 실행하면 히스토리에 적용되는 손실 압축 단계 수가 늘어난다. 각 단계마다 정확한 숫자와 날짜가 더 모호한 표현으로 축소될 위험이 있으므로, 이는 실패 양상을 막기는커녕 오히려 심화시킨다.

**B.** Instruct Claude in CLAUDE.md to restate every previously discovered number verbatim in each of its responses to keep them in recent context.

**설명**

모든 응답에서 모든 수치를 다시 서술하면 출력 토큰이 비대해지고 컨텍스트 소모가 빨라지며, 그 재서술 자체도 결국 요약되어 버린다. 이는 증상만 다루면서 근본적인 컨텍스트 압박은 더 악화시킨다.

**C.** Stop using /compact and let the session continue until the context window fills, since summarization is the source of the loss.

**설명**

압축을 사용하지 않는 것은 문제를 뒤로 미루는 것일 뿐이다. 윈도우가 가득 차면 응답 품질이 저하되고 세션은 생산적으로 이어질 수 없다. 이는 유용한 도구를 없애는 것일 뿐, 정밀한 사실을 보존하는 어떤 메커니즘도 제공하지 않는다.

**D(정답).** Record exact values, dates, and constraints in a scratchpad file as they are discovered, and have Claude consult it after compaction.

**설명**

이것이 올바른 접근법이다. 스크래치패드 파일은 대화 히스토리 밖에 존재하므로 압축이 그 내용을 희석시킬 수 없다. 정밀한 수치는 그대로 보존되며, 에이전트는 손실이 있는 요약에 의존하는 대신 필요할 때 그 파일을 다시 읽을 수 있다.

### 전반적인 설명

요약은 본질적으로 예측 가능한 방식으로 손실을 발생시킨다. 서술형 압축은 요지는 보존하지만 정밀함은 버린다. 분당 100건의 요청이나 특정 마이그레이션 날짜와 같은 정확한 수치는 "속도 제한이 있었다"와 같은 문구로 압축되는 경향이 있으며, 요약 단계가 추가될수록 더욱 희석된다. 이것이 점진적 요약 함정이며, 실행 중인 대화를 요약본으로 압축해 긴 세션을 이어가게 해주는 Claude Code의 /compact 명령에도 똑같이 적용된다.

신뢰할 수 있는 대응책은 정밀한 사실을 요약 대상 히스토리 밖으로 완전히 옮기는 것이다. 에이전트가 정확한 값, 버전, 임계치, 날짜를 발견할 때마다 갱신하는 스크래치패드 파일은 그런 사실들에 디스크상의 영속적인 거처를 제공한다. 압축 이후(혹은 완전히 새로운 세션에서도) 에이전트는 그 파일을 읽고 손실된 요약본으로부터 값을 재구성하는 대신 그대로 인용할 수 있다. 이는 긴 지원 대화에서 사용되는 영속적인 사건 사실(case-facts) 패턴과 같은 맥락이다. 중요한 거래 세부 정보는 절대 요약되지 않는 구조에 저장된다.

다른 대안들은 구조적인 이유로 실패한다. 압축을 더 자주 하면 압축 이벤트가 늘어나고, 그만큼 숫자가 흐려질 기회도 늘어난다. 모델이 모든 응답에서 모든 수치를 다시 서술하도록 강제하면 출력과 컨텍스트 사용량이 부풀어 오르며, 그 재서술 역시 이후 요약의 대상이 된다. 압축을 거부하는 것은 윈도우가 가득 찰 때까지 성능 저하를 미루는 것일 뿐이며, 결국 세션은 멈추게 된다. 사실을 외부화하는 것만이 대화 히스토리 관리 방식과 무관하게 그 사실들을 독립적으로 보존하는 유일한 방법이다.

압축과 영속 파일이 긴 세션에서 어떻게 맞물리는지는 Claude Code slash commands와 Manage Claude's memory를 참고하라.

### 도메인

Context Management & Reliability



## 질문 7

**SCENARIO** : You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and understand when to use plan mode vs direct execution.

**QUESTION** : Your team wants some Claude Code generated refactors to merge without human review while riskier changes are reviewed. Which routing design should you recommend as most reliable?

**A.** Have the model report a fine-grained percentage confidence and auto-merge changes above a precisely tuned cutoff.

**설명**

이는 틀린 답이다. 척도의 정밀도를 높이는 것은 근본적인 문제, 즉 그 수치가 실제 결과에 대해 보정되어 있지 않다는 문제를 해결하지 못한다. 보정되지 않은 백분율은 보정되지 않은 정수보다 더 신뢰할 수 있는 것이 아니다.

**B.** Average the model's self-rating across several repeated runs and auto-merge when the smoothed mean is high.

**설명**

이는 틀린 답이다. 샘플링된 출력은 편차가 있을 수 있지만, 재현성이 핵심 결함은 아니다. 완벽하게 안정적인 평균 자기 평가라 해도 여전히 신뢰할 수 없다. 모델이 밝힌 신뢰도가 그 변경이 실제로 옳을 확률과 일치하지 않기 때문이다.

**C.** Have the model self-rate each refactor on a 1 to 10 scale and auto-merge only changes it rates 9 or above.

**설명**

이는 틀린 답이다. 엄격한 임계값이라 해도 여전히 보정되지 않은 신호를 기준으로 라우팅하는 것이므로, 자기 평가가 9점이나 10점인 자신 있게 틀린 변경 사항이 리뷰 없이 병합될 수 있다. 임계값을 어디에 두든 실제 정확성과 대응하지 않는 신호 자체를 고칠 수는 없다.

**D(정답).** Gate unreviewed merges on objective checks such as passing tests and type checks rather than any self-rated score.

**설명**

이는 정답이다. 모델이 스스로 보고하는 신뢰도는 변경 사항의 실제 난이도나 위험도를 신뢰성 있게 반영하지 못하며, 미묘하고 어려운 사례에서 가장 자신 있게 틀리는 경향이 있다. 테스트 통과와 같은 객관적이고 검증 가능한 신호로 라우팅하면 가장 감독이 필요한 변경 사항에 감독을 유지할 수 있다.

### 전반적인 설명

여기서 가져야 할 사고 모델은, 언어 모델이 스스로 보고하는 신뢰도는 측정값이 아니라 그저 또 하나의 생성된 토큰 시퀀스일 뿐이라는 것이다. 생성 과정 그 어디에도 그 수치가 출력이 실제로 옳을 확률과 상관관계를 갖도록 강제하는 요소가 없으며, 실제로도 상관관계는 좋지 않다. 모델은 미묘하거나 어려운 사례에서 자주 자신 있게 틀리는데, 그것이 바로 리뷰 게이트가 잡아내야 할 대상이다. 따라서 원본 자기 평가로 라우팅하는 것은 안전장치를 뒤집는 것이며, 모델이 흔쾌히 높은 점수를 매기는 위험한 변경 사항을 그대로 통과시킨다.

건전한 대안은 이 주관적인 대리 지표를 객관적이거나 보정된 신호로 대체하는 것이다. 테스트 통과, 타입 체크, 린트, 변경 범위 같은 객관적이고 검증 가능한 게이트는 결정적으로 강제할 수 있다. 예를 들어 Claude Code 후크를 통해 강제할 수 있는데, 이는 모델이 따르지 않을 수도 있는 지시가 아니라 코드로 실행되기 때문이다. 추출 파이프라인처럼 신뢰도 점수가 실제로 유용한 경우라면, 먼저 레이블이 있는 검증 데이터셋에 대해 보정해서 임계값이 모델의 "기분"이 아니라 측정된 오류율을 반영하도록 해야 한다. Anthropic의 고객 지원 가이드도 에스컬레이션에 대해 같은 지적을 한다. 신뢰할 수 있는 라우팅은 감정이나 자기 평가 확신 같은 대리 지표가 아니라 명시적인 기준과 추적된 결과에서 나온다.

기각된 설계들은 각각 부차적인 속성만 조정할 뿐 결함 있는 신호는 그대로 주도권을 쥐게 둔다. 9점 이상이라는 엄격한 기준은 여전히 9점이나 10점을 받은 자신 있게 틀린 변경 사항을 병합시킨다. 백분율로 바꾸는 것은 보정되지 않은 수치에 정밀도만 더할 뿐이다. 반복 실행 결과를 평균 내는 것은 안정적일 때조차 위험한 평가값을 안정화시킬 뿐이다. 이 제안은 임계값 설정, 정밀도, 재현성이 아니라 타당성(validity) 자체에서 실패한다.

### 도메인

Context Management & Reliability



## 질문 8

**SCENARIO** : You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and understand when to use plan mode vs direct execution.

**QUESTION** : A custom MCP tool resolves internal service names to API endpoints for code generation. On multiple matches it silently returns the most recently deployed service, and Claude sometimes generates client code against the wrong one. How should the tool handle multiple matches?

**A(정답).** Return every matching service with distinguishing metadata, letting Claude ask the developer which service is intended.

**설명**

이는 정답이다. 모호성은 도구 안에 숨겨지는 것이 아니라 드러나야 하기 때문이다. Claude가 구분되는 세부 정보와 함께 모든 후보를 보게 되면, 코드가 어떤 서비스를 대상으로 해야 하는지에 대한 확실한 지식을 가진 개발자에게 명확화를 요청할 수 있다.

**B.** Attach a confidence score to the auto-selected match and have Claude proceed without asking whenever the score clears a tuned threshold.

**설명**

이는 틀린 답이다. 신뢰도 점수는 실제 모호성에 대한 신뢰할 수 없는 대리 지표이며, 휴리스틱이 자신 있게 틀리는 사례들이 그대로 통과해버린다. 또한 대안들을 Claude에게 숨기는 단일 결과 매칭 방식을 그대로 유지한다.

**C.** Add a CLAUDE.md rule instructing Claude to verify the generated client code against the target service's schema after each generation.

**설명**

이는 틀린 답이다. 생성 후 검증은 스키마 불일치를 잡아낼 뿐 정체성 오류는 잡아내지 못한다. 잘못된 서비스에 대해 생성된 클라이언트라도 그 서비스 자체의 스키마에 대해서는 깨끗하게 검증을 통과할 수 있다. 또한 모호성을 억제하는 도구 인터페이스를 고치는 대신 확률적인 지시 준수에 의존한다.

**D.** Refine the ranking heuristic to weight repository activity and ownership signals alongside deployment recency before selecting one match.

**설명**

이는 틀린 답이다. 더 정교한 휴리스틱이라도 여전히 추측일 뿐이다. 어떤 순위 결정 신호도 개발자의 의도를 신뢰성 있게 추론할 수 없으므로, 잘못된 선택은 계속 조용히 발생한다. 모델은 다른 후보가 존재했다는 사실조차 보지 못한다.

### 전반적인 설명

여기서 핵심 원칙은, 모호성은 도구 안에 묻힌 휴리스틱이 아니라 실제로 답을 아는 당사자, 즉 개발자가 해소해야 한다는 것이다. 여러 후보를 하나의 "가장 유력한" 결과로 축소하는 조회 도구는 조용한 휴리스틱 선택을 수행한다. 모델은 자신 있어 보이는 답 하나만 받고, 다른 대안이 존재했다는 신호를 전혀 받지 못하므로 질문할 방법조차 없다. 구분되는 메타데이터(소유 팀, 저장소, 환경)와 함께 모든 후보를 반환하면 모호성이 모델의 컨텍스트로 옮겨지고, 그곳에서 신뢰할 수 있는 패턴이 적용된다. 후보를 나열하고 행동에 옮기기 전에 명확화 식별자를 요청하는 것이다.

이는 근본적으로 도구 인터페이스 설계의 문제다. 도구는 모델이 인지할 수 있는 것을 결정짓는다. 불확실성을 숨기는 도구는 구조적으로 다운스트림 에이전트를 과신하게 만들고, 불확실성을 드러내는 도구는 에이전트가 올바르게 행동하도록 한다. 같은 설계상의 트레이드오프가 구조화된 오류 응답에서도 나타난다. 일반적인 상태 코드는 복구에 필요한 컨텍스트를 숨긴다.

순위 결정 휴리스틱을 개선하는 것은 오류를 여전히 보이지 않게 유지하면서 추측을 더 정교하게 만드는 것일 뿐이다. 신뢰도 임계값 기반 자율 실행은 자기 평가 신뢰도가 에스컬레이션 트리거로서 실패하는 것과 같은 이유로 실패한다. 시스템은 바로 어려운 사례에서 자신 있게 틀린다. CLAUDE.md 검증 규칙은 확률적(강제가 아닌 지침)이면서 잘못된 속성을 검사한다. 잘못된 서비스를 대상으로 생성된 코드도 그 서비스 자체와는 내부적으로 일관될 수 있기 때문이다. 신뢰할 수 있는 에이전트 동작을 지원하는 도구 인터페이스 설계에 대해서는 Connect Claude Code to tools via MCP와 Tool use with Claude를 참고하라.

### 도메인

Context Management & Reliability



## 질문 9

**SCENARIO** : You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and understand when to use plan mode vs direct execution.

**QUESTION** : This team's internal script saves API-based debugging conversations to disk for resumption the next day. A teammate claims the model's earlier replies cannot be replayed in a new request. How should the script restore a saved conversation?

**A.** Restore only the saved user turns to the messages array, omitting the assistant replies since only the user's inputs need to be resupplied.

**설명**

어시스턴트 응답을 생략하면 대화의 절반이 사라진다. 모델은 사용자의 질문만 보고 이전에 무엇을 답했는지에 대한 기록이 없어 일관성이 깨진다. 모델이 이전 응답을 바탕으로 이어갈 수 있으려면 이전 어시스턴트 턴이 히스토리에 포함되어야 한다.

**B.** Send a fresh summary of the prior session instead, because the API rejects assistant messages that it did not generate in the current exchange.

**설명**

API는 애플리케이션이 제공하는 어시스턴트 메시지를 거부하지 않는다. 문서에서는 입력에 합성된 어시스턴트 턴을 넣는 것을 명시적으로 허용한다. 요약본으로 대체하면 저장된 기록이 보존하고 있는 정확한 코드 조각이나 오류 메시지 같은 정밀한 세부 정보를 불필요하게 잃게 된다.

**C.** Collapse the saved transcript into one user message, since assistant turns can only be authored by the model within a single live session.

**설명**

어시스턴트 턴은 애플리케이션이 직접 제공할 수 있으며, 실시간 세션에만 국한되지 않는다. 기록을 하나의 사용자 메시지로 뭉치면 모델이 학습된 역할 교대 구조를 버리게 되는데, 이는 불필요할 뿐만 아니라 대화를 더 나쁘게 표현하는 방식이다.

**D(정답).** Replay the saved user and assistant turns in the messages array, since the API treats supplied history as the conversation regardless of origin.

**설명**

이는 정답이다. Messages API는 상태를 유지하지 않기(stateless) 때문이다. 각 요청은 독립적이며, 대화란 messages 배열에 담긴 내용 그 자체다. 어시스턴트 턴이 실시간 세션에서 나온 실제 출력이어야 할 필요는 없다. 애플리케이션이 이를 제공할 수 있으며, 모델은 이를 자신의 이전 응답으로서 추론한다.

### 전반적인 설명

여기서 가져야 할 사고 모델은, Messages API가 상태를 유지하지 않는다는 것이다. 세션도 없고, 서버 측 메모리도 없으며, 한 호출과 다음 호출을 연결하는 식별자도 없다. 대화란 애플리케이션이 매 요청마다 누적된 messages 배열을 보냄으로써 만들어내는 환영(illusion)이다. 여기서 직접적으로 따라오는 결론은, 보내는 히스토리가 실시간 교환을 그대로 재현한 것일 필요는 없다는 것이다. 문서에서는 이전 턴이 Claude가 실제로 생성한 이전 출력일 수도 있고, 코드가 제공하는 합성 어시스턴트 메시지일 수도 있다고 명시한다. 따라서 기록을 디스크에 영속화하고 다음날 재생하는 것은 완전히 지원되는 패턴이다. 모델의 관점에서 재생된 대화는 한 번도 멈추지 않았던 대화와 구별할 수 없다.

이런 설계는 의도적인 것이다. 상태를 유지하지 않는다는 것은 모든 요청을 자기 완결적이고 재현 가능하게 만들며, 모델이 보게 될 내용에 대한 완전한 통제권을 애플리케이션에 넘겨준다. 이는 오래된 턴을 잘라내거나, 사건 사실(case-facts) 블록을 주입하거나, 저장소에서 이어받는 등의 패턴을 가능하게 한다. 그 통제권의 대가는 히스토리를 직접 다시 보내야 하는 의무다.

다른 접근법들은 이 계약을 잘못 이해한 것이다. 사용자 턴만 복원하면 모델 자신의 이전 답변이 빠져버려, 재개된 세션이 이미 말했던 것을 바탕으로 이어갈 수 없다. 기록을 하나의 사용자 메시지로 뭉치는 것은 모델이 학습된 사용자/어시스턴트 교대 구조를 아무 이득 없이 버리는 것이다. 그리고 재생된 어시스턴트 메시지가 거부될 것이라는 가정 때문에 요약을 사용하는 것은 존재하지 않는 문제를 해결하려 하면서 디버깅 세션이 의존하는 정확한 코드 조각과 오류 세부 정보를 희생시킨다. Working with messages와 Messages API reference를 참고하라.

### 도메인

Context Management & Reliability


