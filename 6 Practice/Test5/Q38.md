SCENARIO: You are building a customer support resolution agent using the Claude Agent SDK. The agent handles high-ambiguity requests like returns, billing disputes, and account issues. It has access to your backend systems through custom Model Context Protocol (MCP) tools (get_customer, lookup_order, process_refund, escalate_to_human). Your target is 80%+ first-contact resolution while knowing when to escalate.

QUESTION: Support transcripts show that the raw internal code REFUND_BLOCKED_POLICY_774 occasionally leaks into the agent's replies instead of a customer-friendly explanation. To fix the leak, an engineer points a Claude Code session at the harness repository with two tasks: (a) locate where in the source code the string REFUND_BLOCKED_POLICY_774 is produced, and (b) list every policy definition file under config/policies/ that ends in .yaml. Which TWO tool applications correctly accomplish these tasks? (Select TWO.)

**선택지**:
1. Use Grep with the path pattern config/policies/**/*.yaml to enumerate the policy definition files
2. Use Grep to search file contents for REFUND_BLOCKED_POLICY_774 and surface where the string is produced
3. Use Glob with the pattern REFUND_BLOCKED_POLICY_774 to find the files that contain that string
4. Use Glob with a pattern such as config/policies/**/*.yaml to enumerate the policy definition files

**설명**:

**문제 구분 포인트, 어려운 표현**:
- Support transcripts: 상담 기록
- leak: 누출
