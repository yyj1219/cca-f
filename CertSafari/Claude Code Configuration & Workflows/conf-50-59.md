# 50번 문제

**1. 문제 원문**

A developer configured `.claude/rules/api-security.md` scoped with paths: `["src/api/**/*.ts"]`. During a session, Claude runs `git status` and lists the repository tree with Glob, but has not yet opened any file under `src/api/`. Based on how path-scoped rules are triggered, what should the developer expect?

* **A)** The api-security.md rule loaded as soon as Glob returned a listing that included files under src/api/, because any tool invocation surfacing a matching path counts as a trigger.
* **B)** The api-security.md rule loaded automatically the moment the session started, because all rules under .claude/rules/ are loaded unconditionally regardless of their paths frontmatter.
* **C)** The api-security.md rule has not been loaded into context yet, because path-scoped rules load when Claude reads a file matching the pattern, not merely when it uses other tools like Glob or Bash.
* **D)** The api-security.md rule will never load during this session unless the developer explicitly runs the /memory command to force path-scoped rules to activate.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A developer configured**
개발자가 설정했습니다

**`.claude/rules/api-security.md`**
`.claude/rules/api-security.md` 파일에

**scoped with paths:**
경로 범위를 지정하여

**`["src/api/**/*.ts"]`.**
`["src/api/**/*.ts"]`로

**During a session,**
세션 진행 중에

**Claude runs `git status`**
Claude는 `git status`를 실행하고

**and lists the repository tree with Glob,**
Glob으로 저장소 트리를 조회하지만

**but has not yet opened**
아직 열지 않았습니다

**any file under `src/api/`.**
`src/api/` 하위의 어떤 파일도

**Based on how path-scoped rules**
경로 범위 지정 규칙이

**are triggered,**
트리거되는 방식을 기준으로

**what should the developer expect?**
개발자는 무엇을 예상해야 합니까?

---

**OPTIONS:**

**Option A:**

**The api-security.md rule loaded**
api-security.md 규칙이 로드되었습니다

**as soon as Glob returned a listing**
Glob이 목록을 반환하자마자

**that included files under src/api/,**
src/api/ 하위 파일을 포함하는

**because any tool invocation**
모든 도구 호출이

**surfacing a matching path**
일치하는 경로를 드러내는 경우

**counts as a trigger.**
트리거로 간주되기 때문입니다

**Option B:**

**The api-security.md rule loaded automatically**
api-security.md 규칙이 자동으로 로드되었습니다

**the moment the session started,**
세션이 시작된 순간에

**because all rules under .claude/rules/**
.claude/rules/ 하위의 모든 규칙은

**are loaded unconditionally**
무조건적으로 로드되기 때문입니다

**regardless of their paths frontmatter.**
paths 프론트매터 설정과 상관없이

**Option C:**

**The api-security.md rule has not been loaded**
api-security.md 규칙은 로드되지 않았습니다

**into context yet,**
컨텍스트에 아직

**because path-scoped rules load**
경로 범위 지정 규칙은 로드되기 때문입니다

**when Claude reads a file**
Claude가 파일을 읽을 때

**matching the pattern,**
패턴과 일치하는

**not merely when it uses**
단순히 사용할 때가 아니라

**other tools like Glob or Bash.**
Glob이나 Bash 같은 다른 도구를

**Option D:**

**The api-security.md rule will never load**
api-security.md 규칙은 절대 로드되지 않을 것입니다

**during this session**
이 세션 동안

**unless the developer explicitly runs**
개발자가 명시적으로 실행하지 않는 한

**the /memory command**
/memory 명령을

**to force path-scoped rules to activate.**
경로 범위 지정 규칙을 강제로 활성화하기 위해

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**C번**: The api-security.md rule has not been loaded into context yet, because path-scoped rules load when Claude reads a file matching the pattern, not merely when it uses other tools like Glob or Bash.

**정답 및 해설:**

**핵심 개념**: 경로 범위 지정 규칙 (Path-scoped Rules)
Claude Code 프로젝트 규칙(`.claude/rules/`)에서 `paths` 메타데이터(frontmatter)를 지정하면, Claude가 해당 경로 패턴과 일치하는 파일의 내용을 읽을 때(Read File) 비로소 해당 규칙 파일이 컨텍스트에 활성화되어 로드됩니다.

**문제 상황 분석:**
- 개발자가 `src/api/**/*.ts` 경로 패턴에만 적용되는 경로 범위 규칙(`api-security.md`)을 구성했습니다.
- Claude는 세션 중 `git status` 명령을 실행하고 Glob을 사용해 디렉터리 구조 목록을 확인했습니다.
- Claude는 아직 `src/api/` 경로 내부의 특정 파일 내용을 직접 읽거나 열어보지(Read) 않았습니다.

**C번이 정답인 이유:**
Claude Code에서 `paths` 조건이 걸린 규칙은 단순 디렉터리 목록 조회(Glob)나 CLI 명령어 실행(Bash) 등으로 관련 경로가 노출되는 것만으로는 활성화되지 않습니다. Claude가 해당 경로에 일치하는 파일의 실제 내용을 직접 읽는(Read/View) 작업이 발생할 때 비로소 컨텍스트에 탑재되므로 C번이 올바른 설명입니다.

**오답 분석:**

- Option A (오답): Glob 등의 도구 결과에 경로가 포함되었다고 해서 규칙이 로드되지 않으며, 파일의 직접적인 읽기 작업이 필요합니다.
- Option B (오답): `paths` 조건이 명시되어 있지 않은 모듈/일반 규칙만 세션 시작 시 무조건 로드됩니다. 경로 범위 지정 규칙은 조건부 동적 로드 방식을 따릅니다.
- Option D (오답): `/memory` 명령어를 명시적으로 실행해야만 활성화되는 것이 아니며, 해당 파일 패턴에 접근하여 읽을 때 자동으로 동적 로드됩니다.

<br>

---

# 51번 문제

**1. 문제 원문**

A project's `.claude/skills/review/SKILL.md` is checked into the repo for team-wide use. A senior engineer also keeps a `~/.claude/skills/review/SKILL.md` on their laptop for personal projects, unaware it uses the same name. When this engineer works in the shared repository and runs `/review`, which skill actually executes?

* **A)** Neither skill executes, and Claude Code reports a naming conflict error until one of the two files is renamed.
* **B)** Both skills execute in sequence, first the personal skill and then the project skill, since Claude Code merges same-named skills rather than choosing one.
* **C)** The project skill at `.claude/skills/review/SKILL.md`, because project-scoped skills always take priority over personal ones with the same name.
* **D)** The personal skill at `~/.claude/skills/review/SKILL.md`, because a skill with the same name at the personal level overrides one from the project level.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A project's `.claude/skills/review/SKILL.md`**
프로젝트의 `.claude/skills/review/SKILL.md`가

**is checked into the repo**
저장소에 커밋되어 있습니다

**for team-wide use.**
팀 전체 사용을 위해

**A senior engineer also keeps**
시니어 엔지니어는 또한 유지하고 있습니다

**a `~/.claude/skills/review/SKILL.md`**
`~/.claude/skills/review/SKILL.md` 파일도

**on their laptop**
자신의 노트북에

**for personal projects,**
개인 프로젝트용으로

**unaware it uses the same name.**
동일한 이름을 사용한다는 것을 모른 채

**When this engineer works**
이 엔지니어가 작업할 때

**in the shared repository**
공유 저장소에서

**and runs `/review`,**
그리고 `/review`를 실행할 때

**which skill actually executes?**
어느 스킬이 실제로 실행됩니까?

---

**OPTIONS:**

**Option A:**

**Neither skill executes,**
두 스킬 모두 실행되지 않으며

**and Claude Code reports**
Claude Code가 보고합니다

**a naming conflict error**
이름 충돌 오류를

**until one of the two files**
두 파일 중 하나가

**is renamed.**
이름이 변경될 때까지

**Option B:**

**Both skills execute in sequence,**
두 스킬 모두 순차적으로 실행되며

**first the personal skill**
먼저 개인 스킬이

**and then the project skill,**
그 다음 프로젝트 스킬이 실행됩니다

**since Claude Code merges**
Claude Code가 병합하기 때문에

**same-named skills**
동일한 이름의 스킬들을

**rather than choosing one.**
하나를 선택하는 대신에

**Option C:**

**The project skill**
프로젝트 스킬이

**at `.claude/skills/review/SKILL.md`,**
`.claude/skills/review/SKILL.md`에 있는

**because project-scoped skills**
프로젝트 범주의 스킬이

**always take priority**
항상 우선권을 갖기 때문입니다

**over personal ones**
개인용 스킬보다

**with the same name.**
동일한 이름을 가진

**Option D:**

**The personal skill**
개인 스킬이

**at `~/.claude/skills/review/SKILL.md`,**
`~/.claude/skills/review/SKILL.md`에 있는

**because a skill with the same name**
동일한 이름을 가진 스킬이

**at the personal level**
개인 수준의

**overrides one**
스킬을 덮어쓰기 때문입니다

**from the project level.**
프로젝트 수준의

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: The personal skill at `~/.claude/skills/review/SKILL.md`, because a skill with the same name at the personal level overrides one from the project level.

**정답 및 해설:**

**핵심 개념**: Claude Code 스킬 우선순위 (Skill Precedence / Scope Hierarchy)
Claude Code 공식 문서에 따르면 스킬 및 설정의 검색/적용 우선순위 계층 구조는 `Enterprise > Personal (~/.claude/skills/) > Project (.claude/skills/) > Plugin` 순을 따릅니다. 동일한 이름을 가진 스킬이 여러 범위에 존재할 경우 상위 범주인 개인(Personal) 레벨의 스킬이 하위 프로젝트(Project) 레벨의 스킬을 재정의(Override)하여 우선 실행됩니다.

**문제 상황 분석:**
- 공유 저장소에 팀 공용 스킬인 `.claude/skills/review/SKILL.md`가 포함되어 있습니다.
- 엔지니어의 로컬 개인 경로에 동일한 이름을 가진 `~/.claude/skills/review/SKILL.md`가 존재합니다.
- 해당 엔지니어가 저장소 내에서 `/review` 커맨드를 실행했습니다.

**D번이 정답인 이유:**
Claude Code는 개인 맞춤 설정을 우선 적용할 수 있도록 설계되어 있습니다. 개인 계정 레벨(`~/.claude/skills/`)의 스킬이 프로젝트 계정 레벨(`.claude/skills/`)보다 높은 우선순위를 가지기 때문에, 동일한 충돌 발생 시 개인 수준의 스킬이 실행됩니다.

**오답 분석:**

- Option A (오답): 충돌 에러가 발생하는 것이 아니라 우선순위 규칙에 따라 개인 레벨 스킬이 선택 실행됩니다.
- Option B (오답): 동일 이름의 스킬은 병합 실행되지 않고 하나만 선택하여 실행됩니다.
- Option C (오답): 프로젝트 스킬이 개인 스킬보다 우선권을 갖지 않습니다. 실제 우선순위는 개인(Personal) 레벨이 프로젝트(Project) 레벨보다 높습니다.

<br>

---

# 52번 문제

**1. 문제 원문**

A team wants Claude to evaluate three candidate caching strategies for a high-traffic API, each requiring different changes to the request-handling layer, before any strategy is implemented. Once a strategy is picked, the actual code changes are expected to be small and localized to two files. How should the architect sequence this work?

* **A)** Implement all three caching strategies with direct execution first and delete two of them once the comparison is complete
* **B)** Skip the comparison entirely and implement whichever strategy is mentioned first, adjusting the choice later if it underperforms
* **C)** Use plan mode to compare the three caching strategies and settle on one, then leave plan mode and apply the small change with direct execution
* **D)** Use direct execution for the strategy comparison itself, then switch into plan mode only for the small two-file implementation that follows afterward

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A team wants Claude**
한 팀이 Claude가 수행하기를 원합니다

**to evaluate three candidate caching strategies**
세 가지 캐싱 전략 후보를 평가하기를

**for a high-traffic API,**
트래픽이 높은 API에 대해

**each requiring different changes**
각 전략은 서로 다른 변경을 필요로 합니다

**to the request-handling layer,**
요청 처리 레이어에

**before any strategy is implemented.**
어떠한 전략도 구현되기 전에

**Once a strategy is picked,**
전략이 하나 선정되면

**the actual code changes**
실제 코드 변경은

**are expected to be small**
소규모일 것으로 예상됩니다

**and localized to two files.**
그리고 두 개 파일에 국한될 것으로

**How should the architect**
아키텍터는 어떻게

**sequence this work?**
이 작업의 순서를 구성해야 합니까?

---

**OPTIONS:**

**Option A:**

**Implement all three caching strategies**
세 가지 캐싱 전략을 모두 구현하고

**with direct execution first**
먼저 직접 실행 방식으로

**and delete two of them**
그중 두 가지를 삭제합니다

**once the comparison is complete**
비교가 완료되면

**Option B:**

**Skip the comparison entirely**
비교 과정을 완전히 건너뛰고

**and implement whichever strategy**
어떤 전략이든 구현합니다

**is mentioned first,**
가장 먼저 언급된

**adjusting the choice later**
나중에 선택을 수정합니다

**if it underperforms**
성능이 미달하는 경우

**Option C:**

**Use plan mode**
플랜 모드를 사용하여

**to compare the three caching strategies**
세 가지 캐싱 전략을 비교하고

**and settle on one,**
하나로 결정한 다음

**then leave plan mode**
플랜 모드를 종료하고

**and apply the small change**
소규모 변경을 적용합니다

**with direct execution**
직접 실행 방식으로

**Option D:**

**Use direct execution**
직접 실행을 사용하고

**for the strategy comparison itself,**
전략 비교 자체에 대해서는

**then switch into plan mode**
그런 다음 플랜 모드로 전환합니다

**only for the small two-file implementation**
단 2개 파일의 소규모 구현에 대해서만

**that follows afterward**
그 이후에 이어지는

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**C번**: Use plan mode to compare the three caching strategies and settle on one, then leave plan mode and apply the small change with direct execution

**정답 및 해설:**

**핵심 개념**: Claude Code 플랜 모드 vs 직접 실행 모드 (Plan Mode vs Direct Execution)
Claude Code의 Plan Mode는 코드를 직접 수정하거나 실행하지 않고 구조 분석, 전략 비교, 영향도 평가, 리팩토링 계획 등을 안전하게 세울 때 사용하는 모드입니다. 반면 Direct Execution은 확정된 계획에 맞춰 실제로 파일 읽기/쓰기 및 명령을 수행하는 모드입니다.

**문제 상황 분석:**
- 고트래픽 API를 위한 3가지 캐싱 전략의 장단점 및 요구사항을 구현 전에 미리 평가·비교해야 합니다.
- 전략이 최종 결정된 이후의 실제 코드 수정 작업은 2개 파일에 국한된 소규모 작업입니다.
- 분석/선택 단계와 실제 코드 작성 단계에 맞춰 적절한 작업 순서를 구성해야 합니다.

**C번이 정답인 이유:**
구현 전 복잡한 아키텍처 비교 분석은 코드 변경 없이 안전하게 탐색할 수 있는 Plan Mode를 사용하는 것이 가장 적합합니다. 비교를 통해 최적의 전략 1개를 확정한 후에는, 플랜 모드를 종료하고 2개 파일에 대한 소규모 변경을 Direct Execution으로 빠르게 수행하는 것이 Claude Code의 최적 워크플로우입니다.

**오답 분석:**

- Option A (오답): 비교만 진행하면 되는 단계에서 3가지 전략을 모두 실제로 코드로 구현하는 것은 불필요한 자원 낭비와 불필요한 수정 작업을 발생시킵니다.
- Option B (오답): 비교 평가 과정을 생략하고 임의로 하나를 먼저 구현하는 것은 사전 분석 요구사항을 무시하는 방식입니다.
- Option D (오답): 전략 비교에 직접 실행을 사용하고 단순한 소규모 코드 수정에 플랜 모드를 사용하는 것은 두 모드의 역할이 서로 거꾸로 뒤바뀐 접근입니다.

<br>

---

# 53번 문제

**1. 문제 원문**

An architect wants Claude Code to build a function that validates and reformats postal addresses, but plain-English descriptions of the expected reformatting keep producing inconsistent results across a handful of edge cases (missing unit numbers, PO boxes, and rural route addresses). Which action best resolves this without adding unnecessary process overhead?

* **A)** Increase the temperature or creativity setting used by Claude Code so that it explores a wider variety of reformatting outputs for edge cases such as missing unit numbers, PO boxes, and rural routes.
* **B)** Ask Claude to implement three separate reformatting functions, one for each edge case (missing unit numbers, PO boxes, and rural routes), and let the caller decide which function to invoke.
* **C)** Schedule a full team design review meeting to formally document postal address reformatting standards, including edge cases such as missing unit numbers and PO boxes, before continuing with Claude Code.
* **D)** Supply a small set of concrete input/output examples, one per edge case (missing unit number, PO box, rural route) so each ambiguous case has an unambiguous target output.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**An architect wants**
아키텍트가 원합니다

**Claude Code to build**
Claude Code가 구현하기를

**a function that validates**
검증하는 함수를

**and reformats postal addresses,**
우편 주소를 재형식화하고

**but plain-English descriptions**
그러나 평이한 자연어 설명은

**of the expected reformatting**
예상되는 재형식화에 대한

**keep producing**
계속해서 만들어냅니다

**inconsistent results**
일관되지 않은 결과를

**across a handful of edge cases**
몇 가지 예외 상황들에서

**(missing unit numbers,**
(동·호수 누락,

**PO boxes,**
우체통 상자 주소,

**and rural route addresses).**
그리고 시골 도로 주소).

**Which action best resolves this**
어떤 조치가 이것을 가장 잘 해결합니까

**without adding**
추가하지 않고

**unnecessary process overhead?**
불필요한 프로세스 오버헤드를?

---

**OPTIONS:**

**Option A:**

**Increase the temperature**
온도를 올립니다

**or creativity setting**
또는 창의성 설정을

**used by Claude Code**
Claude Code가 사용하는

**so that it explores**
탐색할 수 있도록

**a wider variety of**
더 다양한

**reformatting outputs**
재형식화 출력 결과를

**for edge cases**
예외 상황들에 대해

**such as missing unit numbers,**
동·호수 누락과 같은

**PO boxes, and rural routes.**
우체통 상자 주소, 그리고 시골 도로.

**Option B:**

**Ask Claude to implement**
Claude에게 구현하도록 요청합니다

**three separate reformatting functions,**
세 개의 분리된 재형식화 함수를

**one for each edge case**
각 예외 상황당 하나씩

**(missing unit numbers,**
(동·호수 누락,

**PO boxes, and rural routes),**
우체통 상자 주소, 그리고 시골 도로),

**and let the caller decide**
그리고 호출자가 결정하도록 합니다

**which function to invoke.**
어떤 함수를 호출할지.

**Option C:**

**Schedule a full team**
전체 팀 일정을 잡습니다

**design review meeting**
디자인 리뷰 미팅을 위해

**to formally document**
공식적으로 문서화하기 위해

**postal address reformatting standards,**
우편 주소 재형식화 표준을

**including edge cases**
예외 상황들을 포함하여

**such as missing unit numbers**
동·호수 누락과 같은

**and PO boxes,**
그리고 우체통 상자 주소,

**before continuing with Claude Code.**
Claude Code 작업을 계속하기 전에.

**Option D:**

**Supply a small set of**
소규모의 세트를 제공합니다

**concrete input/output examples,**
구체적인 입력/출력 예시들의

**one per edge case**
예외 상황당 하나씩

**(missing unit number,**
(동·호수 누락,

**PO box, rural route)**
우체통 상자 주소, 시골 도로)

**so each ambiguous case**
각 모호한 사례가

**has an unambiguous target output.**
명확한 목표 출력을 가지도록.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: Supply a small set of concrete input/output examples, one per edge case (missing unit number, PO box, rural route) so each ambiguous case has an unambiguous target output.

**정답 및 해설:**

**핵심 개념**: 퓨샷 프롬프팅 (Few-Shot Prompting / Example-based Guidance)
LLM(대형 언어 모델)에게 복잡하거나 엣지 케이스가 존재하는 로직을 지시할 때, 단순 텍스트 설명(Plain-English Description)만으로는 모호함이 생겨 결과가 일관되지 않을 수 있습니다. 이때 각 엣지 케이스별 명확한 입력과 출력 예시(Input/Output Examples)를 제시하면 불필요한 프로세스 추가 없이 정확도와 일관성을 극대화할 수 있습니다.

**문제 상황 분석:**
- 우편 주소 재형식화 기능 개발 중 예외 사례(동·호수 누락, PO Box 등)에서 결과가 일관되지 않게 생성됨.
- 개발 프로세스상의 불필요한 오버헤드(시간·회의·구조적 복잡성 등)를 최소화하면서 문제를 해결해야 함.
- 모호한 텍스트 설명을 명확한 기준으로 보완해 주는 간단한 프롬프팅/지시 개선이 필요함.

**D번이 정답인 이유:**
각 엣지 케이스별로 구체적인 입력과 기대 출력 예시(Input/Output)를 단 몇 개만 제공해도 모델은 모호성을 완전히 해소하고 의도한 형식대로 코드를 정확히 구현합니다. 팀 전체 미팅이나 복잡한 구조 변경 없이 가장 빠르고 오버헤드가 적은 해결책입니다.

**오답 분석:**

- Option A (오답): Temperature(온도)나 창의성 설정을 높이면 출력의 무작위성(Randomness)이 증가하여 일관성이 더욱 떨어지고 모호함이 심해집니다.
- Option B (오답): 동일 목적의 함수를 엣지 케이스별로 3개로 분리하는 것은 호출 측(Caller) 코드에 불필요한 조건 분기와 복잡성을 전가하는 잘못된 설계입니다.
- Option C (오답): 문제에서 요구한 "불필요한 프로세스 오버헤드 추가 방지(without adding unnecessary process overhead)" 조건에 위배됩니다. 소규모 코드 조정에 전체 팀 리뷰 회의를 잡는 것은 과도한 오버헤드입니다.

<br>

---

# 54번 문제

**1. 문제 원문**

A developer configured `.claude/rules/db-migrations.md` with paths: `["db/migrations/**/*.sql"]`, but during a session where Claude edited a migration file, the conventions in that rule do not seem to have been followed. The developer wants to confirm exactly which instruction files loaded and when, to determine whether the rule ever entered context. What should they do?

* **A)** Run the `/init` command against the project, which regenerates every rules file from scratch and reports any pattern that fails to match a corresponding file already tracked in the repository.
* **B)** Add the rule's file path to `claudeMdExcludes` in `settings.local.json`, which prints a diagnostic message confirming whether the now-excluded rule would otherwise have loaded during the session.
* **C)** Delete and recreate `db-migrations.md` without a `paths` field, so it loads unconditionally at every launch, then confirm its content shows up by reading through the full session transcript afterward.
* **D)** Use the `InstructionsLoaded` hook to log which instruction files are loaded, when they load, and why, to verify whether `db-migrations.md` actually loaded when the migration file was read.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A developer configured**
개발자가 설정했습니다

**`.claude/rules/db-migrations.md`**
`.claude/rules/db-migrations.md` 파일을

**with paths: `["db/migrations/**/*.sql"]`,**
`["db/migrations/**/*.sql"]` 경로 조건과 함께

**but during a session**
하지만 세션 중에

**where Claude edited**
Claude가 편집했던

**a migration file,**
마이그레이션 파일을

**the conventions in that rule**
해당 규칙의 컨벤션이

**do not seem to have been followed.**
지켜지지 않은 것으로 보입니다

**The developer wants to confirm**
개발자는 확인하고 싶어 합니다

**exactly which instruction files loaded**
정확히 어떤 지침 파일들이 로드되었는지

**and when,**
그리고 언제 로드되었는지

**to determine whether**
여부를 판단하기 위해

**the rule ever entered context.**
그 규칙이 컨텍스트에 진입하긴 했었는지

**What should they do?**
개발자는 무엇을 해야 합니까?

---

**OPTIONS:**

**Option A:**

**Run the `/init` command**
`/init` 명령을 실행합니다

**against the project,**
프로젝트에 대해

**which regenerates every rules file**
이것은 모든 규칙 파일을 재생성합니다

**from scratch**
처음부터 다시

**and reports any pattern**
그리고 어떤 패턴이든 보고합니다

**that fails to match**
매칭에 실패하는

**a corresponding file**
해당하는 파일과

**already tracked in the repository.**
저장소에서 이미 추적 중인

**Option B:**

**Add the rule's file path**
규칙의 파일 경로를 추가합니다

**to `claudeMdExcludes`**
`claudeMdExcludes` 항목에

**in `settings.local.json`,**
`settings.local.json` 파일 안의

**which prints a diagnostic message**
이것은 진단 메시지를 출력합니다

**confirming whether**
여부를 확인해 주는

**the now-excluded rule**
이제는 제외된 규칙이

**would otherwise have loaded**
원래라면 로드되었을지

**during the session.**
세션 동안에

**Option C:**

**Delete and recreate `db-migrations.md`**
`db-migrations.md`를 삭제하고 다시 생성합니다

**without a `paths` field,**
`paths` 필드 없이

**so it loads unconditionally**
따라서 조건 없이 로드되도록

**at every launch,**
실행할 때마다

**then confirm its content shows up**
그런 다음 내용이 나타나는지 확인합니다

**by reading through**
자세히 읽어봄으로써

**the full session transcript afterward.**
이후 전체 세션 트랜스크립트를

**Option D:**

**Use the `InstructionsLoaded` hook**
`InstructionsLoaded` 훅을 사용합니다

**to log which instruction files are loaded,**
어떤 지침 파일이 로드되었는지 로깅하기 위해

**when they load, and why,**
언제 로드되었고 왜 로드되었는지

**to verify whether `db-migrations.md`**
`db-migrations.md`가

**actually loaded**
실제로 로드되었는지 검증하기 위해

**when the migration file was read.**
마이그레이션 읽기 작업이 일어났을 때

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: Use the `InstructionsLoaded` hook to log which instruction files are loaded, when they load, and why, to verify whether `db-migrations.md` actually loaded when the migration file was read.

**정답 및 해설:**

**핵심 개념**: Claude Code Hook System & `InstructionsLoaded`
Claude Code에는 동작 이벤트 시점에 커스텀 로직이나 진단 로깅을 실행할 수 있는 다양한 lifecycle hook이 존재합니다. 그중 `InstructionsLoaded` 훅은 규칙(`rules`)이나 지침 파일이 동적으로 실행 컨텍스트에 포함/로드되는 시점과 이유, 로드된 파일 목록 정보를 감지하고 기록하는 용도로 사용됩니다.

**문제 상황 분석:**
- 개발자가 경로 범위(`paths`) 조건이 붙은 규칙 파일(`.claude/rules/db-migrations.md`)을 작성함.
- Claude가 마이그레이션 파일 수정 시 규칙을 제대로 따르지 않는 현상이 발생함.
- 규칙 파일이 실제로 마이그레이션 파일 조회 시점에 컨텍스트에 동적으로 정상 로드(Load)되었는지 정밀 진단하려는 상황임.

**D번이 정답인 이유:**
경로 범위 지정 규칙(Path-scoped rules)이 실제로 트리거되어 컨텍스트에 포함되었는지, 그리고 언제/왜 로드되었는지를 추적하고 진단하기 위한 가장 정확한 정석 방식은 `InstructionsLoaded` 훅을 활성화하여 파일 로딩 로그 및 메타데이터를 확인하는 것입니다.

**오답 분석:**

- Option A (오답): `/init` 커맨드는 프로젝트의 Claude 설정 환경을 초기화 및 기본 작성해 주는 커맨드일 뿐, 기존 규칙 파일의 동적 로딩 타이밍을 진단해 주지 않습니다.
- Option B (오답): `claudeMdExcludes`는 특정 지침 파일의 로딩을 완전히 제외/차단할 때 사용하는 설정이며, 제외 처리된 파일이 이전에 로드되었을지 여부를 진단 메시지로 출력해 주지 않습니다.
- Option C (오답): `paths` 필드를 제거하면 항상 상시 로드되도록 우회할 수는 있으나, 이는 기존 경로 범위 지정 조건에서 왜 로드되지 않았는지 원인을 진단하고 로딩 시점을 확인하는 해결책이 아닙니다.

<br>

---

# 55번 문제

**1. 문제 원문**

An architect asks Claude Code to "normalize phone numbers to a standard format" across a customer database. After two rounds of correction, Claude is still inconsistently formatting extensions and international codes. What should the architect do to communicate the transformation more effectively?

* **A)** Ask Claude to research five phone-number libraries, extract the formatted output each produces for sample international numbers, and adopt the most frequent convention as the standard.
* **B)** Repeat the original instruction but prepend each rule with all-caps modifiers like IMPORTANT and MUST so that Claude gives the normalization task greater weight.
* **C)** Split the normalization rules into separate sections for domestic numbers, international codes, and extensions, each described with exhaustive prose detail rather than examples.
* **D)** Provide 2-3 concrete input/output pairs covering domestic, international, and extension cases so Claude can infer the exact rule from examples alone, not prose.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**An architect asks**
아키텍트가 요청합니다

**Claude Code to "normalize**
Claude Code가 "정규화하도록

**phone numbers**
전화번호를

**to a standard format"**
표준 형식으로"

**across a customer database.**
고객 데이터베이스 전체에서.

**After two rounds of correction,**
두 차례의 수정 작업을 거친 후에도,

**Claude is still**
Claude는 여전히

**inconsistently formatting**
일관되지 않게 형식화하고 있습니다

**extensions and international codes.**
내선 번호와 국가 코드를.

**What should the architect do**
아키텍트는 무엇을 해야 합니까

**to communicate the transformation**
변환 방식을 전달하기 위해

**more effectively?**
더 효과적으로?

---

**OPTIONS:**

**Option A:**

**Ask Claude to research**
Claude에게 조사를 요청합니다

**five phone-number libraries,**
5개의 전화번호 라이브러리를,

**extract the formatted output**
형식화된 출력값을 추출하고

**each produces**
각 라이브러리가 생성하는

**for sample international numbers,**
샘플 국가 번호에 대해,

**and adopt the most frequent convention**
가장 빈번한 컨벤션을 채택합니다

**as the standard.**
표준으로.

**Option B:**

**Repeat the original instruction**
원래 지침을 반복합니다

**but prepend each rule**
하지만 각 규칙 앞에 붙입니다

**with all-caps modifiers**
대문자 수식어를

**like IMPORTANT and MUST**
IMPORTANT나 MUST와 같은

**so that Claude gives**
Claude가 부여하도록

**the normalization task**
정규화 작업에

**greater weight.**
더 큰 가중치를.

**Option C:**

**Split the normalization rules**
정규화 규칙을 분할합니다

**into separate sections**
별도의 섹션으로

**for domestic numbers,**
국내 번호,

**international codes, and extensions,**
국가 코드, 그리고 내선 번호에 대한,

**each described with**
각각이 서술되도록

**exhaustive prose detail**
철저한 산문(줄글) 상세 설명으로

**rather than examples.**
예시 대신에.

**Option D:**

**Provide 2-3 concrete**
2~3개의 구체적인

**input/output pairs**
입력/출력 쌍을 제공합니다

**covering domestic, international,**
국내, 국가,

**and extension cases**
그리고 내선 번호 사례를 다루는

**so Claude can infer**
Claude가 추론할 수 있도록

**the exact rule**
정확한 규칙을

**from examples alone, not prose.**
글이 아닌 예시만으로도.

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: Provide 2-3 concrete input/output pairs covering domestic, international, and extension cases so Claude can infer the exact rule from examples alone, not prose.

**정답 및 해설:**

**핵심 개념**: 예시 기반 프롬프팅 (Few-Shot / Example-Driven Prompting)
LLM(대형 언어 모델)은 모호하거나 길게 서술된 텍스트 설명(Prose)보다 명확한 **입력-출력 예시(Input/Output Examples)**를 통해 규칙과 변환 패턴을 훨씬 더 정확하고 일관되게 이해합니다. 

**문제 상황 분석:**
- 전화번호 데이터베이스 정규화 작업 중 내선 번호 및 국가 코드 형식이 일관되지 않는 문제 발생.
- 2차례에 걸쳐 텍스트 지시사항으로 수정을 시도했으나 지속적으로 모호성이 해소되지 않음.
- 텍스트 설명의 한계를 극복하고 모델에게 변환 기준을 가장 명확하고 효과적으로 전달하는 방법이 필요함.

**D번이 정답인 이유:**
긴 줄글 설명이나 단순 강조(IMPORTANT, MUST)보다 각 예외 케이스(국내, 국가 코드, 내선 번호 등)별로 구체적인 입력값과 기대되는 출력값 쌍(2~3개)을 제시하는 것이 모호함을 없애는 가장 확실한 방법입니다. Claude는 이러한 구체적 입출력 예시로부터 명확한 규칙을 추론하여 일관된 변환 결과를 출력할 수 있습니다.

**오답 분석:**

- Option A (오답): 라이브러리를 조사하고 빈도수를 계산하는 것은 불필요한 컴퓨팅과 단계를 낭비하며, 사용자가 원하는 고유한 표준 포맷을 보장해 주지 못합니다.
- Option B (오답): 대문자 강조(IMPORTANT, MUST 등)는 지시사항의 모호함을 해소해 주지 못하므로 형식 불일치 문제를 해결할 수 없습니다.
- Option C (오답): 예시 없이 줄글(prose) 설명만 길게 늘려 작성하면 모델이 해석할 수 있는 모호성이나 예외 상황이 여전히 발생하여 인공지능의 불일치를 해결하지 못합니다.

<br>

---

# 56번 문제

**1. 문제 원문**

A team's release-notes generator asks Claude to extract structured data and passes the `--json-schema` flag with a schema string that contains a typo (an unescaped brace). What should they expect to happen?

* **A)** claude exits with an error stating the value is not a valid JSON Schema, along with the validator's diagnostic, instead of producing any output
* **B)** claude silently falls back to unstructured plain-text output with no indication that the schema was invalid
* **C)** claude ignores the malformed schema and instead of failing, returns an empty structured_output object with no fields
* **D)** claude retries the request against the API up to three times using --max-retries before giving up and printing the raw text response

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A team's release-notes generator**
한 팀의 릴리즈 노트 생성기가

**asks Claude**
Claude에게 요청합니다

**to extract structured data**
구조화된 데이터를 추출하도록

**and passes the `--json-schema` flag**
그리고 `--json-schema` 플래그를 전달합니다

**with a schema string**
스키마 문자열과 함께

**that contains a typo**
오타가 포함된

**(an unescaped brace).**
(이스케이프되지 않은 중괄호).

**What should they expect**
그들은 무엇을 예상해야 합니까

**to happen?**
발생할 일로?

---

**OPTIONS:**

**Option A:**

**claude exits with an error**
claude가 오류와 함께 종료됩니다

**stating the value is not**
값이 아니라고 명시하는

**a valid JSON Schema,**
유효한 JSON 스키마가

**along with the validator's diagnostic,**
검증기의 진단 메시지와 함께,

**instead of producing any output**
어떠한 출력 결과도 생성하는 대신에

**Option B:**

**claude silently falls back**
claude가 소리 없이 대체(폴백)됩니다

**to unstructured plain-text output**
구조화되지 않은 일반 텍스트 출력으로

**with no indication**
아무런 표시 없이

**that the schema was invalid**
스키마가 유효하지 않았다는

**Option C:**

**claude ignores the malformed schema**
claude가 손상된 스키마를 무시하고

**and instead of failing,**
실패하는 대신에,

**returns an empty structured_output object**
빈 structured_output 객체를 반환합니다

**with no fields**
필드가 없는

**Option D:**

**claude retries the request**
claude가 요청을 재시도합니다

**against the API**
API에 대해

**up to three times**
최대 3회까지

**using --max-retries**
--max-retries를 사용하여

**before giving up**
포기하기 전에

**and printing the raw text response**
그리고 원시 텍스트 응답을 출력하기 전에

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**A번**: claude exits with an error stating the value is not a valid JSON Schema, along with the validator's diagnostic, instead of producing any output

**정답 및 해설:**

**핵심 개념**: CLI 인자 검증 및 구조화된 출력 (CLI Flag Validation & Structured Output)
Claude Code CLI에서 `--json-schema` 플래그를 사용할 때, 전달된 스키마 문자열은 API 호출이나 출력 생성 전 로컬에서 먼저 구문 분석(Parsing) 및 유효성 검증(Validation)을 거칩니다. 문법 오류(Syntax Error)가 존재하는 스키마가 제공되면 즉시 프로세스를 에러와 함께 종료시킵니다.

**문제 상황 분석:**
- 릴리즈 노트 생성기가 `--json-schema` 플래그를 통해 스키마 문자열을 전달함.
- 전달된 스키마 내에 이스케이프되지 않은 중괄호 등 오타(Typo)가 포함되어 파싱 불가능한 잘못된 형식(Malformed)임.
- 잘못된 CLI 플래그 인자가 입력되었을 때 Claude CLI의 동작 방식을 파악해야 함.

**A번이 정답인 이유:**
`--json-schema`에 전달된 값이 올바른 JSON Schema 형식이 아닌 경우, Claude CLI는 유효성 검증기(Validator) 수준에서 오류를 감지하고 상세한 진단 메시지(Diagnostic)를 출력하며 즉시 에러 코드와 함께 종료(Exit)됩니다. 조용히 텍스트로 전환되거나 무시하고 진행되지 않습니다.

**오답 분석:**

- Option B (오답): 구문 오류가 발생한 상황에서 경고나 오류 표시 없이 소리 없이(Silently) 평문 텍스트로 폴백하지 않습니다.
- Option C (오답): 잘못된 형식을 무시하고 빈 객체를 반환하지 않고, 사전에 검증 실패 에러를 발생시킵니다.
- Option D (오답): 문법 오류가 있는 스키마 파싱 실패는 네트워크/API 재시도 대상이 아니므로 `--max-retries` 동작이 적용되지 않습니다.

<br>

---

# 57번 문제

**1. 문제 원문**

Before implementing a multi-region deployment strategy for a service Claude Code has not worked on before, an architect wants to surface failure-mode considerations such as what happens during a regional outage, how state is reconciled after a network partition heals, and which region is authoritative during a split. What is the recommended way to elicit this input before implementation?

* **A)** Have Claude begin writing the deployment configuration immediately, and rely on production incidents to surface failure modes such as regional outages and partition recovery, adjusting the configuration iteratively as issues arise.
* **B)** Ask Claude to summarize a generic multi-region deployment tutorial, and then adopt its default failure-handling mechanisms, such as failover and consistency, without tailoring them to this service's specific regional constraints.
* **C)** Write out every failure scenario, including regional outages and partition recovery, in exhaustive detail personally beforehand, so that no interview or clarifying questions from Claude are necessary to surface failure-mode considerations.
* **D)** Give Claude a brief description of the deployment goal and have it interview the architect in detail using the AskUserQuestion tool, focusing on failure modes and tradeoffs rather than obvious questions.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**Before implementing**
구현하기 전에

**a multi-region deployment strategy**
다중 리전 배포 전략을

**for a service**
서비스에 대해

**Claude Code has not worked on before,**
Claude Code가 이전에 작업해보지 않은

**an architect wants to surface**
아키텍트가 드러내고자 합니다

**failure-mode considerations**
실패 모드 고려사항들을

**such as what happens**
무슨 일이 일어나는지와 같은

**during a regional outage,**
리전 장애 중에

**how state is reconciled**
상태가 어떻게 조정되는지

**after a network partition heals,**
네트워크 파티션이 복구된 후

**and which region is authoritative**
그리고 어느 리전이 권한을 갖는지

**during a split.**
분할(split-brain) 상태 동안

**What is the recommended way**
권장되는 방법은 무엇입니까

**to elicit this input**
이러한 입력을 도출하기 위한

**before implementation?**
구현 전에?

---

**OPTIONS:**

**Option A:**

**Have Claude begin writing**
Claude가 작성을 시작하도록 합니다

**the deployment configuration immediately,**
배포 설정을 즉시

**and rely on production incidents**
그리고 운영 환경 장애에 의존합니다

**to surface failure modes**
실패 모드를 드러내기 위해

**such as regional outages**
리전 장애와 같은

**and partition recovery,**
그리고 파티션 복구

**adjusting the configuration iteratively**
설정을 반복적으로 수정하면서

**as issues arise.**
문제가 발생할 때마다

**Option B:**

**Ask Claude to summarize**
Claude에게 요약을 요청합니다

**a generic multi-region deployment tutorial,**
일반적인 다중 리전 배포 튜토리얼을

**and then adopt its default**
그런 다음 기본 메커니즘을 채택합니다

**failure-handling mechanisms,**
장애 처리

**such as failover and consistency,**
장애 복구(failover) 및 일관성과 같은

**without tailoring them**
맞춤 조정 없이

**to this service's specific regional constraints.**
이 서비스의 특정 리전 제약 조건에

**Option C:**

**Write out every failure scenario,**
모든 장애 시나리오를 작성합니다

**including regional outages**
리전 장애를 포함하여

**and partition recovery,**
그리고 파티션 복구

**in exhaustive detail personally beforehand,**
미리 직접 철저한 세부 사항으로

**so that no interview**
따라서 어떤 인터뷰도

**or clarifying questions from Claude**
또는 Claude의 명확화 질문도

**are necessary**
필요하지 않도록

**to surface failure-mode considerations.**
실패 모드 고려사항을 드러내기 위해

**Option D:**

**Give Claude a brief description**
Claude에게 간략한 설명을 제공합니다

**of the deployment goal**
배포 목표에 대한

**and have it interview the architect**
그리고 아키텍트를 인터뷰하도록 합니다

**in detail using the AskUserQuestion tool,**
AskUserQuestion 도구를 사용하여 상세하게

**focusing on failure modes and tradeoffs**
실패 모드와 트레이드오프에 집중하여

**rather than obvious questions.**
뻔한 질문보다는

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**D번**: Give Claude a brief description of the deployment goal and have it interview the architect in detail using the AskUserQuestion tool, focusing on failure modes and tradeoffs rather than obvious questions.

**정답 및 해설:**

**핵심 개념**: `AskUserQuestion` 도구 및 상호작용형 설계 (Interactive Clarification / Requirements Elicitation)
Claude Code가 처음 접하거나 복잡한 시스템(예: 다중 리전 배포)을 설계할 때, 사용자가 모든 예외 상황을 완벽히 수동으로 작성하는 대신, Claude가 `AskUserQuestion` 도구를 활용하여 인터뷰 형태로 필요한 질문을 상호작용하며 역으로 던지도록 유도하는 것이 권장되는 베스트 프랙티스입니다. 이를 통해 뻔하지 않은 실패 모드, 네트워크 파티션, 데이터 일관성 트레이드오프 등을 효과적으로 도출할 수 있습니다.

**문제 상황 분석:**
- Claude Code가 기존에 경험해보지 못한 서비스의 다중 리전 배포 전략을 수립해야함.
- 리전 장애, 네트워크 파티션 복구, 권한 리전 결정 등 복잡한 장애 모드(Failure modes) 고려사항을 구현 전에 미리 명확히 정의해야함.
- 아키텍트의 의도와 비즈니스 트레이드오프를 가장 효율적이고 구체적으로 수집/도출하는 가이드라인이 필요함.

**D번이 정답인 이유:**
아키텍트가 대략적인 목표를 제시하고, Claude Code가 `AskUserQuestion` 도구를 사용해 구체적인 장애 시나리오와 트레이드오프에 대해 아키텍트에게 역으로 질의(Interview)하도록 설정하는 것이 가장 효과적입니다. 이를 통해 놓치기 쉬운 에지 케이스를 사전에 함께 도출할 수 있습니다.

**오답 분석:**

- Option A (오답): 사전 검토 없이 구현을 진행하고 실제 운영 장애(Production Incident)에 의존하여 문제를 수정하는 것은 대단히 위험하고 부적절한 방식입니다.
- Option B (오답): 서비스의 특수성과 제약 조건을 고려하지 않고 일반적인 튜토리얼 수준의 기본 메커니즘을 그대로 적용하면 실제 시스템에 부합하지 않는 결함이 발생합니다.
- Option C (오답): 사람이 사전에 모든 시나리오와 세부사항을 혼자 문서화하는 것은 불필요하게 많은 공수가 들며, Claude의 분석적 탐색 능력 및 `AskUserQuestion`을 활용한 상호작용식 도출의 이점을 누리지 못합니다.

<br>

---

# 58번 문제

**1. 문제 원문**

A project ships a shared `.claude/skills/commit/SKILL.md` skill that writes commit messages in a style one developer finds too terse for their own habits. The developer wants a personal richer version of that skill while continuing to invoke the same `/commit` slash command themselves. Teammates should continue to see the original project skill when they run `/commit`. What should they do?

* **A)** Edit `.claude/skills/commit/SKILL.md` with the richer commit guidelines, and rely on the change remaining uncommitted so only the developer's local experience uses it, while teammates' copies are unaffected.
* **B)** Create `~/.claude/skills/commit/SKILL.md` as a personal copy with the same name to locally override the project skill for this developer only, leaving the shared project skill unchanged for teammates.
* **C)** Add `disable-model-invocation: true` to the shared `.claude/skills/commit/SKILL.md` so that it no longer generates output and only the developer's personal instructions apply.
* **D)** Create a differently named skill, such as `~/.claude/skills/commit-verbose/SKILL.md`, so it's invoked separately and the shared project skill remains untouched for teammates.

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A project ships**  
프로젝트가 제공합니다  

**a shared `.claude/skills/commit/SKILL.md` skill**  
공유되는 `.claude/skills/commit/SKILL.md` 스킬을  

**that writes commit messages**  
커밋 메시지를 작성하는  

**in a style**  
한 스타일로  

**one developer finds too terse**  
한 개발자가 너무 간결하다고 느끼는  

**for their own habits.**  
자신의 습관에 비추어.  

**The developer wants**  
이 개발자는 원합니다  

**a personal richer version**  
풍부한 내용을 담은 개인 버전의  

**of that skill**  
해당 스킬을  

**while continuing to invoke**  
계속해서 호출하면서  

**the same `/commit` slash command**  
동일한 `/commit` 슬래시 명령어를  

**themselves.**  
직접.  

**Teammates should continue**  
팀원들은 계속 유지해야 합니다  

**to see the original project skill**  
원래의 프로젝트 스킬을 확인하는 것을  

**when they run `/commit`.**  
그들이 `/commit`을 실행할 때.  

**What should they do?**  
개발자는 무엇을 해야 합니까?  

---

**OPTIONS:**

**Option A:**

**Edit `.claude/skills/commit/SKILL.md`**  
`.claude/skills/commit/SKILL.md`를 수정합니다  

**with the richer commit guidelines,**  
더 풍부한 커밋 가이드라인으로,  

**and rely on the change**  
그리고 변경 사항이 남도록 의존합니다  

**remaining uncommitted**  
커밋되지 않은 상태로  

**so only the developer's local experience**  
개발자의 로컬 환경만  

**uses it,**  
이것을 사용하도록,  

**while teammates' copies**  
반면에 팀원들의 복사본은  

**are unaffected.**  
영향을 받지 않는 동안.  

**Option B:**

**Create `~/.claude/skills/commit/SKILL.md`**  
`~/.claude/skills/commit/SKILL.md`를 생성합니다  

**as a personal copy**  
개인 복사본으로서  

**with the same name**  
동일한 이름의  

**to locally override**  
로컬에서 재정의(덮어쓰기)하기 위해  

**the project skill**  
프로젝트 스킬을  

**for this developer only,**  
이 개발자에게만,  

**leaving the shared project skill**  
공유 프로젝트 스킬은 남겨둔 채  

**unchanged for teammates.**  
팀원들에게 변경 없이.  

**Option C:**

**Add `disable-model-invocation: true`**  
`disable-model-invocation: true`를 추가합니다  

**to the shared `.claude/skills/commit/SKILL.md`**  
공유되는 `.claude/skills/commit/SKILL.md`에  

**so that it no longer generates output**  
더 이상 출력을 생성하지 않도록  

**and only the developer's personal instructions apply.**  
그리고 개발자의 개인 지침만 적용되도록.  

**Option D:**

**Create a differently named skill,**  
다른 이름의 스킬을 생성합니다,  

**such as `~/.claude/skills/commit-verbose/SKILL.md`,**  
`~/.claude/skills/commit-verbose/SKILL.md`와 같은,  

**so it's invoked separately**  
별도로 호출되도록  

**and the shared project skill**  
그리고 공유 프로젝트 스킬이  

**remains untouched for teammates.**  
팀원들에게는 건드려지지 않은 채 남도록.  

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**  
**B번**: Create `~/.claude/skills/commit/SKILL.md` as a personal copy with the same name to locally override the project skill for this developer only, leaving the shared project skill unchanged for teammates.

**정답 및 해설:**

**핵심 개념**: Claude Code 스킬 우선순위 (Skill Precedence & Overriding)  
Claude Code의 스킬 우선순위 계층 구조는 `Enterprise > Personal (~/.claude/skills/) > Project (.claude/skills/) > Plugin` 순을 따릅니다. 동일한 이름을 가진 스킬이 여러 범위에 존재할 경우, 상위 레벨인 Personal(사용자/개인) 레벨의 스킬이 Project(프로젝트) 레벨의 스킬을 오버라이드(Override)합니다.

**문제 상황 분석:**
- 프로젝트 공유 저장소에 팀 공용 스킬인 `.claude/skills/commit/SKILL.md`가 설정되어 있음.
- 특정 개발자가 기존 `/commit` 명령어 이름을 그대로 유지하면서 자신만의 풍부한 커밋 스타일을 사용하고자 함.
- 다른 팀원들에게는 프로젝트 공유 스킬이 그대로 유지되어 영향을 주지 않아야 함.

**B번이 정답인 이유:**  
개인 설정 디렉터리(`~/.claude/skills/commit/SKILL.md`)에 동일한 이름으로 커스텀 스킬을 작성하면, Claude Code의 우선순위 규칙에 따라 해당 개발자의 로컬 환경에서는 개인 스킬이 프로젝트 스킬을 오버라이드하여 실행됩니다. 한편, 프로젝트 저장소 파일은 변경되지 않으므로 팀원들은 기존 프로젝트 스킬을 계속 사용하게 됩니다.

**오답 분석:**

- Option A (오답): 프로젝트 내 파일(.claude/skills/commit/SKILL.md)을 지저분하게 uncommitted 상태로 남겨두는 것은 추후 실수로 커밋되거나 `git clean` / branch switching 시 날아갈 위험이 있어 올바른 구성 방식이 아닙니다.
- Option C (오답): 공유 파일에 `disable-model-invocation: true`를 추가하면 변경 사항을 저장소에 올리지 않더라도 비정식 접근이며, 커밋 시 다른 팀원 전체에게 영향을 주게 됩니다.
- Option D (오답): 개발자가 기존에 쓰던 `/commit` 슬래시 커맨드를 그대로 유지하고 싶어 한다는 요구사항에 위배됩니다. 다른 커맨드 이름(`/commit-verbose`)을 새로 만들어야 하기 때문입니다.

<br>

---

# 59번 문제

**1. 문제 원문**

A repository already has an AGENTS.md file used by several other AI coding tools, containing conventions the team wants Claude Code to follow as well, plus a short list of Claude-specific instructions like 'use plan mode for changes under src/billing/'. The team wants to avoid maintaining the same conventions in two places. What is the recommended way to structure CLAUDE.md?

* **A)** Manually copy the full contents of AGENTS.md into CLAUDE.md today, and remember to re-copy it by hand every time AGENTS.md changes
* **B)** Create CLAUDE.md starting with `@AGENTS.md` as an import, followed by the Claude-specific instructions such as the plan-mode rule underneath
* **C)** Leave CLAUDE.md absent entirely, since Claude Code silently falls back to reading AGENTS.md whenever no CLAUDE.md file is present
* **D)** Create a symbolic link (symlink) named CLAUDE.md pointing to AGENTS.md so both files always share the same content

---

**2. 구간별 직독직해 번역**

**QUESTION:**

**A repository already has**
저장소에는 이미 존재합니다

**an AGENTS.md file**
AGENTS.md 파일이

**used by several other**
다른 여러

**AI coding tools,**
AI 코딩 도구에 의해 사용되는,

**containing conventions**
컨벤션들을 포함하고 있는

**the team wants Claude Code**
팀이 Claude Code도

**to follow as well,**
따르기를 원하는,

**plus a short list**
이와 함께 짧은 목록을 (포함하여)

**of Claude-specific instructions**
Claude 전용 지침들의

**like 'use plan mode**
'플랜 모드를 사용하라'와 같은

**for changes under src/billing/'.**
src/billing/ 하위의 변경에 대해.

**The team wants to avoid**
팀은 피하고 싶어 합니다

**maintaining the same conventions**
동일한 컨벤션을 유지 관리하는 것을

**in two places.**
두 곳에서.

**What is the recommended way**
권장되는 방법은 무엇입니까

**to structure CLAUDE.md?**
CLAUDE.md를 구성하는?

---

**OPTIONS:**

**Option A:**

**Manually copy**
수동으로 복사합니다

**the full contents of AGENTS.md**
AGENTS.md의 전체 내용을

**into CLAUDE.md today,**
오늘 CLAUDE.md 안으로,

**and remember to re-copy it**
그리고 다시 복사할 것을 기억합니다

**by hand**
손으로 직접

**every time AGENTS.md changes**
AGENTS.md가 변경될 때마다

**Option B:**

**Create CLAUDE.md**
CLAUDE.md를 생성합니다

**starting with `@AGENTS.md`**
`@AGENTS.md`로 시작하여

**as an import,**
임포트(불러오기) 문으로서,

**followed by the Claude-specific instructions**
그리고 Claude 전용 지침이 뒤따르도록

**such as the plan-mode rule**
플랜 모드 규칙과 같은

**underneath**
그 바로 아래에

**Option C:**

**Leave CLAUDE.md absent entirely,**
CLAUDE.md를 완전히 두지 않고 비워둡니다,

**since Claude Code silently falls back**
Claude Code가 소리 없이 대체(폴백)하여 읽기 때문에

**to reading AGENTS.md**
AGENTS.md를 읽는 것으로

**whenever no CLAUDE.md file is present**
CLAUDE.md 파일이 존재하지 않을 때마다

**Option D:**

**Create a symbolic link (symlink)**
심볼릭 링크(symlink)를 생성합니다

**named CLAUDE.md**
CLAUDE.md라는 이름의

**pointing to AGENTS.md**
AGENTS.md를 가리키는

**so both files always share**
두 파일이 항상 공유하도록

**the same content**
동일한 내용을

---

**3. 정답 및 해설 (Answer & Explanation)**

**정답:**

**B번**: Create CLAUDE.md starting with `@AGENTS.md` as an import, followed by the Claude-specific instructions such as the plan-mode rule underneath

**정답 및 해설:**

**핵심 개념**: CLAUDE.md에서의 파일 가져오기 구문 (`@import` Syntax)
Claude Code 지침 파일(`CLAUDE.md`)에서는 `@path/to/file.md` 형태의 구문을 사용하여 다른 마크다운 지침 파일의 내용을 동적으로 포함(Import)시킬 수 있습니다. 이를 통해 공통 프로젝트 컨벤션을 단일 파일(예: `AGENTS.md`)에서 중앙 관리하고, Claude 전용 규칙만 `CLAUDE.md`에 추가하여 중복 관리를 방지할 수 있습니다.

**문제 상황 분석:**
- 저장소에 이미 타 AI 도구들과 공유하는 공통 지침 파일 `AGENTS.md`가 존재함.
- 공통 컨벤션을 두 곳에서 이중으로 유지관리하는 오버헤드를 피해야 함.
- 동시에 `src/billing/` 경로에 대한 플랜 모드 사용 규칙 등 Claude Code에만 적용되는 고유 지침을 함께 추가해야 함.

**B번이 정답인 이유:**
`CLAUDE.md` 파일 상단에 `@AGENTS.md`를 기재하면 Claude Code가 실행될 때 `AGENTS.md`의 내용을 자동으로 참조/불러옵니다. 그 하단에 Claude 전용 지침을 덧붙여 작성하면, 공통 지침의 단일 출처(Single source of truth)를 유지하면서도 Claude 전용 규칙을 완벽히 병합하여 전달할 수 있습니다.

**오답 분석:**

- Option A (오답): 변경사항이 생길 때마다 수동으로 수복사하는 방식은 동기화 누락 및 유지보수 문제를 일으키는 잘못된 방식입니다.
- Option C (오답): `CLAUDE.md`를 아예 삭제하면 Claude 전용으로 추가해야 하는 지침(플랜 모드 규칙 등)을 정의할 공간이 사라집니다.
- Option D (오답): 심볼릭 링크를 생성하면 `CLAUDE.md`와 `AGENTS.md`가 100% 동일한 내용만 갖게 되므로, Claude 전용 지침을 별도로 추가하여 확장할 수 없습니다.