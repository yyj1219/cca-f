SCENARIO: You are using Claude Code to accelerate software development. Your team uses it for code generation, refactoring, debugging, and documentation. You need to integrate it into your development workflow with custom slash commands, CLAUDE.md configurations, and understand when to use plan mode vs direct execution.

QUESTION: Your team's custom /audit-summary slash command concatenates static analysis findings from about 60 source files into one long prompt (roughly 80K tokens) and asks Claude to produce a prioritized remediation report. Reviewing the reports, you notice that issues from files appearing early and late in the concatenated input are consistently covered, while critical findings from files in **the middle are frequently omitted**, even when they are the highest severity. Which change to the slash command most directly addresses this failure?

**선택지**:
1. Sort the concatenated file reports alphabetically by filename so the ordering of sections is deterministic across every run
2. Add an instruction to the prompt directing the model to weigh all sections of the input equally regardless of their position
3. Restructure the input to **open with** a key-findings summary and organize per-file details under explicit section headings => 정답
4. Increase the max_tokens setting on the request so the model has enough room to discuss every file's findings in the report

**설명**:
- 로스트인더미들 보완방법
- 프롬프트 입력 시작 부분(모델의 Attention이 가장 높은 위치)에 핵심 발견 사항 요약(Key-findings summary)을 전면 배치(Open with)하고, 세부 내용은 명시적인 섹션 헤더(Explicit section headings)를 활용하여 명확하게 구조화해주어야 합니다.

**문제 구분 포인트, 어려운 표현**:
